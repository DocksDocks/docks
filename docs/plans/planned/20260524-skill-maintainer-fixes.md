---
title: Fix skill-maintainer timestamps and enforce references/ in /docs
status: planned
goal: Bump updated: only on semantic content change, and make /docs reliably generate references/ subdirs when SKILL.md bodies exceed sweet spot
created: 2026-05-24
updated: 2026-05-24
started_at: null
assignee: null
blockers: []
blocked_reason: null
blocked_since: null
ship_commit: null
tags: [skill-maintenance, bugfix, docs-command]
affected_paths:
  - plugins/docks/skills/productivity/agents/SKILL.md
  - plugins/docks/agents/docs-skills-builder.md
  - plugins/docks/agents/docs-verifier.md
  - plugins/docks/agents/docs-explorer.md
  - scripts/guard-skills.sh
  - scripts/skill-content-hash.sh
  - scripts/ci.sh
related_plans: [foundation-categorization-scoring, tree-skill]
review_status: null
---

# Fix skill-maintainer timestamps and enforce references/ in /docs

## Goal

Two fixes to the existing skill-maintenance machinery:

1. **Idempotent updates.** The maintainer bumps `metadata.updated:` only when a skill's *meaning* changes (normalized SKILL.md body or normalized references/ content). Re-running on an unchanged skill is a zero-file-write no-op via a stored `content_hash:` in frontmatter.
2. **References/ generation discipline.** `/docs` Phase 3 (Skills Builder) must produce a `references/<topic>.md` split whenever a generated SKILL.md body would exceed 310 lines; Phase 6 (Verifier) hard-fails skills that exceed 310 lines with no references/.

## Context

**Timestamp drift.** Today the maintainer rewrites a SKILL.md and bumps `updated:` even when the rewrite is semantically a no-op (e.g., a sibling context file changed, or a regeneration produced identical output). Every skill always looks "recently updated," so the field can't be used to triage which skills genuinely need a refresh. This compounds against the tree-skill plan's PostToolUse refresh hook — without a semantic-change predicate, the hook will write-loop on every edit.

**References/ gap.** The plugin's own skills follow a strong pattern — SKILL.md body ≤310 lines (sweet spot), depth content split into `references/<topic>.md` (30–150 lines each). The `docs-skills-builder` agent's brief says it should generate references/, but in practice user-project skills generated by `/docs` often end up as monolithic SKILL.md files with no references/. Either the builder doesn't reliably split, or the verifier doesn't catch missing references when SKILL.md exceeds sweet spot. Bring the consumer-side generation up to the standard the plugin itself enforces.

The two fixes ship together because they both touch `/docs` Phase 3/5/6 and the skill-maintenance skill — splitting them would mean two PRs that touch the same files.

## Steps

| # | Task | Depends | Parallel | Status | Owner |
|---|---|---|---|---|---|
| 1 | Define "semantic change" predicate (table below) and document in CLAUDE.md or successor location | — | with #2 | planned | self |
| 2 | Add `metadata.content_hash:` as a valid optional frontmatter field in guard-skills.sh | — | with #1 | planned | self |
| 3 | Write `scripts/skill-content-hash.sh <skill-dir>` — normalizes and SHA-256s | 1 | — | planned | self |
| 4 | Update skill-maintenance skill / agent to compute proposed hash before write, skip write if equal to stored | 3 | with #5 | planned | self |
| 5 | Add `--check-only` flag to maintainer — reports per skill: `unchanged` / `would-bump <field>`, non-zero exit if any would-bump | 3 | with #4 | planned | self |
| 6 | Update `docs-skills-builder` (Phase 3): MUST split into references/ if body would exceed 310 lines | — | with #7 | planned | self |
| 7 | Update `docs-verifier` (Phase 6): hard-fail body 310–500 lines without references/; existing >500 hard-fail stays | — | with #6 | planned | self |
| 8 | Backfill `content_hash:` on every existing plugin skill via one-pass run | 4 | — | planned | self |
| 9 | Add test `tests/skill-maintainer-idempotency.sh` — re-run maintainer, assert `git status` shows zero changes | 4, 8 | — | planned | self |
| 10 | `bash scripts/ci.sh` green including new test | 8, 9 | — | planned | self |
| 11 | `./scripts/release.sh patch` | 10 | — | planned | self |

### Step details

- **#1** — Predicate table (cheapest-first):
  | Change | Bump `updated:`? | Reason |
  |---|---|---|
  | Frontmatter `description:` text differs | YES | Load-bearing for triggering |
  | Frontmatter `name:` differs | YES | Should never happen, treat as semantic |
  | Frontmatter `allowed-tools:` / `tools:` differs | YES | Capability surface change |
  | Frontmatter `model:` differs | YES | Behavior change |
  | SKILL.md body changes after whitespace normalization | YES | Real content |
  | `references/<name>.md` added/removed | YES | Body-equivalent |
  | Existing `references/<name>.md` body changes after normalization | YES | Real content |
  | Only `metadata.updated:` itself changes | NO | Tautological — write loop |
  | Whitespace-only edits anywhere | NO | Cosmetic |
  | Files outside the skill directory change | NO | Not the skill's content |
- **#3** — "normalize" = strip trailing whitespace per line, collapse blank-line runs, LF line endings. Hash inputs: frontmatter (excluding `updated:` and `content_hash:`) + body + sorted+concatenated `references/*.md` normalized bodies. Output: lowercase hex SHA-256.
- **#4** — Skill-maintenance logic: compute proposed hash → compare to existing `content_hash:` → if equal, skip write entirely (log "no-op (content unchanged)") → if different, write changes + bump `updated:` to today + store new hash.
- **#5** — `--check-only` is what the tree-skill PostToolUse hook calls to decide whether to nudge a refresh; no-op when nothing semantic changed.
- **#7** — Verifier rules after change:
  - body > 500 lines → hard fail (existing)
  - body 310–500 lines AND no references/ → hard fail (NEW)
  - body ≤ 310 lines → no requirement
- **#8** — One-pass backfill computes hash for every existing skill and writes it as frontmatter. After this, the bug is structurally gone.

## Acceptance criteria

- [ ] `metadata.content_hash:` accepted as optional frontmatter; guard-skills.sh passes
- [ ] `scripts/skill-content-hash.sh` deterministic — same input twice → same hash
- [ ] Re-running skill-maintenance on an unchanged skill produces zero file writes (`tests/skill-maintainer-idempotency.sh` green)
- [ ] `--check-only` mode reports per-skill status without writing
- [ ] `/docs` Phase 6 hard-fails skills with body 310–500 lines and no references/
- [ ] Every existing plugin skill has `content_hash:` backfilled
- [ ] `bash scripts/ci.sh` green

## Out of scope

- Migration of existing *user-project* skills that lack references/ — call out in `/docs` audit phase, do not auto-rewrite
- Plugin-shipped skills already follow the pattern; no migration needed there
- Applying the same predicate to agent files — if agents have `metadata.updated:`, fold into this plan; if not, defer

## Mistakes & Dead Ends

(none yet — plan freshly written)

## Sources

- agentskills.io spec — SKILL.md body ≤500 lines, sweet spot 80–310 (rationale for the 310 split threshold)
- `plugins/docks/agents/docs-skills-builder.md` (repo) — current Phase 3 brief that should produce references/ but doesn't reliably
- `plugins/docks/agents/docs-verifier.md` (repo) — current Phase 6 verifier checks; new body-density-vs-references check added here
- `plugins/docks/skills/agents/SKILL.md` (repo) — current skill-maintenance machinery to extend with the predicate

## Blockers

(none — actionable immediately)

## Notes

- Open questions:
  - References-split threshold: 310 (sweet-spot ceiling) or more permissive (400)? Lean: 310 to match what the plugin's own skills enforce.
  - Hash mismatch when a `references/<topic>.md` is listed in SKILL.md but missing on disk (or vice versa): the hash predicate naturally catches it; also surface as validator warning.
  - Agents may have the same drift — confirm during step 1 whether agent `metadata.updated:` exists and is bumped on no-op rewrites.
- Pairs with tree-skill plan: the tree PostToolUse hook calls `tree refresh <node>` which in turn (for skills) calls the maintainer with `--check-only` to decide whether to act. Without this plan landing first, that hook write-loops.

## Review

(filled by plan-review on completion)
