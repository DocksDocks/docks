---
title: Publish Session Relay 0.13.0 and public companion
goal: Correct the legacy publication protocol and dynamic public fixture, publish and verify both bound releases, promote Docks without rollback, finalize stable, and archive both reviewed plans.
status: blocked
created: "2026-07-23T12:31:06-03:00"
updated: "2026-07-24T01:34:24-03:00"
started_at: "2026-07-23T12:58:23-03:00"
assignee: null
review_author_company: openai
review_author_tool: codex
review_author_model: gpt-5.6-sol
review_author_effort: high
review_waivers: []
tags: [session-relay, release, publication, supply-chain]
affected_paths:
  - docs/plans/active/session-relay-linux-workspace-publication.md
  - scripts/lib/session-relay-release-preparation.mjs
  - scripts/lib/session-relay-release-publication.mjs
  - scripts/lib/session-relay-release-promotion.mjs
  - plugins/session-relay/test/release-evidence-contract.mjs
  - plugins/session-relay/test/companion-distribution-contract.mjs
  - plugins/session-relay/test/release-publication-contract.mjs
  - plugins/session-relay/test/release-promotion-contract.mjs
  - /home/vagrant/projects/public/docs/plans/active/session-relay-cli-0.13.0-production-release.md
  - /home/vagrant/projects/public/package.json
  - /home/vagrant/projects/public/SoT/toolchain.json
  - /home/vagrant/projects/public/cli/src/generated/sotPayload.ts
  - /home/vagrant/projects/public/cli/test/lib/harness.ts
related_plans:
  - docs/plans/finished/2026-07-23-session-relay-linux-workspace-recertification.md
  - /home/vagrant/projects/public/docs/plans/active/session-relay-cli-0.13.0-release-preparation.md
  - /home/vagrant/projects/public/docs/plans/finished/2026-07-18-session-relay-cli-production-release.md
review_status: null
planned_at_commit: cdca867e6a140311ea865a81229fb30de1df32c1
execution_base_commit: f28ae1fdf6976ddfb063752bcc4f06b96714a13b
blocked_reason: Focused public unit gate is blocked because cli/test/lib/harness.ts still materializes hardcoded session-relay 0.12.0 instead of the current verified SoT version.
blocked_since: "2026-07-24T01:32:48-03:00"
---

# Publish Session Relay 0.13.0 and public companion

## Goal

Correct the legacy public-release identity in Docks before any tag, Release, branch, transaction-ref, or stable-state mutation. Then independently review and start this plan; preserve and bind the completed recertified source proof; publish exactly one five-asset Session Relay `0.13.0` prerelease; create, independently review, execute, completion-review, and archive the public `0.10.2` production child plan; verify its exact six-asset `cli-v0.10.2` release; promote the reviewed Docks implementation without rolling `origin/main` backward; finalize Session Relay stable; run live install, version, self-test, and workspace-boundary checks; completion-review and archive this plan.

Success matters because the recertified product source and public companion are ready, but the current release tooling still hard-codes the superseded public `0.9.0` identity and equates `promoted_commit` with the earlier shipped archive. Those guards correctly fail closed today; publication is forbidden until test-first protocol changes bind the actual `6c07f9...` companion, the fresh `0.10.2`/`cli-v0.10.2` public identity, and the later publication implementation commit.

## Context & rationale

The completed source authority is `docs/plans/finished/2026-07-23-session-relay-linux-workspace-recertification.md` at archive commit `cdca867e6a140311ea865a81229fb30de1df32c1`. Its embedded candidate SHA-256 is `75e5bf5386a203cd81e3930ca2309ceed4e1a665d995848a29eb73a0fa5cb395`; its recertified product source/tag commit is `3fb9211f3309977f24853a10714d4b7a82b38c8f`; its immutable producer ref is `refs/heads/preflight/session-relay-0.13.0-3fb9211f3309`; producer run `30016169824` and source-CI run `30016063891` succeeded, with receipt digests `a9bd19d988285663b50e92a9ef4662dbada3fa078581cf63090a517448550ac4` and `8da93f817ceceb8b50d189b638afbe8d88041afaae4c7e4c375cb6f893933069`. The binder must derive the reviewed evidence commit from the embedded passed completion receipt; no copied evidence-commit value is authority.

The immutable public companion is repository `DocksDocks/public`, ref `refs/heads/preflight/session-relay-cli-0.13.0-6c07f9bc02ef`, commit `6c07f9bc02ef7a0a26b8ffb539c16c42a87a3172`. Its blocked preparation plan is `docs/plans/active/session-relay-cli-0.13.0-release-preparation.md`, with exact reason `Awaiting the four independently hashed \`session-relay--v0.13.0\` production asset digests.`, plan input SHA-256 `818766be3668ad02bfce234cdb25e5d65bf0760bd7c7b2aea05fb8f075a99ed3`, draft-review receipt SHA-256 `097206c0611c3357e10c0bf69a70819ea67901ef1ae8c3ef1d9e8207520f7c52`, and public red receipt SHA-256 `833e777a509b44584f873628a65212fd92bd8a9305cd2f5f6699fc172738402c`. Public `origin/main` `6f9691cc19349ccd0ce81e8c8bf5cc573f76f3f1` is the companion commit's merge base and ancestor, so the child plan can continue from the immutable companion without discarding main.

The prior public identity `0.10.1`/`cli-v0.10.1` is an observed prior release, not an available target: remote tag `refs/tags/cli-v0.10.1` already exists at `6f9691cc19349ccd0ce81e8c8bf5cc573f76f3f1`, and its stable GitHub Release was published `2026-07-23T02:07:17Z`. Exact revalidation confirms `git ls-remote --tags origin refs/tags/cli-v0.10.1 'refs/tags/cli-v0.10.1^{}'` returns only the lightweight tag row `6f9691cc19349ccd0ce81e8c8bf5cc573f76f3f1` (no peeled `^{}` object), and the observed npm `docks-kit` version list ends at `0.10.1`, leaving `0.10.2` unpublished and free on npm. That release is a prior historical docks-kit release whose `SoT/toolchain.json` still pins Session Relay `0.12.0`; it predates immutable companion `6c07f9...` and is not evidence from this effort. Because the existing tag target can never descend from the companion, `0.10.1`/`cli-v0.10.1` cannot be reused and remains untouched — no deletion, retag, rewrite, or reuse of that tag, its Release, or `docks-kit@0.10.1` on npm is authorized. This plan therefore binds the fresh public identity `0.10.2`/`cli-v0.10.2` throughout.

The protocol mismatch is mandatory pre-publication work, not a documentation discrepancy. `scripts/lib/session-relay-release-promotion.mjs` currently fixes `PUBLIC_VERSION='0.9.0'`, `PUBLIC_TAG='cli-v0.9.0'`, companion base `c3b542220d5a24a98ca05383bbe28afc2319b7e2`, finished plan `2026-07-18-session-relay-cli-production-release.md`, and `DOCKS_KIT_RELEASE='cli-v0.9.0'`. The existing `cli-v0.9.0` tag commit is not descended from the new companion, while `SourcePreparationProofV1.public_reviewed_commit` is `6c07f9...`; current public verification and promotion must fail. Tests must first demonstrate that failure, then production code may be rebound to public package version `0.10.2`, tag/release `cli-v0.10.2`, companion `6c07f9...`, and the new child-plan slug.

`SourcePreparationProofV1` remains a closed schema with no field additions, removals, renames, defaults, or reserialization. Its meanings are refined: `tag_commit` remains the recertified product source `3fb9211...`; `shipped_commit` remains the finished recertification archive commit derived from Git history; `promoted_commit` is the later Docks publication implementation commit. This allows promotion to move or retain main at the exact reviewed publication implementation instead of rolling it back to the source archive.

The sole Session Relay prerelease workflow has now completed, but publication receipt binding exposed one deterministic GitHub timestamp semantic mismatch. Immutable read-back shows lightweight tag `session-relay--v0.13.0` at `3fb9211f3309977f24853a10714d4b7a82b38c8f`; exactly one matching push run `30061627175`, attempt `1`, started `2026-07-24T02:26:14Z`, updated `2026-07-24T02:28:56Z`, and concluded success; and prerelease id `359049927` with exactly five assets, `published_at` `2026-07-24T02:28:53Z`, and every asset `created_at` `2026-07-24T02:28:52Z`. The Release `created_at` is `2026-07-23T14:26:39Z`, exactly the tagged head commit timestamp and necessarily before the only workflow. Initial A12 publication and one legal receiptless rebind both failed only with `release created_at timestamp is outside the bound workflow run window`; neither emitted `$PUBLICATION_RECEIPT`, selected or dispatched another run, or changed the existing tag, run, prerelease, or assets. This amendment corrects that validator defect test-first while preserving all remote evidence.

## Environment & how-to-run

- Docks repository/worktree: `/home/vagrant/projects/docks`, branch `main`; creation base `cdca867e6a140311ea865a81229fb30de1df32c1`; authorized reviewed current-main base `25592c6550069e300a7a0148d3cd3c21880da8e7`.
- Public repository/worktree: `/home/vagrant/projects/public`, repository id `DocksDocks/public`; immutable child base `6c07f9bc02ef7a0a26b8ffb539c16c42a87a3172`; observed public production main `6f9691cc19349ccd0ce81e8c8bf5cc573f76f3f1`; immutable preparation ref `refs/heads/preflight/session-relay-cli-0.13.0-6c07f9bc02ef` resolves to the child base.
- Runtimes: Node `24`, pnpm with frozen lockfile, Rust `1.85.0`; public Bun `1.3.14`, Vitest `3.2.7`, TypeScript `7.0.2`; authenticated GitHub CLI with required repository write permissions only when a mutation step is reached.
- Fixed release identities: Session Relay version/tag `0.13.0` / `session-relay--v0.13.0`; public package version/tag `0.10.2` / `cli-v0.10.2`, bumped from the observed prior `0.10.1` release, which remains untouched; Docks is already independently released at `0.13.4` by commit `25592c6550069e300a7a0148d3cd3c21880da8e7`, and Session Relay remains `0.13.0`.
- Before work, fetch read-only state and revalidate every fixed commit/ref/tag/Release/workflow observation. Never replace a fixed identity with ambient `HEAD`, a mutable branch, a copied digest, or “latest.”
- Create one fresh receipt directory and direct-child outputs:

```bash
set -euo pipefail
umask 077
RECEIPT_ROOT="${XDG_STATE_HOME:-$HOME/.local/state}/docks-release/session-relay-0.13.0"
install -d -m 700 "$RECEIPT_ROOT"
RECEIPT_DIR="$(mktemp -d "$RECEIPT_ROOT/publication.XXXXXX")"
RECEIPT_DIR="$(realpath -e -- "$RECEIPT_DIR")"
chmod 700 "$RECEIPT_DIR"
test "$(stat -Lc '%a' -- "$RECEIPT_DIR")" = 700
test "$(stat -Lc '%u' -- "$RECEIPT_DIR")" = "$(id -u)"
SOURCE_PROOF="$RECEIPT_DIR/source-proof.json"
PUBLICATION_RECEIPT="$RECEIPT_DIR/publication.json"
PUBLIC_REQUEST="$RECEIPT_DIR/public-request.json"
PUBLIC_RELEASE_RECEIPT="$RECEIPT_DIR/public-release.json"
PROMOTION_RECEIPT="$RECEIPT_DIR/promotion.json"
FINALIZATION_RECEIPT="$RECEIPT_DIR/finalization.json"
PROTOCOL_RED_RECEIPT="$RECEIPT_DIR/protocol-red.json"
ARCHIVE_FENCE_RED_RECEIPT="$RECEIPT_DIR/archive-fence-red.json"
LEGACY_POLICY_RED_RECEIPT="$RECEIPT_DIR/legacy-policy-red.json"
LEGACY_POLICY_FORMATTED_RED_RECEIPT="$RECEIPT_DIR/legacy-policy-formatted-red.json"
test ! -e "$LEGACY_POLICY_RED_RECEIPT"
test ! -e "$LEGACY_POLICY_FORMATTED_RED_RECEIPT"
export LEGACY_POLICY_RED_RECEIPT LEGACY_POLICY_FORMATTED_RED_RECEIPT
for output in "$SOURCE_PROOF" "$PUBLICATION_RECEIPT" "$PUBLIC_REQUEST" "$PUBLIC_RELEASE_RECEIPT" "$PROMOTION_RECEIPT" "$FINALIZATION_RECEIPT" "$PROTOCOL_RED_RECEIPT" "$ARCHIVE_FENCE_RED_RECEIPT"; do test ! -e "$output"; done
export SOURCE_PROOF PUBLICATION_RECEIPT PUBLIC_REQUEST PUBLIC_RELEASE_RECEIPT PROMOTION_RECEIPT FINALIZATION_RECEIPT PROTOCOL_RED_RECEIPT ARCHIVE_FENCE_RED_RECEIPT
export SOURCE_PROOF_SHA256 SOURCE_PROOF_V2_SHA256 PUBLICATION_SHA256 PUBLIC_REQUEST_SHA256 PUBLIC_RELEASE_SHA256 PROMOTION_SHA256 FINALIZATION_SHA256 PUBLICATION_IMPLEMENTATION_COMMIT EXPECTED_ORIGIN_MAIN
```

The fresh receipt-directory setup above was consumed before the first legacy-policy red. Formatter recovery MUST reuse the retained owner-only receipt directory rather than create another root:

```bash
RECEIPT_DIR="$(realpath -e -- "${XDG_STATE_HOME:-$HOME/.local/state}/docks-release/session-relay-0.13.0/publication.Y2tqxo")"
test "$(stat -Lc '%a' -- "$RECEIPT_DIR")" = 700
test "$(stat -Lc '%u' -- "$RECEIPT_DIR")" = "$(id -u)"
LEGACY_POLICY_RED_RECEIPT="$RECEIPT_DIR/legacy-policy-red.json"
LEGACY_POLICY_FORMATTED_RED_RECEIPT="$RECEIPT_DIR/legacy-policy-formatted-red.json"
SOURCE_PROOF="$RECEIPT_DIR/source-proof.json"
SOURCE_PROOF_V2="$RECEIPT_DIR/source-proof-v2.json"
PUBLICATION_RECEIPT="$RECEIPT_DIR/publication.json"
PUBLICATION_TIMESTAMP_RED_RECEIPT="$RECEIPT_DIR/publication-timestamp-red-v3.json"
test "$(sha256sum "$SOURCE_PROOF" | cut -d' ' -f1)" = 419b23ccdcf0ca21672e81c05ae9d22c55bc67781839ffb6a29e7eecc2b59396
test ! -e "$SOURCE_PROOF_V2"
test ! -e "$PUBLICATION_RECEIPT"
test "$(sha256sum "$PUBLICATION_TIMESTAMP_RED_RECEIPT" | cut -d' ' -f1)" = f87a248cdc93760d4734229dfdc414f750b19bfe965b0e717e5ffa19f4a52d5d
test "$(sha256sum "$LEGACY_POLICY_RED_RECEIPT" | cut -d' ' -f1)" = eb498e500685f86b00807482bdbf32c710653b138d30fae2c8ae5baca116090e
test ! -e "$LEGACY_POLICY_FORMATTED_RED_RECEIPT"
export RECEIPT_DIR SOURCE_PROOF SOURCE_PROOF_V2 PUBLICATION_RECEIPT PUBLICATION_TIMESTAMP_RED_RECEIPT LEGACY_POLICY_RED_RECEIPT LEGACY_POLICY_FORMATTED_RED_RECEIPT
```

Never delete, overwrite, rename, or recapture `$LEGACY_POLICY_RED_RECEIPT`. `$LEGACY_POLICY_FORMATTED_RED_RECEIPT` is its distinct no-clobber successor in the same receipt root. Likewise, preserve immutable `$SOURCE_PROOF` v1 at SHA-256 `419b23ccdcf0ca21672e81c05ae9d22c55bc67781839ffb6a29e7eecc2b59396`; `$PUBLICATION_TIMESTAMP_RED_RECEIPT` is the authoritative immutable timestamp red at SHA-256 `f87a248cdc93760d4734229dfdc414f750b19bfe965b0e717e5ffa19f4a52d5d`, while `$SOURCE_PROOF_V2` remains a distinct nonexistent direct-child output. Source proof v2 becomes authoritative only after it binds the corrected final commit; no command may overwrite or reinterpret v1.

Every release mode takes exactly one positional `0.13.0`, exact `--plugin session-relay`, and each receipt input is immediately followed by its paired `--*-sha256`. Every output is a nonexistent direct child of the owner-only directory and must be canonical RFC 8785 JCS, mode `0600`, nonempty, and have no trailing newline. Capture each printed lowercase 64-hex digest, compare it with `sha256sum`, and never parse/reserialize receipt bytes.

## Interfaces & data shapes

### Corrected source-proof semantics

Keep the existing exact `SourcePreparationProofV1` field set. The implementation must enforce:

```text
source_commit   = candidate.source_commit = 3fb9211f3309977f24853a10714d4b7a82b38c8f
tag_commit      = source_commit
evidence_commit = completion receipt reviewed_head, derived from the finished plan
shipped_commit  = commit that archived the exact finished recertification-plan bytes
promoted_commit = current clean Docks HEAD after the publication protocol implementation
public_reviewed_commit = 6c07f9bc02ef7a0a26b8ffb539c16c42a87a3172
```

The binder verifies `source -> evidence -> shipped -> promoted == current HEAD` ancestry. `source_ancestry` and `non_plan_tree_equivalence` retain their closed shapes and existing source/evidence/shipped meanings. Offline proof validation permits `promoted_commit != shipped_commit` only when these producer-bound invariants hold; a hand-authored proof cannot opt into the relaxation.

The shipped-to-promoted changed-path set is closed and exact. It contains the seven release-owned paths plus the independently reviewed and released Docks schema/current-main tail already present at authorized base `25592c6550069e300a7a0148d3cd3c21880da8e7`:

```text
.claude-plugin/marketplace.json
.codex/agents/plan-manager.toml
.codex/agents/plan-reviewer.toml
AGENTS.md
README.md
docs/plans/AGENTS.md
docs/plans/active/session-relay-linux-workspace-publication.md
docs/scaffold/templates/codex-plan-manager.toml.template
docs/scaffold/templates/codex-plan-reviewer.toml.template
docs/scaffold/templates/root-AGENTS.md.template
plugins/docks/.claude-plugin/plugin.json
plugins/docks/.codex-plugin/plugin.json
plugins/docks/README.md
plugins/docks/agents/plan-manager.md
plugins/docks/agents/plan-reviewer.md
plugins/docks/skills/AGENTS.md
plugins/docks/skills/productivity/plan-creator/SKILL.md
plugins/docks/skills/productivity/plan-manager/SKILL.md
plugins/docks/skills/productivity/plan-repairer/SKILL.md
plugins/docks/skills/productivity/plan-reviewer/SKILL.md
plugins/docks/skills/productivity/plan-reviewer/scripts/review-policy.mjs
plugins/docks/skills/productivity/plan-workspace/SKILL.md
plugins/docks/skills/productivity/plan-workspace/references/codex-agent-templates.md
plugins/docks/skills/productivity/plan-workspace/references/plans-agents-md-template.md
plugins/session-relay/test/companion-distribution-contract.mjs
plugins/session-relay/test/release-evidence-contract.mjs
plugins/session-relay/test/release-promotion-contract.mjs
plugins/session-relay/test/release-publication-contract.mjs
scripts/agents/score.mjs
scripts/lib/session-relay-release-preparation.mjs
scripts/lib/session-relay-release-publication.mjs
scripts/lib/session-relay-release-promotion.mjs
scripts/tests/plan-review-convergence-repair.mjs
scripts/tests/plan-review-policy-regressions.mjs
scripts/tests/plan-review-policy.mjs
scripts/tests/plan-skill-phases.mjs
```

The binder must additionally prove that the authorized current-main base is an ancestor of promoted `HEAD` and that the exact base-to-promoted changed-path set is only these six paths:

```text
docs/plans/active/session-relay-linux-workspace-publication.md
plugins/session-relay/test/companion-distribution-contract.mjs
plugins/session-relay/test/release-evidence-contract.mjs
plugins/session-relay/test/release-publication-contract.mjs
scripts/lib/session-relay-release-preparation.mjs
scripts/lib/session-relay-release-publication.mjs
```

This two-fence contract makes the already-reviewed schema/Docks-release tail immutable while permitting only this amendment's six-path test-first archive lookup, binder correction, and publication-timestamp correction plus manager-owned plan records. Reject a missing or extra path, rename, stale/non-ancestor current head, dirty tree, mismatched constant, changed finished-plan bytes, changed source/evidence/shipped relationship, or proof whose `promoted_commit` differs from current HEAD. Plan-only lifecycle commits after the new final full gate are allowed only while all five amended implementation/test blobs are byte-identical to the gated implementation tree. The sorted shipped-to-promoted set is the exact 36-path list above (SHA-256 `9b7abbb8448dbd4abbd37f018bb71a79f8078e3c43c7a738f4e76a87e0c7e48d` including terminal LF); the sorted authorized-base-to-promoted six-path set hashes to `c6e580a83e6f23831e37d89a907b08b67d3fae04d961bcf64d3add4f9dc8e75b`.

### Exact immutable completion-receipt compatibility boundary

A10's exhaustive dynamic path reaches exactly one immutable `Completion-review-receipt` in `docs/plans/finished/2026-07-23-session-relay-linux-workspace-recertification.md`, selected with embedded candidate SHA-256 `75e5bf5386a203cd81e3930ca2309ceed4e1a665d995848a29eb73a0fa5cb395`. Its exact raw receipt SHA-256 is `d929ab3156532858ec515cc4bcecc00500adb24009dd2cc6b38bc3c396d42cfc`; its policy SHA-256 is `bb95e1516f9fc1b6f4d8a75991d4650428428dc35d842db1710f4d64dc082a1b`; its reviewed head is `762f2ad2b173c964435364cac651a63e43e2501c`; its plan input SHA-256 is `5275399617cf4812a55523aa30606e8a1aad34bf0d36bdf1ee40de3f2f5ebbbf`; its orchestration series is `cd8ec18d-fed0-4063-b49d-812d0f5bda05`; and its exact settled orchestration state SHA-256 is `064e08a437e587d6f5600788754a8af2cc3d5800adfdcb520b7399c7162ed3bb`.

That one receipt has three schema-6 policy incompatibilities with the current validator: `fallback:"availability_only"`, three ordered candidates, and `provenance.candidates:"skill_default"`. After those three legacy values are recognized, receipt validation also requires the exact settled orchestration state context above. The same legacy policy appears transitively 14 times inside this single closed receipt; those repetitions are not 14 receipts. A10 reaches no other policy validator and does not load the companion `Review-receipt`. TDD/preflight/source-CI/candidate/proof families are policy-free on the A10 path.

The immutable public companion `Review-receipt` raw SHA-256 `097206c0611c3357e10c0bf69a70819ea67901ef1ae8c3ef1d9e8207520f7c52` is also historical `availability_only`/three-candidate evidence. It is preparation-time history only: do not describe it as an A10 blocker, route A10 through its validator, or use this amendment to accept it or any other legacy schema-6 receipt globally.

The implementation is binder-local and exact-artifact-only. `scripts/lib/session-relay-release-preparation.mjs` may recognize only the exact finished path, embedded candidate, raw completion-receipt SHA, policy bytes/hash/order/provenance, reviewed head, plan input, series id, and settled-state tuple above. It must extract and validate exactly one canonical settled orchestration state from the exact finished plan and pass that state as the receipt's orchestration context. Reject changed receipt bytes, policy bytes/hash/candidate order/provenance, reviewed head, input, series or state ids/hashes, candidate, path, any waiver, duplicate receipt/state, or absent state. Current schema-6 emission and validation remain globally `fallback:"none"` with the sole runtime-current candidate and `provenance.candidates:"runtime_global"`; do not edit `plugins/docks/skills/productivity/plan-reviewer/scripts/review-policy.mjs`.

The first exact-artifact red is complete historical evidence. Test-only commit `4df3fdb0525e3f1405134f9083355291d41d3c86` captured `$LEGACY_POLICY_RED_RECEIPT` with raw SHA-256 `eb498e500685f86b00807482bdbf32c710653b138d30fae2c8ae5baca116090e` and frozen test Git blob `d70a8525340ef8c4dc1891fe4fd41a3746f0db59`. Production commit `2cd6084caee923c13af8c08d7eb60e0e68b2cdae` then added the binder-local compatibility branch. All focused A9b contracts passed. The subsequent `node scripts/ci.mjs` attempt failed only JavaScript formatting for `plugins/session-relay/test/release-evidence-contract.mjs` and `scripts/lib/session-relay-release-preparation.mjs`: Biome requested formatting around test lines 1797, 1995, and 2155 and production lines 956, 1002, and 1037. It reported no lint error in the new logic; existing unrelated warnings are nonblocking. This failed attempt is preserved as failure evidence and does not count as the required passing A9c. The plan does not claim that the full gate runs exactly once total.

The first receipt is immutable superseded evidence: A9a remains historical and MUST NOT be deleted, overwritten, recaptured, or treated as the final formatted-test red. After this formatter-safe recovery amendment passes fresh changed-input review and plan-manager returns the plan to `ongoing`, recover in this exact order:

1. Commit a production-only revert that restores `scripts/lib/session-relay-release-preparation.mjs` exactly to pre-`2cd6084...` Git blob `a0d015415d735bb82c2fbcf28e7a92b5a18d7743`, leaving test commit `4df3fdb...`, its test blob, receipt, and history intact.
2. Run `pnpm exec biome format --write plugins/session-relay/test/release-evidence-contract.mjs` and no other formatter target. Inspect the exact diff and require `git diff --ignore-all-space --exit-code 4df3fdb0525e3f1405134f9083355291d41d3c86 -- plugins/session-relay/test/release-evidence-contract.mjs`; assertions, strings, fixtures, mutation cases, validator reach, and behavioral cases must be unchanged. Commit only the formatted test.
3. With production still restored, capture a new exact test-only intended red to the nonexistent `$LEGACY_POLICY_FORMATTED_RED_RECEIPT`; A9a2 becomes the authoritative final test-blob red.
4. Reapply the exact binder implementation from `2cd6084caee923c13af8c08d7eb60e0e68b2cdae`, then run `pnpm exec biome format --write scripts/lib/session-relay-release-preparation.mjs` and no other formatter target. Require `git diff --ignore-all-space --exit-code 2cd6084caee923c13af8c08d7eb60e0e68b2cdae -- scripts/lib/session-relay-release-preparation.mjs`, then commit only production.
5. Verify both red receipts remain distinct, canonical, mode `0600`, and no-clobber; the first still hashes to `eb498e...`, and final `HEAD`'s test blob exactly equals the test blob recorded by `$LEGACY_POLICY_FORMATTED_RED_RECEIPT`.
6. Rerun A9b and one passing final A9c full gate on that formatter-clean final implementation tree. No implementation/test blob may change afterward.

STOP if formatting changes any assertion, string, fixture, mutation, validator reach, or behavioral case; if the formatted test does not produce the intended binder-contract failure while production is restored; if either receipt is missing, overwritten, noncanonical, or aliased; or if the final test blob differs from the formatted receipt. This is a process/evidence correction, not a policy, schema, waiver, or compatibility relaxation.

Historical A8/A9 evidence remains valid for implementation commit `c27955df150bcdde6fe651f6e10f3fbc6feb9c37`, but it is superseded for the new final implementation tree and cannot be reused as its gate, review, seal, or push evidence. The formatter-final four authorized-base paths and exact 35 shipped-to-promoted paths remain immutable historical evidence; the corrected final tree must instead satisfy the explicit six-path and 36-path closures above.

### GitHub Release `created_at` recovery boundary

The observed projection is fixed: release `created_at` `2026-07-23T14:26:39Z` equals the tagged head commit timestamp, while release `published_at` `2026-07-24T02:28:53Z` and all five asset `created_at` values `2026-07-24T02:28:52Z` fall inside sole run `30061627175`'s `2026-07-24T02:26:14Z` through `2026-07-24T02:28:56Z` window. Freeze that real projection in `plugins/session-relay/test/release-publication-contract.mjs`; do not introduce a new adapter or API solely to fetch the tagged commit timestamp. Equality between release `created_at` and commit time is an observed explanation, not a new runtime dependency.

For in-run publication provenance, `scripts/lib/session-relay-release-publication.mjs` must require finite release `created_at` and `published_at`, `published_at >= created_at`, release `published_at` inside the bound run window, and every asset `created_at` and `updated_at` inside that window. It must not require release `created_at` itself inside the run window. Exact workflow/release/asset actors, unique successful bound run, closed five-asset inventory, database ids, sizes, digests, checksums, attestations, downloaded bytes, publisher/uploader identities, and drift re-read remain mandatory. Post-run reconciliation semantics remain byte-for-byte unchanged.

The test-only red changes only `plugins/session-relay/test/release-publication-contract.mjs` and `plugins/session-relay/test/release-evidence-contract.mjs`. Production changes only `scripts/lib/session-relay-release-publication.mjs` and the six-path binder constants in `scripts/lib/session-relay-release-preparation.mjs`. Revert both production changes if the new publication contract or any adjacent publication/evidence/distribution contract fails after the fix, or if provenance accepts a release whose `published_at` or any asset timestamp is outside the run window. Never select another run or dispatch another workflow.

### Public request and child-plan contract

`PublicReleaseRequestV1` stays closed and is emitted only from the successful exact Session Relay publication receipt. It must bind repository `DocksDocks/public`, version/tag `0.10.2` / `cli-v0.10.2`, companion base `6c07f9bc02ef7a0a26b8ffb539c16c42a87a3172`, Session Relay tag/source identity, and four production asset digests independently read from the exact five-asset Session Relay prerelease.

The public child's already-observed focused A6 failure is rationale for one exact scope expansion, not a waiver: `cli/test/lib/harness.ts` materializes hardcoded `session-relay 0.12.0` while the production SoT is being updated to verified `0.13.0`. The only authorized fixture correction is to read the current Session Relay `verified` value from `SoT/toolchain.json` when materializing that fixture. Reinstall assertions, curl behavior, frozen unit-test/golden bytes, and all other harness behavior remain unchanged.

Create exactly `docs/plans/active/session-relay-cli-0.13.0-production-release.md` in `/home/vagrant/projects/public` with `planned_at_commit` equal to the immutable companion commit. That child plan must be created by `plan-creator`, independently draft-reviewed and started by public `plan-manager`, and remain the public repository's sole authority for production edits and mutations. It must:

1. Validate the request bytes/hash and companion/main ancestry before editing.
2. Bump only the `version` value in `package.json` from exactly `0.10.1` to exactly `0.10.2`; replace only Session Relay `verified`, `tag`, `plugin_version`, and the four request-authorized asset digest values in `SoT/toolchain.json`; regenerate `cli/src/generated/sotPayload.ts` from the source manifest so the generated package version and payload carry `0.10.2`; and change only `cli/test/lib/harness.ts` fixture version materialization from hardcoded `session-relay 0.12.0` to the current Session Relay `verified` value read from `SoT/toolchain.json`. Do not weaken reinstall assertions or curl behavior.
3. Preserve byte-for-byte the frozen `cli/test/unit/sessionRelayCli.test.ts` and `cli/test/unit/pluginRefresh.test.ts` blobs bound by public red receipt `833e777...`, plus `.github/workflows/release-cli.yml` and `bun.lock`; no test/golden recapture or workflow/lockfile edit is authorized.
4. Run the exact focused test command and the full public gate, prove the implementation diff is only the four non-plan files (`package.json`, `SoT/toolchain.json`, `cli/src/generated/sotPayload.ts`, `cli/test/lib/harness.ts`) plus the child plan's manager-owned lifecycle/archive path, and completion-review before any tag.
5. Derive `PUBLIC_RELEASE_COMMIT` only from the passed completion receipt's exact `reviewed_head`; require it to descend from companion `6c07f9...`; create lightweight tag `cli-v0.10.2` once at that commit and push only that tag refspec.
6. Wait for exactly one matching successful `.github/workflows/release-cli.yml` push run with `head_sha == PUBLIC_RELEASE_COMMIT`; do not dispatch another run.
7. Verify the stable GitHub Release has exactly `SHA256SUMS`, `docks-kit-darwin-arm64`, `docks-kit-darwin-x64`, `docks-kit-linux-arm64`, `docks-kit-linux-x64`, and `docks-kit-windows-x64.exe`; download them; require every size/digest and all five checksum rows to match.
8. Read npm through a fresh mode-`0700` cache. Record `npm.state=published` only when `npm view docks-kit@0.10.2 version` returns `0.10.2`; record `oidc_warning` only when the unique successful workflow contains the protocol's exact trusted-publisher warning. Any other npm result is STOP.
9. Completion-review and archive to the unique date-prefixed `docs/plans/finished/<ship-date>-session-relay-cli-0.13.0-production-release.md`; return exact `PUBLIC_RELEASE_COMMIT`, later plan-archive commit, finished path, and SHA-256 of the exact completion receipt payload.

The existing blocked preparation plan remains read-only historical authority; do not rewrite it into a production-success claim. The child plan consumes its immutable tuple and supersedes its pending production action without altering its frozen evidence.

### Release receipt chain

Use the reviewed CLI grammar exactly. The v1 bind below is immutable historical evidence and MUST NOT be rerun because `$SOURCE_PROOF` already exists. After the corrected implementation, focused contracts, one new full gate, independent final review, plan-only evidence commit, and non-force push establish the new final `HEAD == origin/main`, bind a distinct v2 proof and use it for the one post-fix rebind:

```bash
test "$(sha256sum "$SOURCE_PROOF" | cut -d' ' -f1)" = 419b23ccdcf0ca21672e81c05ae9d22c55bc67781839ffb6a29e7eecc2b59396
test ! -e "$SOURCE_PROOF_V2"
SOURCE_PROOF_V2_SHA256="$(node scripts/release.mjs --bind-completion --plugin session-relay 0.13.0 --finished-plan docs/plans/finished/2026-07-23-session-relay-linux-workspace-recertification.md --embedded-candidate-sha256 75e5bf5386a203cd81e3930ca2309ceed4e1a665d995848a29eb73a0fa5cb395 --receipt-out "$SOURCE_PROOF_V2")"
PUBLICATION_SHA256="$(node scripts/release.mjs --publish-reviewed --plugin session-relay 0.13.0 --source-proof "$SOURCE_PROOF_V2" --source-proof-sha256 "$SOURCE_PROOF_V2_SHA256" --rebind-complete-publication --receipt-out "$PUBLICATION_RECEIPT")"
PUBLIC_REQUEST_SHA256="$(node scripts/release.mjs --emit-public-request --plugin session-relay 0.13.0 --publication "$PUBLICATION_RECEIPT" --publication-sha256 "$PUBLICATION_SHA256" --receipt-out "$PUBLIC_REQUEST")"
PUBLIC_RELEASE_SHA256="$(node scripts/release.mjs --verify-public-release --plugin session-relay 0.13.0 --request "$PUBLIC_REQUEST" --request-sha256 "$PUBLIC_REQUEST_SHA256" --publication "$PUBLICATION_RECEIPT" --publication-sha256 "$PUBLICATION_SHA256" --public-finished-plan "$PUBLIC_FINISHED_PLAN" --public-release-commit "$PUBLIC_RELEASE_COMMIT" --public-plan-commit "$PUBLIC_PLAN_COMMIT" --public-completion-sha256 "$PUBLIC_COMPLETION_SHA256" --receipt-out "$PUBLIC_RELEASE_RECEIPT")"
PROMOTION_SHA256="$(node scripts/release.mjs --promote-reviewed --plugin session-relay 0.13.0 --source-proof "$SOURCE_PROOF_V2" --source-proof-sha256 "$SOURCE_PROOF_V2_SHA256" --publication "$PUBLICATION_RECEIPT" --publication-sha256 "$PUBLICATION_SHA256" --public-release "$PUBLIC_RELEASE_RECEIPT" --public-release-sha256 "$PUBLIC_RELEASE_SHA256" --docks-kit-release cli-v0.10.2 --expected-origin-main "$EXPECTED_ORIGIN_MAIN" --receipt-out "$PROMOTION_RECEIPT")"
FINALIZATION_SHA256="$(node scripts/release.mjs --finalize-reviewed --plugin session-relay 0.13.0 --source-proof "$SOURCE_PROOF_V2" --source-proof-sha256 "$SOURCE_PROOF_V2_SHA256" --publication "$PUBLICATION_RECEIPT" --publication-sha256 "$PUBLICATION_SHA256" --promotion "$PROMOTION_RECEIPT" --promotion-sha256 "$PROMOTION_SHA256" --receipt-out "$FINALIZATION_RECEIPT")"
```

`EXPECTED_ORIGIN_MAIN` is captured once immediately after the corrected timestamp implementation, new final full gate, independent review, and plan-only evidence commit. It must descend from authorized current-main base `25592c6550069e300a7a0148d3cd3c21880da8e7`, equal `PUBLICATION_IMPLEMENTATION_COMMIT`, and remain unchanged through v2 binding, publication rebind, and promotion. A different current or remote main is drift, not permission to recalculate the expected value.

After the amended implementation commit is pushed, bind both identities once:

```bash
PUBLICATION_IMPLEMENTATION_COMMIT="$(git rev-parse HEAD)"
EXPECTED_ORIGIN_MAIN="$PUBLICATION_IMPLEMENTATION_COMMIT"
export PUBLICATION_IMPLEMENTATION_COMMIT EXPECTED_ORIGIN_MAIN
test "$(git rev-parse origin/main)" = "$EXPECTED_ORIGIN_MAIN"
git merge-base --is-ancestor 25592c6550069e300a7a0148d3cd3c21880da8e7 "$PUBLICATION_IMPLEMENTATION_COMMIT"
```

## Steps

| # | Task | Files | Depends | Status | Done when / failure action |
|---|---|---|---|---|---|
| 1 | Obtain one fresh independent current-policy schema-6 draft review of this amended plan, retain its existing start/execution-base identity, and re-fetch all immutable Docks/public identities without mutation. | `docs/plans/active/session-relay-linux-workspace-publication.md` only for manager-owned review and block/unblock commits | — | done | Draft review passes with the sole runtime-current reviewer and no fallback or unauthorized waiver; the plan returns to `ongoing` while retaining `started_at` and `execution_base_commit`; recertification completion receipt/candidate, companion tuple/ancestry, authorized Docks base `25592c6...`, absence of fresh `session-relay--v0.13.0` and `cli-v0.10.2` tags/Releases, npm `docks-kit@0.10.2` absence, current versions, and corrected public constants reverify. Any non-pass or identity drift is STOP. |
| 2 | Commit test-only contract changes that fail because the binder cannot yet accept the exact reviewed current-main tail under the two-fence contract; capture the intended red before production edits. | `plugins/session-relay/test/release-evidence-contract.mjs` | 1 | done | A1 fails only because the binder lacks the authorized-base ancestry and exact three-path base-to-promoted fence. Tests freeze acceptance of the exact 34-path shipped-to-promoted set and rejection of a changed base, non-ancestor base, missing/extra base-tail path, implementation drift, arbitrary path drift, or dirty tree. Setup, parse, timeout, or unrelated failures STOP. |
| 3 | Implement the closed two-fence current-main correction and make focused contracts green without changing receipt schemas or public-release semantics. | `scripts/lib/session-relay-release-preparation.mjs` | 2 | done | The binder pins authorized base `25592c6...`, requires it as an ancestor of current `HEAD`, requires the exact 34-path shipped-to-promoted set and exact three-path base-to-promoted set, and continues to derive `promoted_commit` from clean current `HEAD`; A2-A5 pass. |
| 4 | Preserve the completed formatter-safe binder history, then execute only the explicitly authorized timestamp-recovery continuation through 4e-4f without renewing plan review. | `plugins/session-relay/test/companion-distribution-contract.mjs`; `plugins/session-relay/test/release-evidence-contract.mjs`; `plugins/session-relay/test/release-publication-contract.mjs`; `scripts/lib/session-relay-release-preparation.mjs`; `scripts/lib/session-relay-release-publication.mjs`; this plan for manager-owned evidence | 3 | done | Historical 4a-4d evidence remains immutable. The already-recorded zero-launch `replaceReviewTerminalFamily` failure means no fresh plan-review claim is allowed; the current user's exact timestamp-recovery authorization permitted only the now-complete 4e test provenance and 4f implementation evidence. The non-force push, source-proof-v2 bind, and receipt-only publication rebind remain separate pending A9h/A12 operations. |
| 4a | After this compatibility amendment passes fresh changed-input schema-6 draft review, freeze one test-only exact-artifact contract and capture the intended red before any new production edit. | `plugins/session-relay/test/release-evidence-contract.mjs`; `$LEGACY_POLICY_RED_RECEIPT`; this plan for manager-owned review/status/evidence | 4 | done | Historical A9a succeeded at exact test-only commit `4df3fdb0525e3f1405134f9083355291d41d3c86`; receipt SHA-256 `eb498e500685f86b00807482bdbf32c710653b138d30fae2c8ae5baca116090e` freezes test blob `d70a8525340ef8c4dc1891fe4fd41a3746f0db59`. Preserve that receipt byte-for-byte as superseded evidence; never rerun or overwrite it. |
| 4b | With the first test red frozen, add only the binder-local exact-artifact compatibility branch, run all new focused/final evidence, independently review the final code, reseal the new implementation commit, and push without force before retrying proof binding. | `scripts/lib/session-relay-release-preparation.mjs`; this plan for manager-owned evidence only | 4a | done | Production commit `2cd6084caee923c13af8c08d7eb60e0e68b2cdae` added the intended binder logic and all focused A9b contracts passed, but the first full-gate attempt failed only Biome formatting in the test and production files. No lint error occurred in the new logic. Preserve that failed attempt; recover only through 4c-4d after fresh review. This block is superseded recovery history: 4c-4d completed the authorized formatter-safe recovery, while the failed attempt remains formatting evidence and is not passing A9c. |
| 4c | Preserve A9a, restore pre-production bytes, format only the test, and capture a distinct authoritative formatted-test red. | `scripts/lib/session-relay-release-preparation.mjs`; `plugins/session-relay/test/release-evidence-contract.mjs`; `$LEGACY_POLICY_RED_RECEIPT`; `$LEGACY_POLICY_FORMATTED_RED_RECEIPT`; this plan for manager-owned review/status/evidence | 4b failed formatting gate; mechanical continuation authorized | done | Production-only revert `0e4af0299a38c6303dc3dcf5bb7f704c5c5e2b43` restored blob `a0d015415d735bb82c2fbcf28e7a92b5a18d7743`. The pinned-Biome formatter oracle proved byte identity against the exact baseline blobs without assertion/string/fixture/mutation/logic changes. Formatted test-only commit `f85d62a60262b8931e64261b2b868037eecc5c7b` has blob `d7c1553a1344045956f2b022b87a9a9c12571270`; authoritative A9a2 receipt SHA-256 is `8ed9268e0657fbdfc61dcd3b846524ec9c1671e9aa0fd465b8cc90df2eaf47e3`, canonical mode `0600`, with exact intended red `fallback must be none`. Historical A9a remains untouched at SHA-256 `eb498e500685f86b00807482bdbf32c710653b138d30fae2c8ae5baca116090e`. |
| 4d | Reapply the same binder logic, format only production, verify both no-clobber red receipts and final test-blob identity, then obtain the passing final implementation evidence. | `scripts/lib/session-relay-release-preparation.mjs`; `plugins/session-relay/test/release-evidence-contract.mjs` read-only after A9a2; both legacy-policy red receipts; this plan for manager-owned evidence only | 4c | done | Formatted production commit `36ad556cd4bbcddcd55e0d9d0983cfd095b8e5b8` has blob `2e2ec1e274630ff2600494b40081305201bc3b31`; A9b focused evidence, companion, publication (46), promotion, distribution (7), and hash checks passed; targeted `pnpm run check:js` passed with only pre-existing warnings; passing A9c `node scripts/ci.mjs` passed all repo/plugin/JS checks, while the earlier failed run remains formatting evidence only. The independent final binder reviewer returned `correct`, confidence `0.98`, and no findings. The exact current path fences are 35 paths from `cdca867e6a140311ea865a81229fb30de1df32c1` and four from `25592c6550069e300a7a0148d3cd3c21880da8e7`; the tree was clean, and no implementation/test bytes changed after the passing gate/review. |
| 4e | Freeze the real commit-time Release `created_at` plus in-run publication/assets projection in a clean test-only commit and capture the intended timestamp red before either production edit. | `plugins/session-relay/test/release-publication-contract.mjs`; `plugins/session-relay/test/release-evidence-contract.mjs`; `$PUBLICATION_TIMESTAMP_RED_RECEIPT` | 4d; failed initial A12 and failed pre-fix rebind preserved | done | Test-only commits `933a448104eb4892e36cc8283189cb3ca5d3f22c` and `3cce4349c11ad58c510b3037470695fe1fd91679` are preserved superseded history because the first evidence path array had an intrinsic sort-assertion defect and the second mechanical correction accidentally reordered an unrelated expected publication/promotion command list. Authoritative corrected test-only commit `262c424b279e3f2826e94a4b935f6b87979a237c` freezes publication blob `4c6928238f206d6f3dbf13c2fa5dcce732c6306a` and evidence blob `9ff3706f1d5ddfabbcb3cb282887a4da3039004d`; the canonical mode-`0600` v3 receipt has SHA-256 `f87a248cdc93760d4734229dfdc414f750b19bfe965b0e717e5ffa19f4a52d5d`. Publication failed exactly `release created_at timestamp is outside the bound workflow run window`, evidence failed exactly `shipped-to-promoted diff does not contain exactly the allowed plan lifecycle paths`, and production remained unchanged through v3. |
| 4f | Implement the minimal timestamp fix and exact six-path binder constants; run focused contracts/hash and one new full gate; obtain independent final code review; record its evidence plan-only. | `scripts/lib/session-relay-release-publication.mjs`; `scripts/lib/session-relay-release-preparation.mjs`; this plan for manager-owned evidence | 4e | done | Production commit `7193917e17aac0d64295a9b7ee89ac20e9b013be` changes only the publication and preparation modules: it removes only Release `created_at` run-window membership while retaining finite/order and all published/asset/actor/digest/provenance checks, and it closes the exact 36-path shipped-to-promoted and six-path authorized-base-to-promoted fences. A9f passed publication (68), evidence, distribution (7), and skill-hash checks; A9g `node scripts/ci.mjs` passed all repo/plugin/JS checks. Independent final code review returned `correct`, confidence `0.99`, with no findings; the independent audit confirmed the test commits precede production and the sole remaining condition is the expected pre-push `origin/main` mismatch. No implementation/test bytes changed after the passing gate/review. The resulting manager-owned plan-only evidence commit is the candidate corrected `PUBLICATION_IMPLEMENTATION_COMMIT`; A9h must capture its exact read-back SHA, establish `origin/main` equality by non-force push, and bind source proof v2 before A12 performs the one receipt-only rebind. |
| 5 | Preserve the already-bound completed recertification source proof v1 as immutable historical evidence. | `$SOURCE_PROOF` read-only; no tracked file | 4d | done | Historical A10-A11 passed once; canonical source proof v1 SHA-256 is `419b23ccdcf0ca21672e81c05ae9d22c55bc67781839ffb6a29e7eecc2b59396`, with `promoted_commit` `31a994622b4b960e8fa88b433e717d27e6c82ad7`. It remains byte-identical and must not be overwritten or rebound; source proof v2 supersedes it only after 4f. |
| 6 | Complete publication receipt binding from the preserved existing Session Relay prerelease without another workflow. | Existing external tag `session-relay--v0.13.0`; sole GitHub run `30061627175`; prerelease id `359049927`; `$SOURCE_PROOF_V2`; `$PUBLICATION_RECEIPT` | 4f, 5 | in-flight | Post-fix A12 invokes `--rebind-complete-publication` exactly once with authoritative v2 and emits canonical `publication.json` without tag, run, Release, or asset mutation; A13 validates tag `3fb9211...`, the sole successful run, exact five assets, publisher/uploaders, sizes, digests, checksums, attestations, timestamp contract, provenance, and prerelease state. |
| 7 | Emit the canonical public request, then create, independently review, and start the exact public child plan from the immutable companion commit. | `$PUBLIC_REQUEST`; `/home/vagrant/projects/public/docs/plans/active/session-relay-cli-0.13.0-production-release.md` | 6 | planned | A14 passes; public `PlanCreatedV1.planned_at_commit == 6c07f9...`; its plan-only creation commit is based on that commit; public draft review passes and start identity is recorded before public implementation. Any companion/main ancestry or request drift is STOP. |
| 8 | Execute the public child plan: bump the exact public package version, update only production Session Relay pins/payload, correct only the dynamic fixture version materialization, preserve frozen tests/workflow/lockfile, run focused/full gates, completion-review, create the exact tag once, verify the one workflow/six-asset stable release/npm result, and archive the child. | `/home/vagrant/projects/public/package.json`; `/home/vagrant/projects/public/SoT/toolchain.json`; `/home/vagrant/projects/public/cli/src/generated/sotPayload.ts`; `/home/vagrant/projects/public/cli/test/lib/harness.ts`; child plan lifecycle/archive path | 7 | planned | A15-A18 pass in the public repo; the exact non-plan implementation set is those four paths; the harness derives the fixture version from current SoT without weakening reinstall assertions or curl behavior; tag `cli-v0.10.2` points to the completion-reviewed descendant of `6c07f9...`; exactly one successful release workflow and closed six-asset Release verify; npm result is canonical; child plan archives with passed completion receipt and exact handoff values. |
| 9 | Verify the completed public release from Docks and emit the canonical public-release receipt. | Public finished plan/read-only remote evidence; `$PUBLIC_RELEASE_RECEIPT` | 8 | planned | A19 passes; request/publication digests, companion ancestry, child release/plan commits, completion receipt, pin map, workflow, six assets/checksums, and npm state agree exactly. |
| 10 | Promote reviewed Docks through the transaction ref, immutable lock, pre-push proof, exact/live smokes, and expected-main compare-and-swap. | `refs/session-relay-release/transaction`; immutable lock/journal refs; Docks `origin/main`; `$PROMOTION_RECEIPT` | 9 | planned | A20-A21 pass; authoritative terminal journal is `terminal_success`; promotion consumes the same receipt chain, observes and leaves `origin/main == PUBLICATION_IMPLEMENTATION_COMMIT`, and exact-source/live docks-kit smokes install Session Relay `0.13.0`. No rollback or unreviewed path occurs. |
| 11 | Finalize Session Relay stable once, then run independent stable Release and live binary/self-test/workspace-boundary checks. | Existing Session Relay GitHub Release; `$FINALIZATION_RECEIPT`; disposable live-check directory only | 10 | planned | A22-A24 pass; stable Release keeps the exact five assets and source/tag identity, canonical finalization receipt succeeds, fresh installed binary reports `0.13.0`, immutable self-test passes, Linux workspace docs contract passes, and managed/unmanaged boundaries remain exact. |
| 12 | Record immutable receipt/evidence summaries, run Docks completion review, and archive this plan. | This plan only for manager-owned evidence, review, status, and dated active-to-finished lifecycle move | 11 | planned | Completion inventory A1-A24 plus A9a, A9a2, and A9b-A9h is complete; A9a remains immutable superseded evidence, A9a2 binds the formatter-final test blob, and A9e binds the timestamp red; source proof v1 remains immutable while v2 is authoritative; the child plan is already finished; no implementation/test blob changed after the new passing A9g; completion review passes; and plan-manager archives this plan under the unique date-prefixed finished path. Non-pass is STOP; no tag/release retry follows. |

## Acceptance criteria

Run in order only after the owning step starts. A1-A9d and both legacy-policy red receipts are immutable history and are not rerun. The explicitly authorized no-renewal continuation completed A9e-A9g: A9e preserves two superseded no-clobber receipts and binds authoritative v3 to the corrected test-only commit; A9f proves the minimal publication fix plus exact six/36-path fences; A9g is the authoritative full gate for the unchanged corrected implementation tree. A9h is next and must bind the manager-read-back plan-only evidence commit as `PUBLICATION_IMPLEMENTATION_COMMIT`, establish non-force `origin/main` equality, preserve proof v1, and create authoritative proof v2. Historical A10-A11 are record-only. Post-fix A12 remains pending and may perform exactly one receipt-only rebind with v2 and no workflow dispatch; A13 validates the resulting receipt before downstream work.

| ID | Command | Expected |
|---|---|---|
| A1 | `test -z "$(git status --porcelain=v1 --untracked-files=all)" && PROTOCOL_RED_COMMIT="$(git rev-parse HEAD)" && git merge-base --is-ancestor 25592c6550069e300a7a0148d3cd3c21880da8e7 "$PROTOCOL_RED_COMMIT" && node scripts/capture-tdd-red.mjs --repo "$PWD" --repository-id DocksDocks/docks --pre-production-commit "$PROTOCOL_RED_COMMIT" --test plugins/session-relay/test/release-evidence-contract.mjs --receipt-out "$PROTOCOL_RED_RECEIPT" -- node plugins/session-relay/test/release-evidence-contract.mjs` | Exit 0 only from an exact clean test-only commit over an intended nonzero contract run; the frozen test blob requires authorized base `25592c6...` ancestry, exact 34-path shipped-to-promoted closure, and exact three-path base-to-promoted closure while rejecting missing/extra/drifted paths. |
| A1b | `test -z "$(git status --porcelain=v1 --untracked-files=all)" && ARCHIVE_FENCE_RED_COMMIT="$(git rev-parse HEAD)" && git merge-base --is-ancestor 25592c6550069e300a7a0148d3cd3c21880da8e7 "$ARCHIVE_FENCE_RED_COMMIT" && node scripts/capture-tdd-red.mjs --repo "$PWD" --repository-id DocksDocks/docks --pre-production-commit "$ARCHIVE_FENCE_RED_COMMIT" --test plugins/session-relay/test/companion-distribution-contract.mjs --test plugins/session-relay/test/release-evidence-contract.mjs --receipt-out "$ARCHIVE_FENCE_RED_RECEIPT" -- node plugins/session-relay/test/release-evidence-contract.mjs` | Exit 0 only from an exact clean test-only commit over an intended nonzero contract run; the frozen test blobs require the archived recertification authority, exact 35-path shipped-to-promoted closure, and exact four-path base-to-promoted closure, while production still enforces the prior closure. Setup, parse, timeout, unrelated, or already-passing results STOP. |
| A2 | `node plugins/session-relay/test/release-evidence-contract.mjs` | Exit 0; producer-bound proof accepts exact later promoted HEAD only under closed ancestry/path semantics and rejects extra paths, dirty/stale heads, mismatched constants, and hand-authored relaxation. |
| A3 | `node plugins/session-relay/test/release-publication-contract.mjs` | Exit 0; bind/publish/request/finalization fixtures use the exact recertified source and current `0.10.2` public tuple while retaining five-asset publication closure and legal recovery rejection. |
| A4 | `node plugins/session-relay/test/release-promotion-contract.mjs` | Exit 0; public verification and promotion require `cli-v0.10.2`, companion `6c07f9...`, new child-plan identity, exact expected main, closed six public assets, and terminal journal safety. |
| A5 | `node plugins/session-relay/test/distribution-contract.mjs` | Exit 0; release CLI grammar, receipt pairing/no-clobber modes, four native targets plus `SHA256SUMS`, and distribution closure remain unchanged. |
| A6 | `node plugins/session-relay/test/companion-distribution-contract.mjs --public-remote https://github.com/DocksDocks/public.git --public-ref refs/heads/preflight/session-relay-cli-0.13.0-6c07f9bc02ef --public-commit 6c07f9bc02ef7a0a26b8ffb539c16c42a87a3172 --detached-clone` | Exit 0; immutable reviewed blocked companion tuple and frozen public contract revalidate detached. |
| A7 | `node scripts/skills/content-hash.mjs --check-only plugins/session-relay/skills` | Exit 0; Session Relay skill hashes remain current without product changes. |
| A8 | `node scripts/ci.mjs` | Exit 0 once on the final Docks implementation tree before `PUBLICATION_IMPLEMENTATION_COMMIT` is fixed; no implementation/test blob changes afterward. |
| A9 | `test -z "$(git status --porcelain=v1 --untracked-files=all)" && test "$(git rev-parse HEAD)" = "$PUBLICATION_IMPLEMENTATION_COMMIT" && test "$(git rev-parse origin/main)" = "$PUBLICATION_IMPLEMENTATION_COMMIT" && git merge-base --is-ancestor cdca867e6a140311ea865a81229fb30de1df32c1 "$PUBLICATION_IMPLEMENTATION_COMMIT" && git merge-base --is-ancestor 25592c6550069e300a7a0148d3cd3c21880da8e7 "$PUBLICATION_IMPLEMENTATION_COMMIT"` | Exit 0; clean exact implementation commit descends from the shipped archive and authorized current-main base and equals remote main. Independently compare `cdca867...HEAD` to the closed 35-path set and `25592c6...HEAD` to the closed four-path amendment set; no rename or extra path is present. |
| A9a | Historical command, DO NOT RERUN: `test -z "$(git status --porcelain=v1 --untracked-files=all)" && LEGACY_POLICY_RED_COMMIT="$(git rev-parse HEAD)" && node scripts/capture-tdd-red.mjs --repo "$PWD" --repository-id DocksDocks/docks --pre-production-commit "$LEGACY_POLICY_RED_COMMIT" --test plugins/session-relay/test/release-evidence-contract.mjs --receipt-out "$LEGACY_POLICY_RED_RECEIPT" -- node plugins/session-relay/test/release-evidence-contract.mjs` | Already passed at exact test-only commit `4df3fdb0525e3f1405134f9083355291d41d3c86`; immutable receipt SHA-256 `eb498e500685f86b00807482bdbf32c710653b138d30fae2c8ae5baca116090e` records test blob `d70a8525340ef8c4dc1891fe4fd41a3746f0db59`. Preserve it as superseded evidence; any recapture, deletion, overwrite, or byte drift is STOP. |
| A9a2 | `test -z "$(git status --porcelain=v1 --untracked-files=all)" && test "$(git diff-tree --no-commit-id --name-only -r HEAD)" = plugins/session-relay/test/release-evidence-contract.mjs && test "$(git rev-parse HEAD:scripts/lib/session-relay-release-preparation.mjs)" = a0d015415d735bb82c2fbcf28e7a92b5a18d7743 && git merge-base --is-ancestor 4df3fdb0525e3f1405134f9083355291d41d3c86 HEAD && git diff --ignore-all-space --exit-code 4df3fdb0525e3f1405134f9083355291d41d3c86 -- plugins/session-relay/test/release-evidence-contract.mjs && test ! -e "$LEGACY_POLICY_FORMATTED_RED_RECEIPT" && LEGACY_POLICY_FORMATTED_RED_COMMIT="$(git rev-parse HEAD)" && LEGACY_POLICY_FORMATTED_RED_SHA256="$(node scripts/capture-tdd-red.mjs --repo "$PWD" --repository-id DocksDocks/docks --pre-production-commit "$LEGACY_POLICY_FORMATTED_RED_COMMIT" --test plugins/session-relay/test/release-evidence-contract.mjs --receipt-out "$LEGACY_POLICY_FORMATTED_RED_RECEIPT" -- node plugins/session-relay/test/release-evidence-contract.mjs)" && test "$LEGACY_POLICY_FORMATTED_RED_SHA256" = "$(sha256sum "$LEGACY_POLICY_FORMATTED_RED_RECEIPT" | cut -d' ' -f1)" && export LEGACY_POLICY_FORMATTED_RED_COMMIT LEGACY_POLICY_FORMATTED_RED_SHA256` | Exit 0 only from the exact clean formatted-test-only commit with production restored to pre-`2cd6084...` blob `a0d015...`; the test differs from `d70a852...` only by reviewed Biome formatting and still produces the intended binder-contract failure. The new canonical mode-`0600` receipt is a distinct no-clobber file and becomes authoritative for the final test blob. Assertion, string, fixture, mutation, validator-reach, behavioral, or failure-shape drift is STOP. |
| A9b | `test "$(sha256sum "$LEGACY_POLICY_RED_RECEIPT" | cut -d' ' -f1)" = eb498e500685f86b00807482bdbf32c710653b138d30fae2c8ae5baca116090e && test "$LEGACY_POLICY_RED_RECEIPT" != "$LEGACY_POLICY_FORMATTED_RED_RECEIPT" && node --input-type=module -e 'import assert from "node:assert/strict"; import fs from "node:fs"; import {execFileSync} from "node:child_process"; import {canonicalize} from "./scripts/lib/session-relay-release-core.mjs"; for (const name of ["LEGACY_POLICY_RED_RECEIPT","LEGACY_POLICY_FORMATTED_RED_RECEIPT"]) { const file=process.env[name]; const bytes=fs.readFileSync(file); assert.equal(fs.statSync(file).mode & 0o777,0o600); assert.equal(bytes.toString(),canonicalize(JSON.parse(bytes))); } const formatted=JSON.parse(fs.readFileSync(process.env.LEGACY_POLICY_FORMATTED_RED_RECEIPT)); assert.match(formatted.pre_production_commit,/^[0-9a-f]{40}$/); assert.equal(execFileSync("git",["rev-parse",`${formatted.pre_production_commit}:scripts/lib/session-relay-release-preparation.mjs`],{encoding:"utf8"}).trim(),"a0d015415d735bb82c2fbcf28e7a92b5a18d7743"); assert.equal(formatted.test_paths.length,1); assert.equal(formatted.test_paths[0].path,"plugins/session-relay/test/release-evidence-contract.mjs"); assert.equal(formatted.test_paths[0].blob_id,execFileSync("git",["rev-parse","HEAD:plugins/session-relay/test/release-evidence-contract.mjs"],{encoding:"utf8"}).trim());' && node plugins/session-relay/test/release-evidence-contract.mjs && node plugins/session-relay/test/companion-distribution-contract.mjs --public-remote https://github.com/DocksDocks/public.git --public-ref refs/heads/preflight/session-relay-cli-0.13.0-6c07f9bc02ef --public-commit 6c07f9bc02ef7a0a26b8ffb539c16c42a87a3172 --detached-clone && node plugins/session-relay/test/release-publication-contract.mjs && node plugins/session-relay/test/release-promotion-contract.mjs && node plugins/session-relay/test/distribution-contract.mjs && node scripts/skills/content-hash.mjs --check-only plugins/session-relay/skills` | Exit 0 with the A9a2 test blob unchanged; both red receipts remain distinct, canonical, mode `0600`, and no-clobber; A9a retains exact SHA `eb498e...`; final test blob equals A9a2; exact-artifact binder compatibility and companion/publication/promotion/distribution/hash contracts are green; current fallback-none behavior is unchanged; no other legacy policy artifact is accepted. |
| A9c | `node scripts/ci.mjs` | Exit 0 once as the required passing final gate on the formatter-clean implementation tree after A9b. The earlier full-gate attempt on production commit `2cd6084...` failed only formatting and is preserved but not counted as passing A9c; this plan makes no exactly-once-total claim. This passing result supersedes the otherwise-valid A8 result for `c27955df150bcdde6fe651f6e10f3fbc6feb9c37`, and no implementation/test blob changes afterward. |
| A9d | `test -z "$(git status --porcelain=v1 --untracked-files=all)" && test "$PUBLICATION_IMPLEMENTATION_COMMIT" != c27955df150bcdde6fe651f6e10f3fbc6feb9c37 && git merge-base --is-ancestor c27955df150bcdde6fe651f6e10f3fbc6feb9c37 "$PUBLICATION_IMPLEMENTATION_COMMIT" && test "$(git rev-parse HEAD)" = "$PUBLICATION_IMPLEMENTATION_COMMIT" && test "$(git rev-parse origin/main)" = "$PUBLICATION_IMPLEMENTATION_COMMIT" && git merge-base --is-ancestor cdca867e6a140311ea865a81229fb30de1df32c1 "$PUBLICATION_IMPLEMENTATION_COMMIT" && git merge-base --is-ancestor 25592c6550069e300a7a0148d3cd3c21880da8e7 "$PUBLICATION_IMPLEMENTATION_COMMIT"` | Exit 0 only after independent final code review passes, the reviewed implementation is resealed, and an ancestry-preserving non-force push makes local and remote main equal the new final commit. Independently compare `cdca867...HEAD` to the unchanged 35-path set and `25592c6...HEAD` to the unchanged four-path set; no rename or extra path is present. |
| A9e | Historical command, DO NOT RERUN: `test -z "$(git status --porcelain=v1 --untracked-files=all)" && test ! -e "$PUBLICATION_TIMESTAMP_RED_RECEIPT" && PUBLICATION_TIMESTAMP_RED_COMMIT="$(git rev-parse HEAD)" && node scripts/capture-tdd-red.mjs --repo "$PWD" --repository-id DocksDocks/docks --pre-production-commit "$PUBLICATION_TIMESTAMP_RED_COMMIT" --test plugins/session-relay/test/release-publication-contract.mjs --test plugins/session-relay/test/release-evidence-contract.mjs --receipt-out "$PUBLICATION_TIMESTAMP_RED_RECEIPT" -- node plugins/session-relay/test/release-publication-contract.mjs` | Completed at authoritative corrected test-only commit `262c424b279e3f2826e94a4b935f6b87979a237c`, whose publication/evidence blobs are `4c6928238f206d6f3dbf13c2fa5dcce732c6306a` and `9ff3706f1d5ddfabbcb3cb282887a4da3039004d`. Canonical mode-`0600` v3 receipt SHA-256 is `f87a248cdc93760d4734229dfdc414f750b19bfe965b0e717e5ffa19f4a52d5d`; the inner publication run failed only `release created_at timestamp is outside the bound workflow run window`, the paired evidence red was the exact shipped-to-promoted path-set failure, and production was unchanged. The two earlier receipts remain preserved, no-clobber, and superseded rather than authority. |
| A9f | `node plugins/session-relay/test/release-publication-contract.mjs && node plugins/session-relay/test/release-evidence-contract.mjs && node plugins/session-relay/test/distribution-contract.mjs && node scripts/skills/content-hash.mjs --check-only plugins/session-relay/skills` | Passed on production commit `7193917e17aac0d64295a9b7ee89ac20e9b013be`: publication (68), evidence, distribution (7), and skill-hash checks were green. In-run publication accepts finite commit-time release `created_at <= published_at` while continuing to reject out-of-window `published_at` or asset timestamps, actor/digest/provenance drift, extra paths, and any second run; evidence enforces the exact six-path authorized-base and 36-path shipped closures. |
| A9g | `node scripts/ci.mjs` | Passed all repo/plugin/JS checks once on the corrected final implementation tree after A9f. This full gate supersedes earlier A8/A9c for implementation authority; no implementation/test blob changed afterward. |
| A9h | `test -z "$(git status --porcelain=v1 --untracked-files=all)" && PUBLICATION_IMPLEMENTATION_COMMIT="$(git rev-parse HEAD)" && EXPECTED_ORIGIN_MAIN="$PUBLICATION_IMPLEMENTATION_COMMIT" && export PUBLICATION_IMPLEMENTATION_COMMIT EXPECTED_ORIGIN_MAIN && test "$(git rev-parse origin/main)" = "$PUBLICATION_IMPLEMENTATION_COMMIT" && git merge-base --is-ancestor 25592c6550069e300a7a0148d3cd3c21880da8e7 "$PUBLICATION_IMPLEMENTATION_COMMIT" && test "$(git diff --name-only cdca867e6a140311ea865a81229fb30de1df32c1 HEAD | sort | sha256sum | cut -d' ' -f1)" = 9b7abbb8448dbd4abbd37f018bb71a79f8078e3c43c7a738f4e76a87e0c7e48d && test "$(git diff --name-only 25592c6550069e300a7a0148d3cd3c21880da8e7 HEAD | sort | sha256sum | cut -d' ' -f1)" = c6e580a83e6f23831e37d89a907b08b67d3fae04d961bcf64d3add4f9dc8e75b && test "$(sha256sum "$SOURCE_PROOF" | cut -d' ' -f1)" = 419b23ccdcf0ca21672e81c05ae9d22c55bc67781839ffb6a29e7eecc2b59396 && test ! -e "$SOURCE_PROOF_V2" && SOURCE_PROOF_V2_SHA256="$(node scripts/release.mjs --bind-completion --plugin session-relay 0.13.0 --finished-plan docs/plans/finished/2026-07-23-session-relay-linux-workspace-recertification.md --embedded-candidate-sha256 75e5bf5386a203cd81e3930ca2309ceed4e1a665d995848a29eb73a0fa5cb395 --receipt-out "$SOURCE_PROOF_V2")" && export SOURCE_PROOF_V2_SHA256 && test "$SOURCE_PROOF_V2_SHA256" = "$(sha256sum "$SOURCE_PROOF_V2" | cut -d' ' -f1)"` | Pending. The independent final code review already returned `correct`, confidence `0.99`, with no findings, and plan-manager is recording that evidence in the candidate plan-only commit. Next, an ancestry-preserving non-force push must make that exact read-back `HEAD == origin/main`; only then may this command confirm the exact six/36-path closure hashes, leave v1 untouched, and create canonical no-clobber v2 binding `promoted_commit == PUBLICATION_IMPLEMENTATION_COMMIT`. No push or proof-v2 claim exists yet. |
| A10 | Historical command, DO NOT RERUN: `node scripts/release.mjs --bind-completion --plugin session-relay 0.13.0 --finished-plan docs/plans/finished/2026-07-23-session-relay-linux-workspace-recertification.md --embedded-candidate-sha256 75e5bf5386a203cd81e3930ca2309ceed4e1a665d995848a29eb73a0fa5cb395 --receipt-out "$SOURCE_PROOF"` | Already passed once and emitted immutable v1 SHA-256 `419b23ccdcf0ca21672e81c05ae9d22c55bc67781839ffb6a29e7eecc2b59396`, binding `promoted_commit` `31a994622b4b960e8fa88b433e717d27e6c82ad7`. Its output path exists; never overwrite, reconstruct, or reinterpret it under the corrected binder. |
| A11 | Historical read-back, DO NOT REVALIDATE THROUGH CHANGED PRODUCTION: `test "$(sha256sum "$SOURCE_PROOF" | cut -d' ' -f1)" = 419b23ccdcf0ca21672e81c05ae9d22c55bc67781839ffb6a29e7eecc2b59396` | Exit 0; v1 raw canonical bytes remain immutable evidence. A9h separately binds and validates v2 against the corrected final commit. |
| A12 | `test ! -e "$PUBLICATION_RECEIPT" && PUBLICATION_SHA256="$(node scripts/release.mjs --publish-reviewed --plugin session-relay 0.13.0 --source-proof "$SOURCE_PROOF_V2" --source-proof-sha256 "$SOURCE_PROOF_V2_SHA256" --rebind-complete-publication --receipt-out "$PUBLICATION_RECEIPT")" && export PUBLICATION_SHA256 && test "$PUBLICATION_SHA256" = "$(sha256sum "$PUBLICATION_RECEIPT" | cut -d' ' -f1)"` | Exit 0 exactly once after the fix; emits canonical mode-`0600` `publication.json` from the preserved tag, sole successful run `30061627175`, and prerelease `359049927` without another workflow or any remote mutation. The initial publish and pre-fix rebind failures remain immutable no-receipt evidence. |
| A13 | `node --input-type=module -e 'import fs from "node:fs"; import {validatePublicationReceipt} from "./scripts/lib/session-relay-release-publication.mjs"; validatePublicationReceipt({value:JSON.parse(fs.readFileSync(process.env.PUBLICATION_RECEIPT,"utf8")),digest:process.env.PUBLICATION_SHA256});'` | Exit 0; receipt proves tag `session-relay--v0.13.0` at `3fb9211...`, exactly one successful bound run `30061627175`, prerelease `359049927`, finite release `created_at` `2026-07-23T14:26:39Z <= published_at` `2026-07-24T02:28:53Z`, in-window publication/assets, four native binaries plus `SHA256SUMS`, exact actors, database ids, sizes, digests, checksums, attestations, downloaded bytes, drift re-read, and unchanged prerelease state. |
| A14 | `node scripts/release.mjs --emit-public-request --plugin session-relay 0.13.0 --publication "$PUBLICATION_RECEIPT" --publication-sha256 "$PUBLICATION_SHA256" --receipt-out "$PUBLIC_REQUEST"` | Exit 0 and canonical digest; request binds public `0.10.2` / `cli-v0.10.2`, companion `6c07f9...`, source publication, and exact four production digests. |
| A15 | `cd /home/vagrant/projects/public && bun cli/scripts/generate-sot-payload.ts --check && bun run test:unit -- cli/test/unit/sessionRelayCli.test.ts cli/test/unit/pluginRefresh.test.ts` | Exit 0 after the child implementation; generated payload is current, frozen focused tests stay byte-bound to prior red evidence, the production manifest consumes exact request digests, and the harness materializes the current SoT-verified Session Relay version rather than hardcoded `0.12.0`. |
| A16 | `cd /home/vagrant/projects/public && git diff --exit-code 6c07f9bc02ef7a0a26b8ffb539c16c42a87a3172 -- cli/test/unit/sessionRelayCli.test.ts cli/test/unit/pluginRefresh.test.ts .github/workflows/release-cli.yml bun.lock && test "$(node -p 'require("./package.json").version')" = 0.10.2` | Exit 0; both frozen unit tests, the release workflow, and `bun.lock` are byte-identical to the companion, while the package version is exactly `0.10.2`. No test/golden recapture, workflow edit, or lockfile churn is accepted. |
| A17 | `cd /home/vagrant/projects/public && bash -euo pipefail -c 'bun cli/scripts/generate-sot-payload.ts --check; bun run typecheck; bun run test:unit; bun cli/test/statusline-runtime-smoke.mjs posix; bun run golden:dryrun; set +e; dry_out="$(bun run golden:dryrun --prove-red 2>&1)"; dry_status=$?; set -e; test "$dry_status" -ne 0; grep -q "prove-red OK: golden-dryrun" <<<"$dry_out"; bun run golden:mutation; set +e; mutation_out="$(bun run golden:mutation --prove-red 2>&1)"; mutation_status=$?; set -e; test "$mutation_status" -ne 0; grep -q "prove-red OK: golden-mutation" <<<"$mutation_out"'` | Exit 0; generator, typecheck, all unit tests, runtime smoke, both goldens, and both intentional prove-red message checks pass once in the child plan's full gate with the dynamic fixture correction. |
| A18 | `cd /home/vagrant/projects/public && test "$(git diff --name-only 6c07f9bc02ef7a0a26b8ffb539c16c42a87a3172 "$PUBLIC_RELEASE_COMMIT" -- . ':(exclude)docs/plans/**' | sort)" = "$(printf '%s\n' SoT/toolchain.json cli/src/generated/sotPayload.ts cli/test/lib/harness.ts package.json)" && git diff --exit-code 6c07f9bc02ef7a0a26b8ffb539c16c42a87a3172 "$PUBLIC_RELEASE_COMMIT" -- cli/test/unit/sessionRelayCli.test.ts cli/test/unit/pluginRefresh.test.ts .github/workflows/release-cli.yml bun.lock && test "$(git rev-parse refs/tags/cli-v0.10.2)" = "$PUBLIC_RELEASE_COMMIT" && git merge-base --is-ancestor 6c07f9bc02ef7a0a26b8ffb539c16c42a87a3172 "$PUBLIC_RELEASE_COMMIT" && test -f "$PUBLIC_FINISHED_PLAN"` | Exit 0 after the child release/archive; the exact non-plan implementation set is `package.json`, `SoT/toolchain.json`, `cli/src/generated/sotPayload.ts`, and `cli/test/lib/harness.ts`; protected tests/workflow/lockfile are immutable; the tag target is the completion-reviewed companion descendant; and the exact finished child plan exists with passed completion receipt. Child production verification separately proves one successful workflow, the closed six assets/checksums, and canonical npm result. |
| A19 | `node scripts/release.mjs --verify-public-release --plugin session-relay 0.13.0 --request "$PUBLIC_REQUEST" --request-sha256 "$PUBLIC_REQUEST_SHA256" --publication "$PUBLICATION_RECEIPT" --publication-sha256 "$PUBLICATION_SHA256" --public-finished-plan "$PUBLIC_FINISHED_PLAN" --public-release-commit "$PUBLIC_RELEASE_COMMIT" --public-plan-commit "$PUBLIC_PLAN_COMMIT" --public-completion-sha256 "$PUBLIC_COMPLETION_SHA256" --receipt-out "$PUBLIC_RELEASE_RECEIPT"` | Exit 0 and canonical digest; exact public ancestry, request pins, passed child completion, tag/workflow, six assets/checksums, and npm state validate from Docks. |
| A20 | `node scripts/release.mjs --promote-reviewed --plugin session-relay 0.13.0 --source-proof "$SOURCE_PROOF_V2" --source-proof-sha256 "$SOURCE_PROOF_V2_SHA256" --publication "$PUBLICATION_RECEIPT" --publication-sha256 "$PUBLICATION_SHA256" --public-release "$PUBLIC_RELEASE_RECEIPT" --public-release-sha256 "$PUBLIC_RELEASE_SHA256" --docks-kit-release cli-v0.10.2 --expected-origin-main "$EXPECTED_ORIGIN_MAIN" --receipt-out "$PROMOTION_RECEIPT"` | Exit 0 under the initial or one exact legal recovery branch; terminal receipt records success, expected/observed/pushed Docks main is the corrected publication implementation commit, and exact-source/live smokes pass. |
| A21 | `node --input-type=module -e 'import fs from "node:fs"; import {validatePromotionReceipt} from "./scripts/lib/session-relay-release-promotion.mjs"; const receipt=JSON.parse(fs.readFileSync(process.env.PROMOTION_RECEIPT,"utf8")); validatePromotionReceipt(receipt); if(receipt.outcome!=="success"||receipt.promoted_commit!==process.env.EXPECTED_ORIGIN_MAIN) throw new Error("promotion terminal identity mismatch");'` | Exit 0; canonical receipt and authoritative transaction journal end in terminal success with exact public/source identities, CAS result, compatibility, and live-smoke evidence. |
| A22 | `node scripts/release.mjs --finalize-reviewed --plugin session-relay 0.13.0 --source-proof "$SOURCE_PROOF_V2" --source-proof-sha256 "$SOURCE_PROOF_V2_SHA256" --publication "$PUBLICATION_RECEIPT" --publication-sha256 "$PUBLICATION_SHA256" --promotion "$PROMOTION_RECEIPT" --promotion-sha256 "$PROMOTION_SHA256" --receipt-out "$FINALIZATION_RECEIPT"` | Exit 0 under the initial or exact canonical resume/lost-receipt recovery; stable finalization receipt binds the same corrected source proof, publication, promotion, tag, Release, and five assets with no second stable mutation. |
| A23 | `LIVE_DIR="$(mktemp -d /tmp/session-relay-live.XXXXXX)" && chmod 700 "$LIVE_DIR" && gh release download session-relay--v0.13.0 --repo DocksDocks/docks --pattern session-relay-x86_64-unknown-linux-musl --dir "$LIVE_DIR" && chmod 755 "$LIVE_DIR/session-relay-x86_64-unknown-linux-musl" && test "$("$LIVE_DIR/session-relay-x86_64-unknown-linux-musl" --version)" = 'session-relay 0.13.0' && SESSION_RELAY_TEST_BIN="$LIVE_DIR/session-relay-x86_64-unknown-linux-musl" node plugins/session-relay/test/selftest.mjs` | Exit 0 on x86-64 Linux; freshly downloaded stable asset reports exact version and immutable 133-check self-test passes. On another host select only the matching closed target and retain the same digest/provenance checks. |
| A24 | `node plugins/session-relay/test/workspace-smoke.mjs --case docs-contract --bin "$LIVE_DIR/session-relay-x86_64-unknown-linux-musl"` | Exit 0 on Linux/ext4; documented nine-command surface, managed/unmanaged boundary, custody, and workspace behavior hold for the stable downloaded binary. macOS remains ordinary Relay support with exact managed-workspace refusal and is verified by the release evidence/full gate, not by pretending to run the Linux positive case. |

## Recovery matrix

| Boundary | Legal recovery | Mandatory STOP |
|---|---|---|
| Session Relay publication | Initial publish and one pre-fix receiptless rebind are immutable failed evidence, both stopping only on the deterministic Release `created_at` check and emitting no receipt. After A9e-A9h, exactly one post-fix `--rebind-complete-publication` with `$SOURCE_PROOF_V2` may reconcile the already-complete matching prerelease and emit `$PUBLICATION_RECEIPT`; it must perform no remote mutation. | A second matching run or any new dispatch; changed tag target, Release id/state/body, actor, asset id/set/size/digest/checksum/timestamp/provenance; stable Release; extra path; any need to relax published/asset run windows; failed post-fix rebind; any retry after it. |
| Public child release | Reconcile a nonzero tag push once by exact remote ref read-back; continue only if remote `cli-v0.10.2` already equals `PUBLIC_RELEASE_COMMIT`. Bounded waiting may observe the one already-triggered workflow. | Public main no longer ancestor-compatible with companion; companion tuple/frozen tests drift; preexisting `cli-v0.10.2` tag target or Release conflicts (the historical `cli-v0.10.1` Release at `6f9691...` is recorded prior history, never a reuse target, and must remain untouched); multiple matching workflows; production pin, package-version, six-asset, checksum, npm, child receipt/review/archive mismatch; any tag rewrite or second trigger. |
| Docks promotion | Resume only through `--resume-promotion` with authoritative transaction ref and the same receipt inputs. Retry only an exact canonical retryable restored-failure receipt through paired retry flags. `--repair-prepush` is restricted to the known legacy sync-target failure and is not a general repair. | Expected/current/remote main differs from `PUBLICATION_IMPLEMENTATION_COMMIT`; transaction lock/ref or immutable tuple conflicts; nonretryable/manual-incident terminal journal; unproven restore; extra path; force/CAS bypass. |
| Stable finalization | Initial `--finalize-reviewed` only after promotion terminal success. Resume only with paired `--resume-finalization` receipt/digest. Lost-receipt already-stable recovery is allowed only when exact live tag/Release/assets identity revalidates. | Any source/publication/promotion mismatch, prerelease/stable conflict, asset change, or action that would perform a second stable mutation. |

Recovery never changes receipt inputs, overwrites a receipt, deletes/rewrites a ref, retags, force-pushes, or guesses remote state. When a named legal branch does not match exactly, STOP and preserve all evidence for incident handling.

## Out of scope / do-NOT-touch

- No closed receipt/schema field, Session Relay product behavior, platform matrix, asset inventory, installer algorithm, frozen public test/golden, public workflow, lockfile, or Session Relay version changes. Docks `0.13.4` and the current schema-6 controller are immutable reviewed inputs; the only public exceptions are the exact `0.10.1` -> `0.10.2` package version/pin/payload changes and the source-level dynamic fixture-version correction in `cli/test/lib/harness.ts`.
- The current `review-policy.mjs` schema-6 emitter/validator and global fallback-none/one-runtime-candidate policy are protected. Legacy compatibility is authorized only inside `scripts/lib/session-relay-release-preparation.mjs` for the one exact immutable tuple above; no helper, schema, waiver, or global policy edit is in scope.
- Do not edit any Docks path outside the six amendment paths after authorized base `25592c6...`. The full shipped-to-promoted proof must equal the exact 36-path set above. Public non-plan writes remain limited to `package.json`, `SoT/toolchain.json`, `cli/src/generated/sotPayload.ts`, and `cli/test/lib/harness.ts`, plus the child plan's manager-owned lifecycle/archive paths. `cli/test/unit/sessionRelayCli.test.ts`, `cli/test/unit/pluginRefresh.test.ts`, `.github/workflows/release-cli.yml`, and `bun.lock` stay byte-identical.
- Do not edit the existing blocked public preparation plan or rewrite its historical blocked state into success.
- No additional platform or asset; Session Relay remains four binaries plus `SHA256SUMS`, while docks-kit remains five binaries plus `SHA256SUMS`.
- No branch/ref force, tag rewrite, ref deletion, history discard, automatic retry, mutable-ref authority, guessed digest, superseded source receipt, or release reuse.
- The existing `cli-v0.10.1` tag, its stable GitHub Release at `6f9691cc19349ccd0ce81e8c8bf5cc573f76f3f1`, and `docks-kit@0.10.1` on npm are prior history from an earlier effort: never delete, retag, rewrite, republish, or reuse them.
- This plan-only dynamic-fixture scope amendment performs no workflow dispatch, tag/Release/asset mutation, publication rebind, public repository write, promotion, finalization, push, or other remote mutation. Later post-fix A12 is read/reconcile plus local receipt emission only.

## Known gotchas

- Current tooling's failure against `6c07f9...` is a safety success. Do not bypass it or substitute the old `cli-v0.9.0`; implement the test-first rebind.
- `0.10.1`/`cli-v0.10.1` is an observed prior release at `6f9691cc19349ccd0ce81e8c8bf5cc573f76f3f1`, published `2026-07-23T02:07:17Z`, whose `SoT/toolchain.json` still pins Session Relay `0.12.0`; it predates immutable companion `6c07f9...` and is not evidence from this effort. It cannot be reused and remains untouched; the fresh public identity is exactly `0.10.2`/`cli-v0.10.2`.
- The 14 legacy-policy appearances are repeated nested copies inside one exact completion receipt, not 14 independent artifacts. Count and validate one raw receipt plus one unique settled state; never broaden acceptance by iterating those copies as separate authorities.
- The companion `Review-receipt` raw SHA `097206...` is legacy preparation history but is outside A10's policy-validation path. Loading or accepting it to make A10 pass proves the binder reached the wrong boundary.
- `shipped_commit` and `promoted_commit` are intentionally different after correction. Equality is no longer the invariant; exact producer-derived ancestry, current clean HEAD, and the closed changed-path set are.
- The active publication plan is one amendment path and remains part of both exact path fences. Manager-owned status/evidence commits may advance its blob without changing implementation bytes; rerun the final full gate if any of the five amended implementation/test blobs changes.
- Session Relay tag points to product source `3fb9211...`, not authorized Docks base `25592c6...`, the later Docks publication implementation, or the finished-plan archive.
- GitHub Release `created_at` is not the publication event clock here: the observed value equals the tagged commit timestamp and predates the workflow. `published_at`, asset timestamps, exact actors, and bound asset digests remain the run-authoritative publication evidence; do not add a commit-time adapter read or loosen those windows.
- Public tag points to the child completion receipt's reviewed implementation commit, not its later plan archive commit or ambient public HEAD.
- Source Release has five assets; public docks-kit Release has six. Their `SHA256SUMS` row counts are four and five respectively.
- Workflow success alone is not npm proof because the public workflow may downgrade the known trusted-publisher failure to a warning. Record only the two closed npm states with their required evidence.
- GitHub and npm visibility are eventually consistent. Bounded read-only waiting is allowed; a timeout does not authorize a duplicate dispatch, retag, or reused cache.
- A remote push can succeed while the client exits nonzero. Reconcile the exact ref/branch once; never blind-retry.
- Hash exact raw canonical receipt bytes, not parsed JSON, Markdown labels, trimmed `git show`, or a reconstructed line.

## Global constraints

- Correctness, provenance, and no-history-loss outrank release speed.
- All commit ids are full lowercase 40-hex; all receipt/input digests are lowercase 64-hex; all fixed refs and versions are exact.
- Every external mutation is authorized by a passed independent plan/completion review and uses create-once or compare-and-swap semantics with exact read-back.
- Receipt output is canonical JCS, no trailing newline, mode `0600`, no-clobber, under one fresh mode-`0700` owner directory.
- No secrets enter plans, receipts, refs, release bodies, logs, command output, or source.
- Plan-manager owns every review, receipt, status, step-status, evidence, completion, and archive write. Plan-creator owns only each missing plan's add-only creation commit.
- The exact-artifact compatibility exception must remain binder-local, waiver-free, and closed over the pinned finished path/candidate/receipt/policy/reviewed-head/input/series/state tuple. Current schema-6 policy remains fallback none globally.
- Any implementation/test change after the new final full gate, current-main base ancestry failure, exact 36-path or six-path fence mismatch, immutable tuple change, wrong/nonunique workflow or asset set, noncanonical receipt, failed focused/full/live gate, failed independent final code review, or publication drift is STOP. The prior caller-authorized timestamp amendment altered no start/execution identity; this later dynamic-fixture scope expansion is substantive changed input and remains blocked until fresh current-policy review passes.

## STOP conditions

- The original passed draft review, retained `started_at`, and `execution_base_commit` remain historical lifecycle authority, but the prior receipt cannot authorize the new public fixture path. Keep the plan blocked until one fresh schema-6 draft review of this exact four-file public scope passes and plan-manager consumes the current user's unblock authorization.
- STOP if any Docks implementation exceeds completed 4e-4f, if public implementation exceeds the four non-plan paths authorized here, if review orchestration bypasses the current helper, or if historical review/receipt evidence or protocol identities are rewritten.
- STOP if historical A9a receipt/blob evidence is not preserved byte-for-byte, if A9a2 does not freeze a formatted test-only intended failure against the actual archived completion receipt/state with production restored, if the A9a2 test changes afterward, or if A9b-A9d do not bind the same new final implementation tree.
- STOP if Biome formatting changes any assertion, string, fixture, mutation, validator reach, or behavioral case, or if the formatted test does not fail with the intended binder-contract failure while production is restored exactly to blob `a0d015...`.
- STOP if the immutable A9a receipt no longer hashes to `eb498e...`, if either legacy-policy receipt is deleted, overwritten, aliased, noncanonical, or not mode `0600`, or if final `HEAD`'s test blob differs from A9a2's recorded blob.
- STOP on any unpinned legacy receipt/policy/state, any mutation or absence/duplication of the exact tuple, any waiver, any companion-policy-validator reach from A10, or any need to edit `plugins/docks/skills/productivity/plan-reviewer/scripts/review-policy.mjs`.
- STOP before the post-fix receipt rebind unless A9e captures the one intended timestamp red, A9f-A9g pass on the minimal correction, independent final code review passes, plan-manager records its evidence, non-force push establishes exact final `HEAD == origin/main`, and A9h binds valid source proof v2.
- STOP if either pre-existing A1/A1b red test blob, authoritative A9a2 blob, or new A9e test blobs change after capture; if authorized base `25592c6...` is not an ancestor of the final implementation commit; or if the shipped-to-promoted and base-to-promoted changed-path sets differ from their exact 36-path and six-path contracts.
- STOP if Docks `origin/main` cannot become the exact reviewed corrected publication implementation commit by non-force ancestry-preserving push, or if it changes afterward.
- STOP if v2 proof binding cannot derive the completion-reviewed evidence commit, exact shipped archive, clean corrected promoted HEAD, both closed path fences, or public companion from committed evidence; v1 remains preserved, never a fallback authority.
- STOP on a second matching workflow or any dispatch; changed tag target, Release id/body/state, actor, asset id/set/size/digest/checksum/timestamp/provenance, or fixed ref; stable Release; extra path; any need to relax release `published_at` or asset run-window checks; missing/noncanonical red or proof receipt; or failed post-fix rebind. Preserve the existing prerelease and emit no publication receipt on failure.
- STOP if the public child plan is not created from `6c07f9...`, is not independently reviewed, changes either frozen unit test, any golden, the release workflow, `bun.lock`, unrelated paths, any `package.json` byte other than the exact `0.10.1` -> `0.10.2` version value, or any `cli/test/lib/harness.ts` behavior beyond reading current SoT `verified` for fixture version materialization while preserving reinstall assertions and curl behavior, or cannot completion-review before tagging.
- STOP if promotion's transaction identity, lock, expected main, restore proof, journal chain, exact/live smoke, or CAS result is not authoritative terminal success.
- STOP if stable finalization or live version/self-test/workspace checks fail. Do not archive this plan on partial publication.

## Cold-handoff checklist

- **File manifest:** the exact six authorized-base Docks paths, exact 36-path shipped closure, four exact public non-plan implementation paths (`package.json`, `SoT/toolchain.json`, `cli/src/generated/sotPayload.ts`, `cli/test/lib/harness.ts`), exact protected test/workflow/lockfile paths, exact child plan, and manager-owned lifecycle/archive path rules are enumerated.
- **Environment and commands:** both repositories, branches/bases, runtime pins, versions/tags, sole workflow/release timestamps, refs/runs/digests, retained receipt root, no-clobber v1/red/v2 paths, CLI grammar, and A1-A24 are explicit.
- **Interface and data contracts:** closed `SourcePreparationProofV1`, immutable v1 versus authoritative v2 use, producer-bound promoted semantics, corrected Release timestamp authority, public request, child handoff, release receipts, assets, workflow, npm states, and commit roles are defined before use.
- **Executable acceptance:** ordered historical evidence, timestamp red, focused/full gates, final identity/v2 bind, publication rebind, public release, promotion, finalization, and live checks have observable expected results.
- **Out of scope:** product/schema/version/platform/workflow/lockfile/frozen-test/golden/unrelated-path/ref-rewrite changes, fixture behavior beyond dynamic SoT version materialization, a new commit-timestamp adapter read, and any second workflow are prohibited.
- **Decision rationale:** the Docks timestamp fix preserves fail-closed publication provenance and current main while removing only the false Release `created_at` run-window requirement; the public fixture correction is separately justified by the observed focused failure and reads the already-authoritative SoT version without weakening assertions or transport behavior.
- **Known gotchas:** commit-role separation, Release-versus-publish clocks, active-plan allowlist, asset counts, npm warning semantics, eventual consistency, and raw-byte hashing are explicit.
- **Global constraints:** exact identities, no-clobber evidence, manager write ownership, create-once/CAS mutation, one post-fix receipt-only rebind, and secret hygiene are fixed.
- **No undefined terms:** every environment variable is assigned by its producing command or exact child handoff before consumption; v1, red, v2, and publication outputs are distinct named paths.

A cold read using only this plan and its cited sources leaves no user decision unresolved. Any observation outside the fixed contracts routes to a named STOP/amendment path rather than improvisation.

## Self-review

- `standalone_executability` — caught/fixed: added exact repository bases, receipt setup, commit-role definitions, child-plan contract, CLI commands, and external recovery ownership so a cold executor does not need conversation context.
- `actionability` — caught/fixed: separated red tests, protocol implementation, full gate/main publication, proof binding, source publication, child release, Docks verification, promotion, finalization, and archive into exact-path rows with observable completion/failure actions.
- `dependency_order` — pass: legacy-contract red precedes production edits; implementation/full gate/main equality precede proof binding; proof precedes source publication; source digests precede public production; public completion precedes Docks verification/promotion; promotion precedes stable finalization; both child completion and live proof precede parent completion.
- `evidence_reverification` — pass: re-opened the finished recertification evidence, current binder/proof validation, legacy public constants, release CLI grammar, immutable public companion plan/ancestry, package/SoT pins, public workflow, and prior production protocol.
- `goal_coverage` — caught/fixed: explicitly included the mandatory test-first legacy-identity correction, public child lifecycle, both asset inventories, no-rollback main binding, live self-test/workspace checks, and completion/archive of both new plans.
- `executable_acceptance` — caught/fixed: added ordered A1-A24 covering intended red, focused/full gates, canonical receipt chain, exact remote identities, public production checks, authoritative promotion, final stable state, and fresh binary behavior.
- `failure_modes` — caught/fixed: added closed recovery matrix and STOP branches for main drift, tag/Release conflicts, workflow uniqueness, remote-push ambiguity, receipt loss, transaction recovery, npm warning, extra paths, and failed review/live gates.
- `open_questions` — pass: version/tag/commits/paths/assets/recovery rules are fixed by authoritative evidence; no destructive fallback or unresolved human choice is silently guessed.

This is the single mandated local author critique pass. It is not independent review evidence and carries no score or lifecycle effect.

Archive-path amendment self-review (`2026-07-23T20:20:09-03:00`): one-pass inspection found only the local Docks recertification lookup stale; the archived Notes preserve the blocked public tuple byte-for-byte and the pinned public-ref assertions remain valid. The 35-path shipped fence and four-path base fence add exactly `plugins/session-relay/test/companion-distribution-contract.mjs`; the second test-only red precedes that production fence update, and every subsequent mutation remains behind fresh current-policy review and the existing release receipt chain.

Legacy completion-receipt amendment self-review (`2026-07-23T21:48:41-03:00`): the exhaustive A10 path is now closed over one immutable completion receipt and one unique settled state; its three legacy policy mismatches, 14 nested policy copies, and required orchestration context are explicit without treating the companion receipt as an A10 blocker. One new frozen test-first red precedes the binder-only production change, ordinary schema-6 fallback-none validation stays untouched, the exact four-path/35-path fences remain fixed, and prior A8/A9 evidence for `c27955df150bcdde6fe651f6e10f3fbc6feb9c37` is preserved as superseded history rather than reused.

Formatter-safe red recovery amendment self-review (`2026-07-23T22:38:59-03:00`): the first red commit/receipt/blob and production commit are recorded as immutable history; the earlier full gate is plainly failed formatting evidence, not passing A9c. The recovery restores production before formatting the test, captures a distinct no-clobber A9a2, reapplies identical binder logic before formatting production, and binds both receipts plus the final test blob before focused and one passing final gate. Exact four-path/35-path fences, schema-6 policy, started identity, execution base, and prior review/receipt histories remain unchanged. Semantic formatter drift or a nonfailing formatted test on restored production is STOP.

Timestamp recovery amendment self-review (`2026-07-23T23:32:28-03:00`): the immutable tag/run/prerelease projection explains the deterministic defect without introducing commit-time API authority. Test-only 4e freezes the real commit-time `created_at`, in-run `published_at`, in-run assets, and sole run before the only two production edits. Production preserves finite ordered Release timestamps, strict published/asset windows, actors, closed assets/checksums/provenance, drift re-read, and post-run reconciliation. The final fence grows mechanically from four/35 to six/36 by exactly the publication test and production paths. Distinct red and source-proof-v2 outputs preserve v1 SHA `419b23...`; Step 6 remains in flight, the prerelease remains unchanged, and the unavailable renewal is recorded rather than misrepresented as a passed review.

Dynamic public-fixture scope amendment self-review (`2026-07-24T01:34:24-03:00`): the already-observed public child A6 failure is recorded as evidence, not waived. The amendment adds exactly `cli/test/lib/harness.ts` to the three existing public non-plan paths, limits its source change to deriving fixture `session-relay` version text from current `SoT/toolchain.json`, preserves reinstall assertions and curl behavior, and makes A15-A18 prove focused/full gates, exact four-file scope, and immutable unit-test/workflow/lockfile blobs. All Docks implementation fences, release identities, historical receipts, `started_at`, and `execution_base_commit` remain unchanged; the plan is blocked pending fresh current-policy review.

## Open questions

None.

## Sources

- `docs/plans/finished/2026-07-23-session-relay-linux-workspace-recertification.md` — completed source/candidate/native/source-CI identities and passed completion evidence.
- `scripts/lib/session-relay-release-preparation.mjs` — closed candidate/proof schemas, completion receipt binder, ancestry, tree-equivalence, and current promoted-equals-shipped limitation.
- `scripts/lib/session-relay-release-promotion.mjs` — legacy public constants, public request/release validation, six-asset/npm boundary, transaction/CAS journal, and live smokes.
- `scripts/lib/session-relay-release-publication.mjs` — tag/workflow/five-asset prerelease publication, resume/rebind, and stable finalization.
- `scripts/lib/session-relay-release-cli.mjs` — exact mode grammar, one positional version, adjacent receipt/digest pairing, recovery flags, and no-clobber outputs.
- `plugins/session-relay/test/release-evidence-contract.mjs`, `release-publication-contract.mjs`, `release-promotion-contract.mjs`, `distribution-contract.mjs`, and `companion-distribution-contract.mjs` — focused contract/fixture boundaries to change or preserve.
- `/home/vagrant/projects/public/docs/plans/active/session-relay-cli-0.13.0-release-preparation.md` — immutable blocked companion tuple, frozen red evidence, runtimes, and source/preparation contract.
- `/home/vagrant/projects/public/package.json`, `SoT/toolchain.json`, `cli/src/generated/sotPayload.ts`, `cli/test/lib/harness.ts`, `cli/test/unit/sessionRelayCli.test.ts`, `cli/test/unit/pluginRefresh.test.ts`, `.github/workflows/release-cli.yml`, and `bun.lock` — exact four-file public implementation scope; observed hardcoded `0.12.0` fixture mismatch; protected frozen tests; tag-triggered build/release/npm behavior; immutable lockfile; and six assets.
- `/home/vagrant/projects/public/docs/plans/finished/2026-07-18-session-relay-cli-production-release.md` — prior public production-release ordering, completion-reviewed tag commit, single-run/read-back, npm, asset, and archive protocol; identities are historical and must be rebound.
- `docs/plans/finished/2026-07-19-session-relay-prebuilt-cli-release.md` — prior end-to-end publication/promotion/finalization and exact/live smoke recovery lessons; prior receipts and versions are not reusable.
- `https://github.com/DocksDocks/public/releases/tag/cli-v0.10.1` and `refs/tags/cli-v0.10.1` at `6f9691cc19349ccd0ce81e8c8bf5cc573f76f3f1` — the observed prior historical docks-kit release (published `2026-07-23T02:07:17Z`, pinning Session Relay `0.12.0`) that conflicts with reusing `0.10.1`/`cli-v0.10.1`; exact revalidation observed only the lightweight tag row (no peeled `^{}` object) via `git ls-remote --tags`, and the npm `docks-kit` version list ending at `0.10.1`, leaving `0.10.2` free; recorded as untouched history only, never as evidence for this effort.
- `fb3f5b7` and `25592c6550069e300a7a0148d3cd3c21880da8e7` — independently reviewed/current-schema implementation and Docks `0.13.4` release tail that the amended two-fence binder treats as immutable current-main input.
- The exact immutable `Completion-review-receipt` and settled `Review-orchestration-state` embedded in `docs/plans/finished/2026-07-23-session-relay-linux-workspace-recertification.md` — the sole A10 legacy-policy artifact/context tuple; raw receipt SHA `d929ab...` and settled state SHA `064e08...`.
- `plugins/docks/skills/productivity/plan-reviewer/scripts/review-policy.mjs` — protected current fallback-none/one-runtime-candidate schema-6 behavior used as an unchanged control, not an amendment path.

## Notes

- Administrative execution evidence (`2026-07-23T22:58:51-03:00`): the attempted fresh changed-input review emitted zero reviewer launches. The correct `replaceReviewTerminalFamily` recovery failed with `replacement source must be an exact terminal family`; no controller bypass or machine-record rewrite occurred. This administrative unblock does not claim that a fresh review passed.
- The current user explicitly authorizes this exact timestamp-recovery amendment without review renewal. Authority is limited to 4e-4f, the six/36-path contract, source proof v2, and one post-fix receipt-only rebind; it does not authorize orchestration rewrites, another workflow, relaxed published/asset windows, or any remote mutation in this amendment commit.
- Formatter recovery/final binder evidence (`2026-07-23T23:20:41-03:00`): production-only revert `0e4af0299a38c6303dc3dcf5bb7f704c5c5e2b43` restored `scripts/lib/session-relay-release-preparation.mjs` blob `a0d015415d735bb82c2fbcf28e7a92b5a18d7743`. The formatter-oracle proof superseded the mechanically wrong ignore-space command: formatting the exact baseline blobs through pinned Biome yielded byte-identical current files, with no assertion, string, fixture, mutation, or production-logic change. Formatted test-only commit `f85d62a60262b8931e64261b2b868037eecc5c7b` has blob `d7c1553a1344045956f2b022b87a9a9c12571270`. Its authoritative A9a2 receipt is canonical mode `0600`, SHA-256 `8ed9268e0657fbdfc61dcd3b846524ec9c1671e9aa0fd465b8cc90df2eaf47e3`, and records the exact intended red `fallback must be none`; historical A9a remains untouched at SHA-256 `eb498e500685f86b00807482bdbf32c710653b138d30fae2c8ae5baca116090e`. Formatted production commit `36ad556cd4bbcddcd55e0d9d0983cfd095b8e5b8` has blob `2e2ec1e274630ff2600494b40081305201bc3b31`. A9b's focused evidence, companion, publication (46), promotion, distribution (7), and hash checks passed; targeted `pnpm run check:js` passed with only pre-existing warnings; passing A9c `node scripts/ci.mjs` passed all repo/plugin/JS checks, while the earlier failed run remains formatting evidence rather than a pass. The independent final binder reviewer returned `correct`, confidence `0.98`, with no findings. The clean formatter-final tree has exactly 35 changed paths from `cdca867e6a140311ea865a81229fb30de1df32c1` and four from `25592c6550069e300a7a0148d3cd3c21880da8e7`; no implementation/test bytes changed after the passing gate/review.
- Publication incident handoff (`2026-07-23T23:32:28-03:00`): current clean `HEAD` and `origin/main` were both `31a994622b4b960e8fa88b433e717d27e6c82ad7` before this plan-only amendment. Source proof v1 exists at SHA-256 `419b23ccdcf0ca21672e81c05ae9d22c55bc67781839ffb6a29e7eecc2b59396` and no publication receipt exists. Initial A12 created the one tag/run/prerelease; both that invocation and the one legal pre-fix rebind stopped only on `release created_at timestamp is outside the bound workflow run window`. No second run was selected or dispatched and the remote prerelease remains preserved. Step 5 is done; Publish Step 6 remains `in-flight`.
- Timestamp red provenance (`2026-07-24T00:07:24-03:00`): `$RECEIPT_DIR/publication-timestamp-red.json` at test commit `933a448104eb4892e36cc8283189cb3ca5d3f22c` is preserved mode `0600`, SHA-256 `e6641453335ae4529337d8dd7717c32c4db7ecf60a305366d48ae54f1e4a7ccf`, but superseded because the evidence path array had an intrinsic sort-assertion defect. `$RECEIPT_DIR/publication-timestamp-red-v2.json` at `3cce4349c11ad58c510b3037470695fe1fd91679` is preserved mode `0600`, SHA-256 `bf2760a33fd63c9487227ae132152412d9e9f35ca3fdeafd29a28196b1ab4ab8`, but superseded because a mechanical correction accidentally reordered the unrelated expected publication/promotion command list. Both files remain immutable no-clobber history and never authority. The authoritative `$PUBLICATION_TIMESTAMP_RED_RECEIPT` is v3 at corrected test-only commit `262c424b279e3f2826e94a4b935f6b87979a237c`, mode `0600`, SHA-256 `f87a248cdc93760d4734229dfdc414f750b19bfe965b0e717e5ffa19f4a52d5d`, with publication blob `4c6928238f206d6f3dbf13c2fa5dcce732c6306a` and evidence blob `9ff3706f1d5ddfabbcb3cb282887a4da3039004d`. Its publication red is exactly `release created_at timestamp is outside the bound workflow run window`; the paired evidence red is exactly `shipped-to-promoted diff does not contain exactly the allowed plan lifecycle paths`. Production remained unchanged until v3.
- Timestamp implementation evidence (`2026-07-24T00:07:24-03:00`): production commit `7193917e17aac0d64295a9b7ee89ac20e9b013be` edits only `scripts/lib/session-relay-release-publication.mjs` and `scripts/lib/session-relay-release-preparation.mjs`. It removes only Release `created_at` run-window membership while retaining finite/order and every published/asset/actor/digest/provenance check, and adds the exact 36-path shipped-to-promoted and six-path authorized-base-to-promoted fences. A9f passed publication (68), evidence, distribution (7), and skill-hash checks; A9g full `node scripts/ci.mjs` passed all repo/plugin/JS checks. Independent final code review returned `correct`, confidence `0.99`, with no findings. Independent audit confirmed the test commits precede production and that the sole pending condition is expected pre-push remote mismatch: clean local implementation `HEAD` was `7193917e17aac0d64295a9b7ee89ac20e9b013be`, while `origin/main` remains `31a994622b4b960e8fa88b433e717d27e6c82ad7`. No implementation/test bytes changed after the passing gate/review. This manager write preserves `ongoing`, `started_at`, `execution_base_commit`, and the honest zero-renewal record; it reruns no review, test, formatter, CI, receipt, source proof, workflow, release, rebind, push, or remote action. Its committed read-back becomes the candidate corrected `PUBLICATION_IMPLEMENTATION_COMMIT`; A9h non-force push/origin equality/source-proof-v2 and A12 receipt-only publication rebind remain next.
- Dynamic public-fixture scope authorization (`2026-07-24T01:34:24-03:00`): the current user authorizes exactly one additional public non-plan path, `cli/test/lib/harness.ts`, solely to replace hardcoded fixture materialization of `session-relay 0.12.0` with the current verified version read from `SoT/toolchain.json`. The observed focused A6 failure is rationale, not a waiver. `cli/test/unit/sessionRelayCli.test.ts`, `cli/test/unit/pluginRefresh.test.ts`, `.github/workflows/release-cli.yml`, and `bun.lock` remain byte-immutable; no implementation, test, CI, tag, Release, npm, push, receipt, or other external mutation occurs in this Docks amendment.

### Timestamp recovery evidence

| Evidence | Observable command | Immutable observation |
|---|---|---|
| E1 tag/head | `git ls-remote --tags origin refs/tags/session-relay--v0.13.0 'refs/tags/session-relay--v0.13.0^{}'` | Exactly one lightweight tag row at `3fb9211f3309977f24853a10714d4b7a82b38c8f`; no alternate or peeled target. |
| E2 sole workflow | `gh api repos/DocksDocks/docks/actions/runs/30061627175 --jq '{id,run_attempt,event,head_sha,status,conclusion,run_started_at,updated_at}'` | Run `30061627175`, attempt `1`, push event, head `3fb9211...`, started `2026-07-24T02:26:14Z`, updated `2026-07-24T02:28:56Z`, completed success; the matching-run inventory count is exactly one. |
| E3 prerelease projection | `gh api repos/DocksDocks/docks/releases/359049927 --jq '{id,tag_name,target_commitish,draft,prerelease,created_at,published_at,assets:[.assets[]|{id,name,size,digest,created_at,updated_at,uploader}]}'` | Prerelease id `359049927`, exactly five unchanged assets, `created_at` `2026-07-23T14:26:39Z`, `published_at` `2026-07-24T02:28:53Z`, and every asset `created_at` `2026-07-24T02:28:52Z`; published/assets fall within E2's window. |
| E4 commit-time explanation | `gh api repos/DocksDocks/docks/git/commits/3fb9211f3309977f24853a10714d4b7a82b38c8f --jq .committer.date` | `2026-07-23T14:26:39Z`, exactly equal to E3 release `created_at`; observation only, not a new runtime adapter dependency. |
| E5 local evidence | `test "$(sha256sum "$SOURCE_PROOF" | cut -d' ' -f1)" = 419b23ccdcf0ca21672e81c05ae9d22c55bc67781839ffb6a29e7eecc2b59396 && test "$(sha256sum "$PUBLICATION_TIMESTAMP_RED_RECEIPT" | cut -d' ' -f1)" = f87a248cdc93760d4734229dfdc414f750b19bfe965b0e717e5ffa19f4a52d5d && test ! -e "$PUBLICATION_RECEIPT" && test ! -e "$SOURCE_PROOF_V2"` | Source proof v1 and authoritative timestamp red v3 are immutable; initial A12 and the pre-fix rebind emitted no `publication.json`. Source proof v2 and publication receipt remain distinct future no-clobber outputs; no push, v2 bind, or receipt-only rebind has occurred. |

Review-orchestration-state: {"apply_state":"none","current_input_sha256":"c9cf4838768a7ffc8a4d2ea49d3a8f8e2f4ae7e6039985874d22df92b7efa307","initial_input_sha256":"c9cf4838768a7ffc8a4d2ea49d3a8f8e2f4ae7e6039985874d22df92b7efa307","lifecycle_intent":"none","orchestration_attempt":1,"phase":"draft","plan_path":"docs/plans/active/session-relay-linux-workspace-publication.md","request_ids":["fc729b56-b286-4d74-9c01-6ea5c2c973ba"],"retry_authorization":null,"round_index":1,"schema":2,"series_id":"e9d8aff3-ad15-4508-8125-7f35e548d325","series_sha256":null,"state_sha256":"401c0ef7418b11b0e5aa60c8085190cb126e07370343a624206f42449922ab59","status":"active","stop_reason":null,"terminal_evidence_sha256":null,"terminated_from_state":null,"terminated_from_state_sha256":null,"transitioned_from_state_sha256":null}
Review-orchestration-prepared-request: {"lifecycle_intent":"none","orchestration_series_id":"e9d8aff3-ad15-4508-8125-7f35e548d325","orchestration_state_sha256":"401c0ef7418b11b0e5aa60c8085190cb126e07370343a624206f42449922ab59","phase":"draft","plan_path":"docs/plans/active/session-relay-linux-workspace-publication.md","prepared_at":"2026-07-24T01:38:29-03:00","request":{"acceptance_inventory_sha256":null,"author":{"company":"openai","effort":"high","model":"gpt-5.6-sol","tool":"codex"},"bundle_sha256":"65f91a008026254cd6b84250e66fcfc17ddee6f2160c0560c040a31b25b2af4e","diff_sha256":null,"execution_base_commit":null,"input_sha256":"c9cf4838768a7ffc8a4d2ea49d3a8f8e2f4ae7e6039985874d22df92b7efa307","lifecycle_intent":"none","orchestration_series_id":"e9d8aff3-ad15-4508-8125-7f35e548d325","orchestration_state_sha256":"401c0ef7418b11b0e5aa60c8085190cb126e07370343a624206f42449922ab59","phase":"draft","planned_at_commit":null,"policy":{"candidates":[{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"}],"fallback":"none","max_rounds":2,"provenance":{"candidates":"runtime_global","fallback":"skill_default","max_rounds":"skill_default","role":"skill_default"},"role":"primary","schema":6},"policy_sha256":"4317663e576334beacd0f174120385311e3f9d36f86101ee888437eab438800f","previous_input_sha256":null,"repair_targets_sha256":null,"request_id":"fc729b56-b286-4d74-9c01-6ea5c2c973ba","review_mode":"full","reviewed_commit_or_head":"cf9fbcc4601679cfc1aeb2529093ede72142f93e","round_index":1,"schema":6},"request_ids":["fc729b56-b286-4d74-9c01-6ea5c2c973ba"],"request_sha256":"cf13ac811ce0ce2dcebf61b502e9e6b2f58f15faf4874617bee2ed2f8ad4d37c","schema":1,"type":"ReviewPreparedRequestV1"}
## Review

(filled by main-context plan-manager after completion evidence)
