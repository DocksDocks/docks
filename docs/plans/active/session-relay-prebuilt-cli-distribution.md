---
title: Deliver Session Relay as a prebuilt CLI
goal: Prepare reviewed launcher-only Session Relay 0.12.0 and docks-kit 0.9.0 sources plus the resumable release tooling required by a separate release plan.
status: ongoing
created: "2026-07-16T04:10:15-03:00"
updated: "2026-07-17T13:56:58-03:00"
started_at: "2026-07-17T13:55:37-03:00"
assignee: null
review_author_company: openai
review_author_tool: codex
review_author_model: gpt-5.6-sol
review_author_effort: high
review_waivers: []
tags: [session-relay, distribution, rust, github-actions]
affected_paths:
  - .claude-plugin/marketplace.json
  - .github/AGENTS.md
  - .github/workflows/build-binaries.yml
  - .github/workflows/ci.yml
  - plugins/session-relay/AGENTS.md
  - plugins/session-relay/.claude-plugin/plugin.json
  - plugins/session-relay/.codex-plugin/plugin.json
  - plugins/session-relay/bin/SHA256SUMS
  - plugins/session-relay/bin/relay
  - plugins/session-relay/bin/relay-aarch64-apple-darwin
  - plugins/session-relay/bin/relay-aarch64-unknown-linux-musl
  - plugins/session-relay/bin/relay-x86_64-apple-darwin
  - plugins/session-relay/bin/relay-x86_64-unknown-linux-musl
  - plugins/session-relay/rust/Cargo.lock
  - plugins/session-relay/rust/Cargo.toml
  - plugins/session-relay/rust/src/main.rs
  - plugins/session-relay/skills/productivity/session-relay/SKILL.md
  - plugins/session-relay/test/distribution-contract.mjs
  - plugins/session-relay/test/selftest.mjs
  - scripts/AGENTS.md
  - scripts/capture-tdd-red.mjs
  - scripts/ci.mjs
  - scripts/lib/plugins.mjs
  - scripts/lib/rust-bin.mjs
  - scripts/release.mjs
  - scripts/verify-session-relay-preflight.mjs
related_plans: []
review_status: null
planned_at_commit: 025a00af547547f53fb4e1fbe01823b55a99181a
execution_base_commit: 327ea47ec365917b8a268530c565d9397e197655
---

# Deliver Session Relay as a prebuilt CLI

## Goal

Prepare Session Relay as a normal precompiled CLI: the reviewed `0.12.0` source contains only a small launcher, GitHub Actions can build the four supported Rust targets plus `SHA256SUMS`, and a companion reviewed `docks-kit` `0.9.0` source installs an exact host asset whose digest is pinned independently in its repository. This plan also implements and tests prerelease staging, finalization, serialized promotion, smoke receipts, compatibility restoration, and retry machinery. Actual tags, Releases, npm publication, and branch promotion belong to a separate plan created and reviewed only after this source-preparation plan finishes.

## Context & rationale

The current plugin commits four compiled executables and dispatches among them from `plugins/session-relay/bin/relay`. This makes the plugin payload large, obscures provenance during review, and requires an unusual build-artifact commit before every release. The chosen replacement follows the conventional Rust CLI pattern used by projects such as Grok Build: compile per platform in CI, publish immutable checksummed release assets, and let an installer place one host executable on the user's PATH.

The plugin must not download executables from hooks or MCP startup. Those paths are latency-sensitive and operate on untrusted plugin installation state. The plugin launcher therefore performs resolution only and fails with an actionable installation command. `docks-kit` owns downloads, checksum verification, smoke testing, and atomic replacement in the companion repository.

This plan owns the Docks release producer, final launcher-only plugin payload, plugin-consumer contract, resumable release state machine, and immutable handoff to a later release plan. A companion plan in `/home/vagrant/projects/public` owns the `docks-kit` installer and its source-pinned asset schema. Session Relay is the required cross-project implementation transport; the public worker must be dispatched while the current embedded binary still exists. Each repository retains its own plan lifecycle and commit history. The only external mutations allowed here are immutable validation refs for the exact Docks and public source commits; this plan performs no release tag, Release, npm, or production-branch mutation.

## Environment & how-to-run

- Node.js 24 and the lockfile-pinned pnpm dependencies: `corepack enable && pnpm install --frozen-lockfile`.
- Rust toolchain: `plugins/session-relay/rust/rust-toolchain.toml`; all builds use `cargo build --release --locked`.
- Focused Docks tests:
  - `node plugins/session-relay/test/distribution-contract.mjs`
  - `cargo build --manifest-path plugins/session-relay/rust/Cargo.toml --release --locked && SESSION_RELAY_TEST_BIN="$PWD/plugins/session-relay/rust/target/release/relay" node plugins/session-relay/test/selftest.mjs`
  - `node scripts/ci.mjs --plugin session-relay`
- Required broad Docks gate before commit: `node scripts/ci.mjs`.
- Cross-repository dispatch while the embedded Relay is still present:
  - Set `PARENT_SESSION_ID` to the current registered Session Relay UUID.
  - Run this literal task:

    ```bash
    plugins/session-relay/bin/relay spawn /home/vagrant/projects/public --fanout --from "$PARENT_SESSION_ID" --tool codex --model gpt-5.6-sol --effort high --service-tier default -- 'Read /home/vagrant/projects/public/AGENTS.md and every applicable nested AGENTS.md, then read the producer contract in /home/vagrant/projects/docks/docs/plans/active/session-relay-prebuilt-cli-distribution.md. Use the public repository plan-manager to create, independently review, and start /home/vagrant/projects/public/docs/plans/active/session-relay-cli-installation.md. Use TDD: add installer and sync-order tests first. Before public production edits, create a fresh canonical /tmp/session-relay-public-red.* directory at mode 0700, retain its canonical path plus device/inode identity in plan-manager operation state, and write the one canonical TddRedReceiptV1 there at mode 0600. Immediately copy the exact one-line RFC 8785 JCS bytes and their SHA-256 into fixed companion-plan Notes fields without parsing, reserialization, or rewrite; verify the embedded bytes and hash against the temporary file, then re-check the directory canonical path plus device/inode and remove only that directory. Do not depend on an EXIT trap, environment variable, or temporary directory surviving another shell, and never recapture after production edits. The receipt must bind repository identity, pre-production commit, exact command, nonzero exit, and stdout/stderr SHA-256 values; do not alter the frozen tests while implementing. Add a closed SoT/toolchain entry for Session Relay 0.12.0 from DocksDocks/docks with the four target names, fixture-only 64-hex digests until production assets exist, and install path ~/.local/bin/session-relay. Implement supported OS/architecture mapping, checksum and source-pin equality, staged chmod 0755, exact --version smoke, atomic replacement preserving any previous executable on every failure, and clear unsupported-platform errors. Every docks-kit sync targeting Claude or Codex must ensure the CLI before Session Relay plugin installation or refresh; an agents-only sync must not ensure it. Regenerate the embedded SoT payload, update docs and focused/golden tests, run the public repository full CI, and commit a clean implementation. Push only that exact commit without force to refs/heads/preflight/session-relay-cli-0.9.0-<first 12 commit hex>; do not advance public main. Transition the companion plan to blocked with the exact reason Awaiting the four independently hashed `session-relay--v0.12.0` production asset digests. Hand back the clean commit SHA, immutable validation ref, repository ID, companion plan input SHA-256, execution-base commit, review-receipt SHA-256, the companion-plan path and exact Notes field names containing the TddRedReceiptV1 canonical bytes and SHA-256, status, exact blocked reason, focused/full test evidence, and changed paths. Do not publish, tag, push public main, or edit the Docks repository.'
    ```

  - Record the returned worker UUID as `PUBLIC_WORKER_ID`.
  - `plugins/session-relay/bin/relay collect "$PUBLIC_WORKER_ID" --from "$PARENT_SESSION_ID"`; repeat once and require the same committed handback to prove idempotent collection.
  - Immediately after collection, the Docks plan-manager reads the fixed companion-plan Notes fields at the collected immutable commit, verifies the public JCS bytes are canonical `TddRedReceiptV1`, verifies their recorded SHA-256 and frozen public identities, copies those exact bytes without parsing, reserialization, newline insertion, or other rewrite into this plan's fixed `Companion TDD-red receipt JCS bytes` Notes field, and writes the same hash into the adjacent SHA field. It re-reads both plans byte-for-byte before any Docks production edit; conversational handback text is not receipt evidence.
- Skill maintenance after the shipped skill text changes:
  - `node scripts/skills/content-hash.mjs --fix plugins/session-relay/skills`
  - `node scripts/skills/content-hash.mjs --check-only plugins/session-relay/skills`
- GitHub workflow syntax is validated by the repository CI gate; no release workflow is dispatched before both implementations and local gates are complete.
- Exact producer preflight is required before completion review:
  - Step 8 starts one fresh plan-manager-owned producer-preflight shell only after both fixed TDD-red Notes byte/hash pairs are non-pending. In that shell run `REQUEST_DIR="$(mktemp -d /tmp/session-relay-preflight.XXXXXXXX)"`, `REQUEST_DIR="$(realpath -e -- "$REQUEST_DIR")"`, `case "$REQUEST_DIR" in /tmp/session-relay-preflight.*) ;; *) exit 1 ;; esac`, `chmod 0700 "$REQUEST_DIR"`, `test "$(stat -Lc '%a' -- "$REQUEST_DIR")" = 700`, and `REQUEST_DIR_ID="$(stat -Lc '%d:%i' -- "$REQUEST_DIR")"`. Install the exact scoped cleanup with `cleanup_request_dir() { current="$(realpath -e -- "$REQUEST_DIR")" || return 0; test "$current" = "$REQUEST_DIR" || return 0; test "$(stat -Lc '%d:%i' -- "$current")" = "$REQUEST_DIR_ID" || return 0; rm -rf -- "$current"; }; trap cleanup_request_dir EXIT`. This trap owns only the Step 8 shell and preserves the directory on any identity mismatch; no earlier capture directory, trap, or environment survives into it.
  - In that same shell set `DOCKS_RED_SHA256="$(sed -n 's/^- Docks TDD-red receipt SHA-256: //p' docs/plans/active/session-relay-prebuilt-cli-distribution.md)"` and `PUBLIC_RED_SHA256="$(sed -n 's/^- Companion TDD-red receipt SHA-256: //p' docs/plans/active/session-relay-prebuilt-cli-distribution.md)"`, require each to match exactly 64 lowercase hex, then run `node scripts/release.mjs --materialize-tdd-red --plugin session-relay 0.12.0 --plan docs/plans/active/session-relay-prebuilt-cli-distribution.md --docks-red-out "$REQUEST_DIR/docks-red.json" --public-red-out "$REQUEST_DIR/public-red.json"`. This mode reads only the two fixed Notes byte/hash pairs, rejects `pending`, requires each embedded byte sequence to be one canonical closed-schema `TddRedReceiptV1` with the expected repository ID, verifies its adjacent hash, and atomically creates byte-identical mode-`0600` files through exclusive mode-`0600` temporary siblings. It refuses existing outputs and prints only the two verified hashes. The Step 8 driver re-hashes both files and requires equality with `DOCKS_RED_SHA256` and `PUBLIC_RED_SHA256` before any preflight action.
  - Commit the exact release-owned source as `SOURCE_COMMIT`, then push only `SOURCE_COMMIT:refs/heads/preflight/session-relay-0.12.0-<SOURCE_COMMIT first 12 hex>` without force; do not advance `main` or create a release tag.
  - `.github/workflows/build-binaries.yml` has one closed `workflow_dispatch` input object: required `mode` choice with values `validate-only|publish-existing-tag`, required `expected_commit` matching 40 lowercase hex, and optional `expected_tag` with explicit default `""`. Validation requires the empty default; manual publication requires exact `session-relay--v<manifest-version>`. Tag-push publication does not consume manual inputs.
  - Snapshot the set of matching workflow run database IDs with `gh run list --workflow build-binaries.yml --branch "preflight/session-relay-0.12.0-${SOURCE_COMMIT:0:12}" --event workflow_dispatch --json databaseId`, run `gh workflow run build-binaries.yml --ref "preflight/session-relay-0.12.0-${SOURCE_COMMIT:0:12}" -f mode=validate-only -f expected_commit="$SOURCE_COMMIT"`, then poll until exactly one database ID is new relative to the snapshot. Query that run through `gh api`, require its workflow file/SHA, event, head branch/SHA, and complete input object to equal the requested validation identity, and stop on zero or multiple new IDs.
  - Run `gh run watch "$RUN_ID" --exit-status`, then `gh run download "$RUN_ID" --dir "$REQUEST_DIR/artifacts"`. Run `node scripts/verify-session-relay-preflight.mjs --run-id "$RUN_ID" --expected-commit "$SOURCE_COMMIT" --artifacts "$REQUEST_DIR/artifacts" --receipt-out "$REQUEST_DIR/preflight.json"`.
  - The build matrix uses native runners for every target: `ubuntu-24.04` for Linux x86_64, `ubuntu-24.04-arm` for Linux aarch64, `macos-15-intel` for Darwin x86_64, and `macos-15` for Darwin aarch64. Each native job runs its produced binary with `--version` before upload and emits a same-run attestation binding target, asset name, SHA-256, runner OS/architecture, and exact stdout `session-relay 0.12.0`.
  - The verifier reads the run and artifact database identities through `gh`, requires validation-only mode and the expected commit, verifies exactly four same-run binaries, four attestations, and `SHA256SUMS`, validates target file format/architecture, stable names, attested native version output, and every digest after staged mode `0755`, then emits `ProducerPreflightReceiptV1`.
  - Snapshot the matching `ci.yml` workflow-dispatch database IDs, dispatch `ci.yml` against the exact preflight ref, select exactly one new database ID relative to that snapshot, and query it through `gh api` to require the expected workflow file/SHA, event, head branch/SHA, and empty input object before `gh run watch "$SOURCE_CI_RUN_ID" --exit-status`.
  - Run `node scripts/release.mjs --verify-source-ci --plugin session-relay 0.12.0 --run-id "$SOURCE_CI_RUN_ID" --expected-commit "$SOURCE_COMMIT" --receipt-out "$REQUEST_DIR/source-ci.json"`. It verifies the authoritative GitHub run used Node 24, frozen pnpm install, pinned Claude materialization/PATH, Rust `1.85.0`, musl tools, and exact `node scripts/ci.mjs`, then emits `SourceCiReceiptV1`.
  - Set `PREFLIGHT_SHA256="$(sha256sum "$REQUEST_DIR/preflight.json" | cut -d" " -f1)"` and `SOURCE_CI_SHA256="$(sha256sum "$REQUEST_DIR/source-ci.json" | cut -d" " -f1)"`, require each to match exactly 64 lowercase hex, then run `node scripts/release.mjs --check-prepared --plugin session-relay 0.12.0 --source-commit "$SOURCE_COMMIT" --docks-red "$REQUEST_DIR/docks-red.json" --docks-red-sha256 "$DOCKS_RED_SHA256" --public-red "$REQUEST_DIR/public-red.json" --public-red-sha256 "$PUBLIC_RED_SHA256" --preflight "$REQUEST_DIR/preflight.json" --preflight-sha256 "$PREFLIGHT_SHA256" --source-ci "$REQUEST_DIR/source-ci.json" --source-ci-sha256 "$SOURCE_CI_SHA256" --receipt-out "$REQUEST_DIR/candidate.json"`. Plan-manager embeds the exact canonical preflight, source-CI, and candidate bytes plus hashes under Notes, runs exact `node scripts/ci.mjs` on that uncommitted plan-only tree, records the green result, and only then commits this plan as `EVIDENCE_COMMIT`. The red Notes fields remain the original pre-production captures and are never replaced from the Step 8 materializations.
  - Require `git diff --exit-code "$SOURCE_COMMIT..$EVIDENCE_COMMIT" -- . ':(exclude)docs/plans/active/session-relay-prebuilt-cli-distribution.md'`; completion review targets `EVIDENCE_COMMIT` while all release-owned tree evidence remains bound to `SOURCE_COMMIT`. Temporary evidence ownership remains with plan-manager and is identity-checked and removed only after the canonical bytes are embedded.
- Companion repository commands and prerequisites live in its own plan; the Docks completion receipt consumes only the companion plan's pinned clean commit and the distribution-contract report, not unbound conversational claims.
- Release sequencing is out of this plan's execution scope. The required follow-up plan must preserve public Session Relay prerelease assets with a non-install staging notice → reviewed public digest pins → live docks-kit → serialized Docks promotion and live smoke → stable Session Relay finalization with `docks-kit sync` instructions, and may start only from this plan's reusable passed completion receipt.

## Interfaces & data shapes

### Published release assets

For tag `session-relay--v<VERSION>`, the GitHub Release must contain exactly one executable for every supported target plus one checksum manifest:

```text
session-relay-x86_64-unknown-linux-musl
session-relay-aarch64-unknown-linux-musl
session-relay-x86_64-apple-darwin
session-relay-aarch64-apple-darwin
SHA256SUMS
```

`SHA256SUMS` uses the standard `<64 lowercase hex><two spaces><asset-name>` format and covers all four executables. Asset names are stable and are the contract consumed by `docks-kit`.

### Installed executable

`docks-kit` pins Session Relay `0.12.0` plus one reviewed SHA-256 per supported target in `SoT/toolchain.json` and installs one verified host asset as:

```text
~/.local/bin/session-relay
```

The Rust CLI must support `session-relay --version` and print `session-relay <semver>`. The Cargo package version stays synchronized with the Session Relay plugin manifest version during `scripts/release.mjs`.

The manifest entry is closed data: repository `DocksDocks/docks`, tag `session-relay--v0.12.0`, exact version `0.12.0`, install path `~/.local/bin/session-relay`, and an `assets` map whose four target keys each hold exactly 64 lowercase hex characters. Every ordinary `docks-kit sync` that targets Claude or Codex must ensure the pinned Session Relay CLI before installing, refreshing, or enabling the Session Relay plugin. The ensure path maps the host OS and architecture to one target, downloads the matching executable and `SHA256SUMS`, requires the manifest entry and downloaded bytes both to equal the repository-pinned digest, writes a sibling staged file without replacing the existing command, applies mode `0755`, verifies the exact version from that staged path, and only then atomically renames it into place. An unsupported platform, absent pin, failed download, checksum mismatch, chmod failure, version mismatch, or replacement failure aborts that sync path clearly, removes only its owned stage file, and preserves any existing executable byte-for-byte.

### Plugin launcher resolution

`plugins/session-relay/bin/relay` resolves in this order:

1. Non-empty `SESSION_RELAY_BIN`.
2. `session-relay` found on `PATH`.
3. `$HOME/.local/bin/session-relay`.
4. Exit nonzero with a clear `docks-kit sync` / `docks-kit toolchain ensure session-relay` installation message.

The launcher rejects a resolver target that is itself, preventing recursion. The reviewed and tagged `0.12.0` payload never downloads, compiles, or falls back to an embedded executable. Remote `main` remains on the distinct compatible `0.11.2` payload until `cli-v0.9.0` is live.

### Release orchestration

The binary builder is reusable by tag CI and manual dispatch. On `session-relay--v*`, the validation job must pass before publication. The publisher downloads only artifacts produced by that same workflow run, validates the tag against the manifests and Cargo version, generates `SHA256SUMS`, and creates a public prerelease with the five assets and a staging-only body that explicitly says not to install or advertise `docks-kit sync` yet. The publishing job alone receives `contents: write`; the workflow-level default remains `contents: read`. GitHub's `push` contract binds `GITHUB_SHA` to the pushed tag tip, and a same-repository `./.github/workflows/...` reusable workflow resolves from the same caller commit, so the release producer does not require that commit on the default branch.

The final stable Session Relay GitHub Release install block is exactly `docks-kit sync`, because that command ensures the verified CLI before installing or enabling the plugin for either supported agent runtime. It must not retain the current plugin-only `/plugin install session-relay@docks` instructions. The prerelease body is a separate fixed staging notice with no install command. `scripts/lib/plugins.mjs` owns both descriptors, and the distribution-contract test proves prerelease staging cannot advertise installation while finalization cannot publish plugin-first instructions after embedded binaries are removed.

`scripts/release.mjs --prepare --plugin session-relay 0.12.0` applies every manifest, marketplace, Cargo, and lockfile version change, runs the full CI gate on that exact changed tree, and commits locally without pushing or tagging. A failed post-bump gate prevents the commit. Completion review seals that final launcher-only source. The identity names are fixed: `TAG_COMMIT = SOURCE_COMMIT`, and `PROMOTED_COMMIT = SHIPPED_COMMIT`, where `SHIPPED_COMMIT` is the plan-manager commit that archives this plan with a passed reusable completion receipt. `SOURCE_COMMIT` must be an ancestor of `SHIPPED_COMMIT`, and every path outside this one plan's active/finished lifecycle locations must have the same Git blob or tree identity in both commits. Release-only modes reject an unfinished source-preparation plan. `--publish-reviewed` tags exactly `TAG_COMMIT` and creates only the staged public prerelease. `--promote-reviewed` pushes exactly `PROMOTED_COMMIT`, requires live `cli-v0.9.0`, proves both release identities, creates the durable promotion transaction before its first release mutation, acquires the promotion lock, verifies the expected `origin/main`, downloads and verifies the exact host docks-kit release executable, performs an exact-source pre-push smoke at `TAG_COMMIT`, pushes the reviewed Docks lineage through `PROMOTED_COMMIT`, performs the live smoke, and emits a canonical success receipt. `--resume-promotion` reconciles an interrupted transaction from its permanent remote journal even when no terminal receipt was written. Only `--finalize-reviewed` consumes a successful promotion receipt and changes the exact prerelease to stable plus the final `docks-kit sync` body.

Promotion is one idempotent serialized transaction. Before the lock or any `main` mutation, it creates the permanent append-only remote ref `refs/heads/transactions/session-relay-0.12.0`; every `PromotionJournalEntryV1` commit has exactly one first parent and key `K = (attempt, sequence)`, where `attempt` is a nonnegative integer starting at 0 and `sequence` starts at 0 for each attempt. Keys increase lexicographically with no gap, reuse, or regression. Every entry repeats the immutable source-proof/`TAG_COMMIT`/`PROMOTED_COMMIT`/publication/docks-kit/expected-main identities, prior-entry commit, transaction ref, and promotion-lock identity. Updates use a compare-and-swap lease and never rewrite or delete history. Attempt 0 atomically creates the immutable tombstone `refs/heads/locks/session-relay-0.12.0` at a nonce commit; the lock is never deleted or reused, and every retry reuses that exact transaction and lock. An existing mismatched lock or pre-push drift stops before mutation. The exact-source pre-push smoke runs the released docks-kit executable against a detached reviewed Docks checkout supplied through a release-test-only explicit source argument, proving the launcher-only plugin before `main` changes.

The phase vocabulary is closed to `initialized|locked|prepush_passed|main_pushed|live_passed|restore_pushed|reapply_pushed|terminal_success|terminal_failure|manual_incident`. Attempt 0 has only these legal chains: `initialized -> locked -> prepush_passed -> main_pushed -> live_passed -> terminal_success`; `initialized -> locked -> prepush_passed -> main_pushed -> restore_pushed -> terminal_failure`; or `terminal_failure` from a definitively failed nonterminal phase. When authoritative refs cannot prove one unique safe next phase, the only transition is `manual_incident`. A receipt-authorized retry attempt has only these legal chains: `initialized -> locked -> reapply_pushed -> terminal_success`, where `terminal_success` carries the required post-reapply live-smoke result; `initialized -> locked -> reapply_pushed -> restore_pushed -> terminal_failure`; or `terminal_failure` from a definitively failed nonterminal retry phase. Its initialization records the immediately prior attempt's sealed restored-failure receipt SHA-256 and verified restore commit/head/blob set. `terminal_success`, `terminal_failure`, and `manual_incident` have no outgoing transition within their attempt. No phase may be skipped or repeated, and no retry may execute pre-push publication, tag, Release, npm, or initial-main-push mutations.

Every invocation reconstructs and validates the complete first-parent chain and then reconciles it with authoritative remote refs, GitHub Release identity, exact `origin/main`, closed-set blob IDs, and any restore/reapply commits. A crash never invents a phase: for a nonterminal attempt reconciliation appends the single legal next entry implied by authoritative state or appends `manual_incident` and stops. In attempt 0, pre-push smoke success is journaled before pushing `main`; after a crash, observed `main == PROMOTED_COMMIT` derives `main_pushed`, observed `main == expected-origin-main` derives `prepush_passed`, and any other head is a manual incident. The existing `--resume-promotion` invocation has exactly two disjoint behaviors. For a nonterminal attempt it requires the exact transaction ref plus the same immutable identities and receipt hashes, performs reconciliation, and continues only the unique legal successor. For a terminal tip it performs terminal-receipt recovery only: it requires the exact terminal key and journal commit at the authoritative transaction-ref tip, recomputes the complete chain digest, verifies every immutable identity, promotion-lock identity, authoritative ref and Release identity, exact expected/observed `origin/main`, and the complete before/promoted/restored/reapplied closed-set blob map. It maps `terminal_success` to `success`, `manual_incident` to `manual_incident`, a `terminal_failure` with a verified authoritative restore commit and restored closed-set map to `restored_failure`, and any other valid terminal failure to non-retryable `failure`. Every receipt field, including its creation time, is copied or deterministically derived from the terminal journal payload and validated authoritative state, so uninterrupted emission and recovery produce byte-identical RFC 8785 JCS. Terminal recovery never appends a journal entry, changes phase, pushes, restores, reapplies, runs a smoke, or repeats any mutation. If the filesystem path named by `--receipt-out` is absent, recovery atomically creates the mode-`0600` receipt through an exclusive mode-`0600` temporary sibling; if that path already contains the exact expected bytes at mode `0600`, recovery succeeds idempotently; any other existing bytes, type, mode, hash, terminal outcome, or identity fail closed without mutation.

The compatibility restore/reapply set is closed to the entire tracked `plugins/session-relay/**` tree plus the whole `.claude-plugin/marketplace.json` blob. `PromotionReceiptV1` records before, promoted, restored, and reapplied Git blob IDs for every member. Shared `.github/**` and `scripts/**` changes deliberately remain because their contract tests prove compatibility with the restored `0.11.2` payload and unchanged docks/effect-kit release semantics. A definitive post-push failure first creates a normal fast-forward restore commit atop the locked `main`, restoring every closed-set member from `expected-origin-main` after proving each still equals its promoted blob, and the restore must pass manifest/catalog parity and exact full CI before push. Restore failure appends `terminal_failure` with the attempted restore identity and no reapply; an unverifiable restore, unexpected main movement, or concurrent closed-set change appends `manual_incident`, keeps the prerelease staged, and stops finalization.

Receipt-based retry exists only as `--retry-failed <receipt-path> --retry-failed-sha256 <sha256>` against the latest journal tip. It accepts only a canonical retryable `restored_failure` receipt—whether emitted uninterrupted or byte-identically recovered by `--resume-promotion`—whose `attempt` equals the terminal attempt, whose terminal journal commit/key and chain digest equal the permanent transaction tip, whose prior-receipt binding and immutable identities match, whose exact restore commit is still remote `main`, and whose closed-set blobs remain unchanged. Of the recoverable outcomes, only that retryable `restored_failure` authorizes one next retry attempt; recovered `success`, `failure`, `manual_incident`, and non-retryable restored failures never do. It rejects non-retryable or older receipts, a nonterminal/forked/regressed journal, mismatched attempts or keys, missing/extra commits, a restore-head mismatch, and every intervening closed-set change. Acceptance increments `attempt` exactly once, appends the next `initialized` entry with the recovered receipt SHA-256 as `prior_attempt_receipt_sha256` under compare-and-swap, and then follows the closed retry chain. Reusing that receipt after the next attempt exists cannot append another `initialized` entry or repeat reapply; it resolves only the already-bound attempt or fails closed. A definitive failure after that reapply is terminal and non-retryable for `0.12.0`; only a separately reviewed `0.12.1` transaction may supersede it. Force-push, journal or transaction-ref deletion, lock replacement, and history rewriting are forbidden.

Publication is independently resumable. `--publish-reviewed` reconciles these states against the exact reviewed commit and tag:

| State | Required behavior |
|---|---|
| Tag absent | Create and push the immutable tag, then wait for its bound tag-push publication run. |
| Matching tag, no bound run, no Release | Dispatch `publish-existing-tag` against the exact tag/ref and commit; any mismatch stops. |
| Matching tag and bound run, no Release | Resume that run or create the prerelease from artifacts produced by that exact run. |
| Matching prerelease, partial assets | Verify every existing asset's name and digest, upload only missing assets from the bound run, then verify the closed five-asset set and staging body. |
| Matching complete prerelease | Return success without replacing assets. |
| Matching stable Release | `--publish-reviewed` returns a typed premature-stable conflict. Stable idempotence belongs only to `--finalize-reviewed` with the bound successful promotion and final-publication receipts. |
| Tag, run, Release ID, target commit, asset, or digest conflict | Stop with a typed conflict result; never clobber, retag, or substitute another run. |

`--finalize-reviewed` treats a matching complete prerelease plus a successful promotion receipt as actionable and an already stable matching Release/body as success; absent/partial/conflicting state or a failure receipt stops. Every transition binds repository ID, tag object/commit, workflow file/SHA/run ID/attempt, Release database ID, prerelease state/body hash, and asset database IDs/digests. Contract tests simulate interruption at every row and prove convergence or fail-closed conflict behavior.

### TDD-red capture (before production edits)

Step 1 adds `scripts/capture-tdd-red.mjs` as repository-agnostic test infrastructure before either repository freezes or runs its red tests. Its only CLI is `node /home/vagrant/projects/docks/scripts/capture-tdd-red.mjs --repo <absolute-canonical-repository-root> --repository-id <owner/name> --pre-production-commit <40-lowercase-hex> --test <repo-relative-path>... --receipt-out <absolute-new-path> -- <executable> <arg>...`. Options are closed and unique except repeatable `--test`; `--` is required and terminates helper options. The helper passes every following token verbatim to `spawnSync(executable, args, { cwd: repo, shell: false, stdio: ["ignore", "pipe", "pipe"] })`; it never joins, interpolates, evaluates, or invokes a shell. It rejects an unknown, duplicate, or missing option, duplicate/noncanonical test path, noncanonical repository root, untracked or non-regular test, invalid commit or repository identity, existing output, empty command argv, signal termination, or zero/non-numeric exit.

Each red capture has its own ephemeral directory and finishes embedding plus cleanup before its repository's first production edit. It is never the later `REQUEST_DIR`. For Docks, the plan-manager retains the canonical path and device/inode in operation state rather than relying on exported variables or a cross-shell trap. In a fresh shell, run:

```bash
DOCKS_RED_DIR="$(mktemp -d /tmp/session-relay-docks-red.XXXXXXXX)"
DOCKS_RED_DIR="$(realpath -e -- "$DOCKS_RED_DIR")"
case "$DOCKS_RED_DIR" in /tmp/session-relay-docks-red.*) ;; *) exit 1 ;; esac
chmod 0700 "$DOCKS_RED_DIR"
DOCKS_RED_DIR_ID="$(stat -Lc '%d:%i' -- "$DOCKS_RED_DIR")"
DOCKS_RED_PATH="$DOCKS_RED_DIR/docks-red.json"
```

The plan-manager records the resulting literal `DOCKS_RED_DIR`, `DOCKS_RED_DIR_ID`, and `DOCKS_RED_PATH` in its operation state. The capture shell need not survive the next plan edit.
From `/home/vagrant/projects/docks`, after the helper and both complete Docks tests are committed but before any production edit, the one canonical Docks red run is exactly:

```bash
node scripts/capture-tdd-red.mjs --repo "$PWD" --repository-id DocksDocks/docks --pre-production-commit "$(git rev-parse HEAD)" --test plugins/session-relay/test/distribution-contract.mjs --test plugins/session-relay/test/selftest.mjs --receipt-out "$DOCKS_RED_PATH" -- node plugins/session-relay/test/distribution-contract.mjs
```

That command must exit nonzero after atomically creating one mode-`0600` receipt. Before any Docks production edit, plan-manager reads the exact file bytes without JSON parse/stringify, verifies canonical `TddRedReceiptV1`, computes SHA-256, replaces the fixed Docks Notes byte/hash fields, and re-reads the plan to require that the bytes after the fixed JCS prefix through but excluding LF are exactly the file bytes and that the adjacent hash matches. It then independently resolves the recorded literal directory, requires the path still matches `/tmp/session-relay-docks-red.*`, requires its current device/inode equals the recorded `DOCKS_RED_DIR_ID`, requires `DOCKS_RED_PATH` is its direct child, and removes only that directory. The companion plan follows the same per-capture sequence under `/tmp/session-relay-public-red.*` and embeds its receipt in its own fixed Notes fields before public production edits; after collection, Docks copies those exact embedded bytes as specified above. Each repository invokes the capture helper exactly once. No receipt is regenerated or recaptured after production edits; Step 8 only byte-for-byte materializes the already embedded bytes.

`TddRedReceiptV1` is closed to exactly `{schema:1,type:"TddRedReceiptV1",repository_id,pre_production_commit,test_paths:[{path,blob_id}],command:{cwd,argv:[string,...]},exit_code,stdout_sha256,stderr_sha256,captured_at,producer:{path:"scripts/capture-tdd-red.mjs",blob_id,version:"1"}}`. The helper canonicalizes `repo` into the recorded `command.cwd`, preserves argv tokens exactly, sorts test paths lexicographically, resolves and verifies every test Git blob ID at `pre_production_commit`, hashes the exact stdout/stderr bytes, records UTC RFC 3339 time, and records its own Docks Git blob plus fixed producer version. It serializes UTF-8 RFC 8785 JCS, hashes those exact bytes, and atomically creates the receipt mode `0600` through an exclusive-created temporary sibling, write and file fsync, rename, and parent-directory fsync; on success it prints only the lowercase receipt SHA-256. Later validation requires the pre-production commit to be an ancestor of the implementation commit and every recorded test and producer blob identity to remain exact.

### Release evidence schemas

Release evidence uses RFC 8785 JSON Canonicalization Scheme UTF-8 bytes with SHA-256 over the exact canonical bytes. Every schema is closed, versioned with `schema: 1` plus an exact `type`, rejects unknown/missing fields, and is written only to an explicit `--receipt-out` path. Writers create a mode-`0600` temporary sibling with exclusive create, write and fsync the canonical bytes, atomically rename, fsync the parent directory, then print the lowercase receipt SHA-256. Validators require the caller-provided path plus SHA-256 and never search ambient directories.

- `TddRedReceiptV1`: repository ID, repository commit before production edits, test paths/blob IDs, exact command/working-directory identity, required nonzero exit, stdout/stderr SHA-256, capture timestamp, and producer version. Docks and public each emit one canonical receipt immediately after freezing their tests; the exact bytes and hashes are embedded under Notes, later validation requires the recorded pre-production commit to be an ancestor of the implementation commit, and every recorded test path must have exactly the recorded blob ID there.
The fixed Notes encoding is part of the interface: each `... receipt JCS bytes: ` prefix is followed on the same line by exactly one RFC 8785 JSON object, and the embedded receipt byte sequence is the UTF-8 bytes after that prefix through but excluding the line-feed delimiter. Its adjacent `... receipt SHA-256: ` field is exactly the lowercase SHA-256 of that byte sequence. `pending`, duplicate fields, carriage returns, noncanonical JSON, a repository-ID mismatch, or any extra byte are invalid.
- `ProducerPreflightReceiptV1`: repository ID, validation-only branch/ref commit, workflow file/SHA/run database ID/attempt/event/input object/conclusion, four artifact database IDs/names/digests/file identities, four native-attestation database IDs with runner OS/architecture and exact version outputs, checksum artifact/database ID/manifest digest, verifier version, and creation timestamp.
- `SourceCiReceiptV1`: repository ID, `SOURCE_COMMIT`, workflow file/SHA/run database ID/attempt/event/conclusion, Node/pnpm/Claude/Rust/musl bootstrap identities, frozen-lockfile identity, exact command `node scripts/ci.mjs`, job/log SHA-256, and start/end timestamps.
- `SourcePreparationCandidateV1`: repository ID, plan path/blob SHA-256 before evidence embedding, execution-base commit, `SOURCE_COMMIT`, Docks red-receipt SHA-256 and test blob identities, companion repository ID/immutable validation ref/commit/plan path/input SHA-256/execution-base/review-receipt SHA-256/red-receipt SHA-256/status/blocked reason, preflight-receipt and source-CI-receipt SHA-256 plus every producer identity, internally rerun A1–A6 stdout/stderr/exit hashes, and creation timestamp. `--check-prepared` consumes the two explicit red receipts plus preflight and source-CI receipts, freshly fetches the public validation ref and requires its commit, reruns A1–A6 itself, verifies both frozen test identities and the complete release-owned tree at `SOURCE_COMMIT`, and emits the candidate before completion review.
- `SourcePreparationProofV1`: every candidate field plus candidate SHA-256, final plan path/blob SHA-256, exact reviewed plan-only evidence commit, `TAG_COMMIT` equal to unchanged `SOURCE_COMMIT`, `PROMOTED_COMMIT` equal to `SHIPPED_COMMIT`, their required ancestry and non-plan tree-equivalence proof, embedded reusable completion-review receipt SHA-256 and `review_status: passed`, and binding timestamp. The later release plan begins with `--bind-completion --finished-plan <path> --embedded-candidate-sha256 <sha256> --receipt-out <path>`; it emits the proof only when the finished plan and receipt bind the evidence commit, the complete release-owned tree still equals `SOURCE_COMMIT`, and the promoted descendant differs only at this plan's lifecycle path.
- `SessionRelayPublicationReceiptV1`: source-proof SHA-256, repository/tag/`TAG_COMMIT` (exactly `SOURCE_COMMIT`), workflow file/SHA/run ID/attempt, Release database ID and `prerelease|stable` state, body SHA-256, five closed asset records with database ID/name/size/digest, transition performed, and timestamp. Publish/finalize modes consume the prior receipt when resuming and emit the reconciled state.
- `PromotionJournalEntryV1`: permanent transaction ref; exact `attempt`, `sequence`, and prior-entry commit (null only at `(0,0)`); immutable source-proof/`TAG_COMMIT`/`PROMOTED_COMMIT`/publication/docks-kit/expected-main identities; immutable promotion-lock ref/nonce; closed phase; prior-attempt receipt SHA-256 (null only on attempt 0); observed remote main and Release state; relevant push/restore/reapply commits and closed-set blob IDs; smoke descriptor/result hashes when available; and timestamp. The current entry commit is derived from the Git object containing the canonical payload and is never included in that payload. Validators reconstruct the full first-parent chain, require gap-free lexicographic keys and one of the exact attempt-0 or retry chains, and reject any outgoing transition from a terminal entry.
- `PromotionReceiptV1`: source-proof and publication-receipt SHA-256, exact `TAG_COMMIT` and `PROMOTED_COMMIT` plus ancestry/non-plan equivalence proof, transaction ref, exact `attempt`, terminal key and terminal journal commit, journal-chain digest, prior-attempt receipt SHA-256 (null only on attempt 0), promotion-lock ref/nonce, expected and observed `origin/main`, Docks pre-push/pushed/restore/reapply commits, closed compatibility-set paths and before/promoted/restored/reapplied Git blob IDs, public reviewed/tag commits and Release ID, docks-kit and Session Relay asset identities, exact-source/live isolation-root descriptors and sync argv, stdout/stderr/ordering-log hashes, installed binary and launcher identities/version outputs, post-restore parity/full-CI evidence, outcome `success|restored_failure|failure|manual_incident`, retryability, and terminal-entry-derived creation time. The terminal journal entry carries the complete receipt projection needed to reproduce those fields; only terminal commit identity and chain digest are derived from the validated chain. Recovery status is not encoded in the receipt. The receipt is atomically written after the terminal journal entry, and `--resume-promotion` reconstructs these same unique canonical bytes when the write was interrupted.

### Release-mode CLI grammar

Unknown, duplicate, missing, or cross-mode arguments fail before mutation. Receipt inputs always require adjacent path and digest arguments. Receipt outputs refuse an existing path except where a mode already defines identity-matching idempotence; specifically, terminal `--resume-promotion` accepts its existing `--receipt-out` only when it is mode `0600` and byte-identical to the uniquely reconstructed `PromotionReceiptV1`, and every conflict fails closed.

```text
release.mjs --prepare --plugin session-relay 0.12.0 [--dry-run]
release.mjs --check-prepared --plugin session-relay 0.12.0
  --source-commit <40-hex-sha>
  --docks-red <docks-red.json> --docks-red-sha256 <sha256>
  --public-red <public-red.json> --public-red-sha256 <sha256>
  --preflight <preflight.json> --preflight-sha256 <sha256>
  --source-ci <source-ci.json> --source-ci-sha256 <sha256>
  --receipt-out <candidate.json>
release.mjs --materialize-tdd-red --plugin session-relay 0.12.0
  --plan docs/plans/active/session-relay-prebuilt-cli-distribution.md
  --docks-red-out <absolute-new-docks-red.json>
  --public-red-out <absolute-new-public-red.json>
release.mjs --verify-embedded-preparation --plugin session-relay 0.12.0
  --plan docs/plans/active/session-relay-prebuilt-cli-distribution.md
release.mjs --verify-source-ci --plugin session-relay 0.12.0
  --run-id <github-run-id> --expected-commit <40-hex-sha>
  --receipt-out <source-ci.json>
release.mjs --bind-completion --plugin session-relay 0.12.0
  --finished-plan <finished-plan.md>
  --embedded-candidate-sha256 <sha256>
  --receipt-out <source-proof.json>
release.mjs --publish-reviewed --plugin session-relay 0.12.0
  --source-proof <source-proof.json> --source-proof-sha256 <sha256>
  --receipt-out <publication.json>
  [--resume-publication <publication.json> --resume-publication-sha256 <sha256>]
release.mjs --promote-reviewed --plugin session-relay 0.12.0
  --source-proof <source-proof.json> --source-proof-sha256 <sha256>
  --publication <publication.json> --publication-sha256 <sha256>
  --docks-kit-release cli-v0.9.0
  --expected-origin-main <40-hex-sha>
  --receipt-out <promotion.json>
  [--retry-failed <promotion.json> --retry-failed-sha256 <sha256>]
release.mjs --resume-promotion --plugin session-relay 0.12.0
  --transaction-ref refs/heads/transactions/session-relay-0.12.0
  --source-proof <source-proof.json> --source-proof-sha256 <sha256>
  --publication <publication.json> --publication-sha256 <sha256>
  --docks-kit-release cli-v0.9.0
  --expected-origin-main <40-hex-sha>
  --receipt-out <promotion.json>
release.mjs --finalize-reviewed --plugin session-relay 0.12.0
  --source-proof <source-proof.json> --source-proof-sha256 <sha256>
  --publication <publication.json> --publication-sha256 <sha256>
  --promotion <promotion.json> --promotion-sha256 <sha256>
  --receipt-out <final-publication.json>
  [--resume-finalization <final-publication.json> --resume-finalization-sha256 <sha256>]

# Preserved shared interface for every non-Session-Relay plugin:
release.mjs [--dry-run] [--plugin docks|effect-kit] patch|minor|major|X.Y.Z
```

The preserved docks/effect-kit interface retains current bump resolution, manifest/catalog mutation, exact-tree full CI before commit, commit message, branch push, immutable plugin tag, tag-CI wait, GitHub Release notes/assets, and dry-run byte-for-byte non-mutation. Session Relay positional bump syntax rejects with the new prepare command. `plugins/session-relay/test/distribution-contract.mjs --release-fixtures <dir>` executes fixture-backed fakes for every publication-table row plus every promotion kill point, including tag-present/no-run recovery, prerelease finalization, promotion success, expected-main drift, lock contention, pre-push failure, transaction creation, gap-free attempt/sequence ordering, every closed attempt-0 and retry chain, terminal immutability, crash-before-checkpoint reconciliation, closed-set restore, manifest-parity/full-CI restore gate, latest restored-failure receipt-only retry/reapply, manual incident, supersession, unknown/duplicate arguments, stale or older receipt rejection, conflicting identities, and unchanged docks/effect-kit positional release flows and tag names without network or repository mutation. For each journal terminal outcome—`terminal_success`, `terminal_failure`, and `manual_incident`—a kill immediately after the terminal journal compare-and-swap and before receipt rename must leave the exact journal, refs, Release identity, and closed-set state; terminal-failure fixtures cover both verified-restored `restored_failure` and unrestored non-retryable `failure`. `--resume-promotion` must then create a receipt byte-identical to the corresponding uninterrupted fixture, and a second resume must return the same receipt without journal append, ref update, smoke, restore, reapply, or other mutation. Separate fixtures reject conflicting existing receipt bytes, mode, terminal tip/chain, identities, refs, Release state, and closed-set blobs. The recovered retryable `restored_failure` fixture must feed `--retry-failed` and prove exactly one next `initialized` attempt plus successful reapply chain; repeating recovery or retry must not create another attempt or repeat a mutation. Recovered `success`, `failure`, `manual_incident`, and a second-attempt non-retryable restored failure must reject retry, with the latter requiring a separately reviewed `0.12.1` supersession.

`--dry-run` remains strictly read-only. It computes and prints the manifest, marketplace, Cargo, and lockfile changes without writing them, skips the full CI execution while clearly reporting that the real release will gate the changed tree, and performs no commit, push, tag, or GitHub mutation. The distribution-contract test snapshots tracked status, untracked status, and every release-owned file byte before and after `node scripts/release.mjs --prepare --plugin session-relay 0.12.0 --dry-run`; a separate negative fixture proves `node scripts/release.mjs --dry-run --plugin session-relay 0.12.0` rejects without mutation.

## Steps

| # | Task | Files | Depends | Status | Done condition |
|---|---|---|---|---|---|
| 1 | Add and freeze the portable TDD-red capture helper before either repository runs red tests. | `scripts/capture-tdd-red.mjs` | — | planned | The closed shell-free CLI, exact receipt property set, test/producer blob binding, nonzero enforcement, RFC 8785 JCS serialization, mode-`0600` atomic write, and SHA-only output are fixture-tested; the committed helper blob is then read-only for both captures and all later steps. |
| 2 | Dispatch the separate `DocksDocks/public` installer implementation through the currently working embedded Session Relay binary under its own reviewed plan; collect a clean commit that implements the digest-pinned installer and fixture-driven tests, while its plan remains explicitly blocked only on the four not-yet-published production digests. | `/home/vagrant/projects/public/docs/plans/active/session-relay-cli-installation.md` (external companion plan; modified only by its plan-manager), Session Relay lifecycle/handback records (external state) | 1 | planned | The worker is launched with explicit Codex Standard tier, creates and starts its repository-local plan, freezes its plan-owned exact tests, captures one canonical `TddRedReceiptV1` in its own identity-checked ephemeral directory before public production edits, immediately embeds the exact bytes/hash under its own Notes, verifies and removes only that directory, implements the exact manifest/digest/atomic-replacement contract and Claude/Codex sync ordering, commits cleanly, pushes only the exact commit to its named immutable validation ref, records `blocked_reason` as awaiting `session-relay--v0.12.0` digests, and is collected before Docks Step 4. Docks verifies and copies the companion plan's exact embedded bytes/hash into its own Notes without rewrite before Docks production edits; neither repository recaptures. |
| 3 | Add the complete final Docks distribution-contract and self-test changes before any Docks production change, run the exact declared helper command against the embedded-binary design, and freeze both blobs. | `plugins/session-relay/test/distribution-contract.mjs`, `plugins/session-relay/test/selftest.mjs` | 1 | planned | The complete tests fail for missing external resolution, version output, release-asset publication, post-bump CI ordering, explicit fresh self-test binary, cross-repo contract receipt, or obsolete committed-binary assumptions; the exact nonzero Docks command writes one canonical `TddRedReceiptV1` to `DOCKS_RED_PATH` in a fresh identity-checked mode-`0700` directory, plan-manager immediately embeds and re-verifies the exact bytes/hash under Notes, then removes only that directory. Steps 4–8 treat both test paths and the helper blob as read-only; no later capture occurs. |
| 4 | Make the Rust CLI and plugin launcher expose the final stable external CLI contract, then delete every committed platform executable and the in-plugin checksum file. | `plugins/session-relay/rust/Cargo.toml`, `plugins/session-relay/rust/Cargo.lock`, `plugins/session-relay/rust/src/main.rs`, `plugins/session-relay/bin/relay`, `plugins/session-relay/bin/SHA256SUMS`, `plugins/session-relay/bin/relay-*` | 2, 3 | planned | `--version` reports the Cargo version; the frozen launcher tests prove external resolution order, recursion rejection, and actionable failure with no embedded fallback; the frozen self-test requires `SESSION_RELAY_TEST_BIN`; neither test blob changes; `git ls-files plugins/session-relay/bin` lists only the launcher. |
| 5 | Convert binary production to validation-only branch preflight plus tag-gated public prerelease assets with least-privilege permissions and same-run artifact provenance. | `.github/workflows/build-binaries.yml`, `.github/workflows/ci.yml`, `.github/AGENTS.md`, `plugins/session-relay/AGENTS.md` | 2, 3 | planned | Workflow contract tests prove all four stable asset names, checksum generation, target identity/version verification, validation-only branch mode, tag restriction, prerelease creation with a non-install staging body, pinned actions, and publisher-only write permission; a real four-target preflight run is recorded before completion review. |
| 6 | Update registry-driven CI and release tooling plus the preflight verifier for source-built local tests, local release preparation, closed evidence schemas and CLI grammar, resumable prerelease publication/finalization, safe release notes, a non-mutating preview, and serialized reviewed branch promotion with exact-source plus live smokes. | `scripts/ci.mjs`, `scripts/lib/plugins.mjs`, `scripts/lib/rust-bin.mjs`, `scripts/release.mjs`, `scripts/verify-session-relay-preflight.mjs`, `scripts/AGENTS.md` | 4, 5 | planned | Local CI builds/tests the explicit host binary without comparing committed executables; `--materialize-tdd-red` validates the fixed embedded Notes bytes/hashes and atomically creates only byte-identical mode-`0600` red files at new explicit paths; the verifier emits `ProducerPreflightReceiptV1`; `--verify-source-ci` binds full CI at `SOURCE_COMMIT`; `--prepare` changes all version files before exact-tree CI and commits locally only; `--check-prepared` validates both red receipts and emits `SourcePreparationCandidateV1`; later `--bind-completion` adds the reusable passed completion receipt into `SourcePreparationProofV1`; release-only modes require exact receipt hashes; publish rejects premature stable state while finalize alone permits bound idempotence; promotion creates the attempt-indexed append-only transaction before its immutable lock, invokes the checksum-verified released docks-kit executable against exact-source and live plugin paths, and emits an attempt/terminal-bound `PromotionReceiptV1`; nonterminal resume reconciles one legal successor, while terminal resume validates the exact tip/chain/identities/refs/closed set and atomically recovers the unique missing receipt without journal or state mutation; byte-identical existing recovery is idempotent and conflicts fail closed; of the three recovered outcomes only the latest restored failure may start exactly the next attempt and reapply without republishing immutable artifacts; docks/effect-kit retain their exact positional semantics; the final install descriptor is exactly `docks-kit sync`; and dry-run snapshots prove no mutation. |
| 7 | Update Session Relay usage guidance for the external CLI and refresh its metadata/content hash through the prescribed maintenance flow. | `plugins/session-relay/skills/productivity/session-relay/SKILL.md` | 4, 6 | planned | Skill docs name `session-relay` as the installed executable, keep instructions and troubleshooting with the skill, put executable helpers in Rust subcommands rather than skill scripts, and content-hash validation is idempotent. |
| 8 | Prepare and seal the exact release-ready source without publication: run the Session Relay `0.12.0` prepare mode, bind the immutable companion implementation ref/commit and both red receipts, commit `SOURCE_COMMIT`, execute the validation-only producer preflight, embed its canonical receipts, run full CI on that uncommitted plan-only tree, create `EVIDENCE_COMMIT`, and leave both repositories clean for completion review. | `.claude-plugin/marketplace.json`, `plugins/session-relay/.claude-plugin/plugin.json`, `plugins/session-relay/.codex-plugin/plugin.json`, `plugins/session-relay/rust/Cargo.toml`, `plugins/session-relay/rust/Cargo.lock`; frozen Docks tests/helper and public immutable validation ref/commit (read-only) | 1–7 | planned | Step 8 creates a fresh identity-checked mode-`0700` `REQUEST_DIR` and scoped preflight trap, rematerializes both original Notes-embedded red receipts byte-for-byte at mode `0600`, verifies their hashes and closed schemas, and never recaptures them. Every Docks version surface is `0.12.0`; the final source is launcher-only; both frozen test blobs and the producer helper blob still match their red receipts; `--check-prepared` passes; a fresh remote fetch proves the public validation ref and clean detached commit plus frozen tests; preflight head equals `SOURCE_COMMIT`; exact `node scripts/ci.mjs` passes after receipt embedding and before `EVIDENCE_COMMIT`; the diff from source to evidence commit is plan-only; canonical red/preflight/source-CI/candidate bytes and hashes are embedded under Notes; only the named immutable Docks and public validation refs were pushed, with no production branch, tag, Release, or npm publication; all steps are ready for plan-manager completion review. |

## Acceptance criteria

| ID | Command | Expected |
|---|---|---|
| A1 | `node plugins/session-relay/test/distribution-contract.mjs --public-remote https://github.com/DocksDocks/public.git --public-ref "$(sed -n 's/^- Companion validation ref: //p' docs/plans/active/session-relay-prebuilt-cli-distribution.md)" --public-commit "$(sed -n 's/^- Companion implementation commit: //p' docs/plans/active/session-relay-prebuilt-cli-distribution.md)" --detached-clone` | Exit 0; the test owns and identity-checks cleanup, freshly fetches only the recorded immutable validation ref, proves its clean detached commit and repository identity, and verifies the installer plus companion plan input, execution-base, review-receipt, red-receipt, blocked status, and exact production-digest blocked reason. |
| A2 | `cargo build --manifest-path plugins/session-relay/rust/Cargo.toml --release --locked && SESSION_RELAY_TEST_BIN="$PWD/plugins/session-relay/rust/target/release/relay" node plugins/session-relay/test/selftest.mjs` | Exit 0; black-box Relay behavior passes using only the explicitly supplied fresh release executable. |
| A3 | `git ls-files plugins/session-relay/bin` | Output is exactly `plugins/session-relay/bin/relay`. |
| A4 | `node scripts/ci.mjs --plugin session-relay` | Exit 0; Session Relay Rust, launcher, workflow, manifest, skill, and self-test gates pass. |
| A5 | `node scripts/skills/content-hash.mjs --check-only plugins/session-relay/skills` | Exit 0 with no stale hash. |
| A6 | `base=$(node -e 'const fs=require("node:fs");const s=fs.readFileSync("docs/plans/active/session-relay-prebuilt-cli-distribution.md","utf8");let v;if(s.trimStart().startsWith("{"))v=JSON.parse(s).execution_base_commit;else v=s.match(/^execution_base_commit:\s*([0-9a-f]{40})$/m)?.[1];if(!/^[0-9a-f]{40}$/.test(v||""))process.exit(1);process.stdout.write(v)') && git diff --check "$base..HEAD"` | Exit 0; the lifecycle base is exactly one full SHA in either tracked YAML or sealed JSON projection, and no whitespace error exists anywhere in the immutable implementation range. |
| A7 | `node scripts/release.mjs --verify-embedded-preparation --plugin session-relay 0.12.0 --plan docs/plans/active/session-relay-prebuilt-cli-distribution.md` | Exit 0; the command reconstructs the two embedded red receipts plus preflight, source-CI, and candidate JCS bytes, verifies every recorded SHA-256/test blob/ref identity against freshly fetched public and plan-derived Docks commits, proves the current release-owned tree is unchanged, and emits no replacement candidate. |
| A8 | `test -z "$(git status --porcelain=v1)"` | Exit 0, proving the Docks verification checkout is clean before completion review; companion cleanliness and exact HEAD identity are already verified immutably by A1. |

## Required follow-up release plan

After this plan is finished, plan-manager must create and independently review `docs/plans/active/session-relay-prebuilt-cli-release.md` before any release or production-branch mutation. Its immutable inputs are this plan's passed completion receipt, embedded red/preflight/source-CI/candidate receipts, exact `TAG_COMMIT = SOURCE_COMMIT`, exact `PROMOTED_COMMIT = SHIPPED_COMMIT`, their ancestry/non-plan equivalence proof, reviewed plan-only evidence commit, and the freshly fetchable companion public validation ref/implementation commit recorded under Notes. `--bind-completion` reconstructs the embedded candidate from the finished plan, freshly fetches the companion ref, and verifies every hash and commit relation; it does not depend on `/tmp` or either original worktree. The release plan owns the authorized Session Relay prerelease at `TAG_COMMIT`, authenticated digest collection, production public pins and completion review, docks-kit `0.9.0` publication, serialized Docks promotion through `PROMOTED_COMMIT` under an expected-main lease and durable transaction journal, exact-source and live smoke evidence, Session Relay stable finalization only after success, interruption resume/retry/reapply if needed, and terminal `0.12.1` supersession if `0.12.0` cannot safely promote. The outer user goal remains incomplete until that release plan is shipped; this source-preparation plan must not claim publication success.

## Out of scope / do-NOT-touch

- Do not commit newly generated Rust release binaries anywhere in the repository; deleting the obsolete committed binaries is required, but producing replacements in-tree is forbidden.
- Do not add a plugin-install hook that downloads or compiles code; plugin hook and MCP startup must remain deterministic local execution.
- Do not add Windows support in this plan; the producer supports the existing four Linux/macOS targets, while `docks-kit` must fail clearly on unsupported platforms.
- Do not modify Session Relay message, wake, fanout, lifecycle, or app-server semantics except for the top-level CLI version response.
- Do not modify unrelated plugin versions or manifests; release only Session Relay in Docks and the `docks-kit` package in the public repository.
- Do not edit `/home/vagrant/projects/public` directly from this Docks worktree; use the Session Relay worker and its repository-local plan-manager.

## Known gotchas

- GitHub Actions references must remain pinned to full 40-character commit SHAs, including any download/upload or release action introduced.
- A reusable workflow called from tag CI must expose artifacts to the publishing job in the same workflow run; do not substitute artifacts from a prior manual run.
- The final reviewed Session Relay commit intentionally exists on the remote only through its tag until `cli-v0.9.0` is live. Do not replace the tag-only push with a normal branch push or run a generic push helper before promotion.
- Both validation refs are create-once versioned names with a commit prefix and a separately recorded full tip. Push them without force, require absence or exact equality, and never advance or delete them.
- Promotion's transaction ref is permanent append-only state, not a cleanup branch. Every update must be a leased first-parent fast-forward whose closed entry validates before any external mutation resumes.
- `actions/download-artifact` extracts per-artifact directories by default; checksum generation must deliberately flatten or reference the resulting paths without accepting duplicate names.
- A shell `command -v session-relay` hit may resolve the launcher itself through a plugin-added PATH entry. Compare canonical paths before `exec` to prevent recursion.
- Local self-tests run before any GitHub Release exists. They must use only `SESSION_RELAY_TEST_BIN` pointing at a freshly built host binary, never select debug/committed fallbacks, skip, or attempt a network download.
- `Cargo.lock` records the package version. Release-version synchronization must update it deterministically, normally through Cargo rather than hand-editing.
- Before publication, the companion installer uses fixture digests only in tests and its plan remains blocked on the four production digests. Production pins are written and reviewed only after the immutable Session Relay assets exist.

## Global constraints

- Treat plugin marketplaces, installers, and downloaded artifacts as untrusted until verified.
- No secrets in committed config.
- Run `node scripts/ci.mjs` before any commit.
- Do not release or modify generated release binaries unless explicitly asked for a release.
- User installation must not require a Rust compiler.
- `docks-kit sync` must automatically install or update the verified precompiled Session Relay CLI for the current supported OS/architecture whenever it installs or refreshes the Session Relay plugin.
- Every downloaded executable must be checksummed before installation and smoke-tested before replacing an existing executable.
- The stable installed command is `session-relay`; the plugin-internal compatibility launcher remains `plugins/session-relay/bin/relay`.
- Supported release targets are exactly `x86_64-unknown-linux-musl`, `aarch64-unknown-linux-musl`, `x86_64-apple-darwin`, and `aarch64-apple-darwin`.

## STOP conditions

- STOP if the exact four-target validation-only producer preflight has not passed on the release-ready commit before completion review.
- STOP if current GitHub Actions artifact semantics cannot prove the published binaries came from the same tag-gated workflow run; do not fall back to latest/manual artifacts.
- STOP if `docks-kit` cannot verify a checksum before replacement or cannot preserve an existing executable on failure.
- STOP if plugin startup would need network access or compilation to retain compatibility.
- STOP if a release tool change would affect non-Session-Relay plugin release behavior without an explicit regression test.
- STOP collection if the Session Relay worker reports a dirty worktree, out-of-scope edits, an unreviewed plan transition, or a merge conflict.
- STOP before releasing `docks-kit` if the live Session Relay asset cannot be checksum-verified and installed through its actual installer in a temporary home.
- STOP before advancing Docks `origin/main` if `cli-v0.9.0` is absent, draft, failed, or does not install the staged public `0.12.0` prerelease asset through the real sync path.
- STOP any publication if this Docks plan lacks a reusable passed completion receipt for the exact reviewed implementation commit.
- STOP the public release if any source-pinned target digest differs from the independently hashed live asset or if the public plan has not passed completion review at the pinned release commit.
- STOP Session Relay stable finalization until the exact `cli-v0.9.0` host asset and checksum are live and a successful locked promotion receipt proves the exact released docks-kit executable against launcher-only Docks `main`; the prerelease staging body must not advertise installation.
- STOP on promotion-lock contention, expected-main drift, or a closed compatibility-set concurrent change. A definitive failed smoke must leave either a verified compatibility restore receipt or a typed manual incident with the prerelease still staged; never finalize it.
- STOP if either red receipt cannot prove a nonzero frozen-test run before production edits, if the public validation ref cannot be freshly fetched at its recorded commit, or if a promotion transaction journal is absent, forked, regressed, or conflicts with authoritative remote state.

## Cold-handoff checklist

- **File manifest:** Present; every Docks-owned step names exact paths and the public-repository boundary is explicitly external.
- **Environment & commands:** Present; Node, pnpm, Rust, focused gates, full CI, and hash maintenance commands are exact.
- **Interface & data contracts:** Present; asset names, checksum format, installed path, version output, launcher order, and release ownership are fixed.
- **Executable acceptance:** Present; A1–A8 are exact commands with no caller substitutions for the source-preparation goal; A7 independently verifies embedded preflight and broad-CI evidence against plan-derived `SOURCE_COMMIT`, while a separately reviewed release plan owns all publication, production-branch mutations, and live evidence.
- **Out of scope:** Present; committing generated replacement binaries, Windows support, runtime semantics, and direct public edits are prohibited; the explicitly authorized Session Relay `0.12.0` and subsequent `docks-kit` releases are required.
- **Decision rationale:** Present; CI assets plus explicit installer are selected to reduce payload size and improve provenance without startup downloads.
- **Known gotchas:** Present; same-run provenance, action pinning, launcher recursion, offline tests, Cargo lockstep, and pre-release pins are covered.
- **Global constraints verbatim:** Present; user compiler, checksum, supported-target, binary, security, and release constraints are explicit.
- **No undefined terms / forward refs:** Present for the source-preparation goal; release-time commands and receipts will be bound to the implemented CLI surfaces in the required follow-up release plan.

## Self-review
Review-receipt: {"input_sha256":"c782b00337b2ee28c553c1cc9925da735018885d36f70100a40c5f6f34eb3dcb","outcome":"passed","phase":"draft","policy":{"candidates":[{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},{"company":"anthropic","effort":"high","model":"fable","tool":"claude"},{"company":"anthropic","effort":"xhigh","model":"opus","tool":"claude"}],"fallback":"availability_only","max_rounds":2,"provenance":{"candidates":"skill_default","fallback":"skill_default","max_rounds":"skill_default","role":"skill_default"},"role":"primary","schema":5},"policy_sha256":"9629d2b08ff15aa63e30f6177f537c7d0f804d8d7847093c251169253fdf07ab","pre_execution_eligible":true,"reproduced":[],"request":{"acceptance_inventory_sha256":null,"author":{"company":"openai","effort":"high","model":"gpt-5.6-sol","tool":"codex"},"bundle_sha256":"8294c86c483586ba3aaec9faddb5fd4a756ca25f9d5b07f0ab57b4a592930834","diff_sha256":null,"execution_base_commit":null,"input_sha256":"c782b00337b2ee28c553c1cc9925da735018885d36f70100a40c5f6f34eb3dcb","lifecycle_intent":"start","phase":"draft","planned_at_commit":null,"policy":{"candidates":[{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},{"company":"anthropic","effort":"high","model":"fable","tool":"claude"},{"company":"anthropic","effort":"xhigh","model":"opus","tool":"claude"}],"fallback":"availability_only","max_rounds":2,"provenance":{"candidates":"skill_default","fallback":"skill_default","max_rounds":"skill_default","role":"skill_default"},"role":"primary","schema":5},"policy_sha256":"9629d2b08ff15aa63e30f6177f537c7d0f804d8d7847093c251169253fdf07ab","previous_input_sha256":"8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527","repair_targets_sha256":"2dd8e167185c002ee2e07a218650c9d562657c33f6ebd4d3220b9de7868a1965","request_id":"8a839dfd-ab5f-449b-a1b9-8c12df6e2a65","review_mode":"repair","reviewed_commit_or_head":"b27b9ba5c5215de26cd3c485397117a9410c9e87","round_index":2,"schema":5},"reviewed_at":"2026-07-17T16:53:17Z","reviewed_commit":"b27b9ba5c5215de26cd3c485397117a9410c9e87","reviewer":{"accepted_finding_ids":[],"raw":{"attempts":[{"candidate":{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},"child_id":"plan-review-9","denial_source":null,"exit_code":0,"output_started":true,"reason":"Fresh OMP plan-review subagent returned closed schema-5 evidence.","result":"passed","schema":5,"signal":null,"started":true,"stderr_sha256":"e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855","stdout_sha256":"7a7aa502add5f62ac2daf7e5aa3089f37211999492e82f3e060616d77659665f","timeout_mode":"orchestrator_tool","timeout_seconds":600}],"findings_sha256":"4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945","reason":null,"request":{"acceptance_inventory_sha256":null,"author":{"company":"openai","effort":"high","model":"gpt-5.6-sol","tool":"codex"},"bundle_sha256":"8294c86c483586ba3aaec9faddb5fd4a756ca25f9d5b07f0ab57b4a592930834","diff_sha256":null,"execution_base_commit":null,"input_sha256":"c782b00337b2ee28c553c1cc9925da735018885d36f70100a40c5f6f34eb3dcb","lifecycle_intent":"start","phase":"draft","planned_at_commit":null,"policy":{"candidates":[{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},{"company":"anthropic","effort":"high","model":"fable","tool":"claude"},{"company":"anthropic","effort":"xhigh","model":"opus","tool":"claude"}],"fallback":"availability_only","max_rounds":2,"provenance":{"candidates":"skill_default","fallback":"skill_default","max_rounds":"skill_default","role":"skill_default"},"role":"primary","schema":5},"policy_sha256":"9629d2b08ff15aa63e30f6177f537c7d0f804d8d7847093c251169253fdf07ab","previous_input_sha256":"8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527","repair_targets_sha256":"2dd8e167185c002ee2e07a218650c9d562657c33f6ebd4d3220b9de7868a1965","request_id":"8a839dfd-ab5f-449b-a1b9-8c12df6e2a65","review_mode":"repair","reviewed_commit_or_head":"b27b9ba5c5215de26cd3c485397117a9410c9e87","round_index":2,"schema":5},"result":"passed","reviewer_output":{"checklist":{"actionability":{"evidence":"The repaired plan supplies literal creation, capture, embedding, cleanup, rematerialization, resume, atomic-write, idempotence, and conflict-rejection behaviors; Steps 3, 6, and 8 assign each behavior to concrete files and commands.","status":"pass"},"dependency_order":{"evidence":"The step table orders the capture helper first, public and Docks red captures before production work, implementation after both captures, release tooling after launcher/workflow work, and fresh receipt rematerialization only in Step 8. Terminal recovery consumes an already terminal authoritative journal and cannot create a later phase in that attempt.","status":"pass"},"evidence_reverification":{"evidence":"The sealed manifest binds current plan.review.md to input c782b00337b2ee28c553c1cc9925da735018885d36f70100a40c5f6f34eb3dcb, prior plan to 8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527, repair targets to the request identity, and reviewed commit b27b9ba5c5215de26cd3c485397117a9410c9e87. Re-reading both plan versions confirms the new per-capture embedding/rematerialization contract and terminal-only recovery contract directly replace the cited P1 and P3 gaps.","status":"pass"},"executable_acceptance":{"evidence":"The plan requires fixture coverage for mode-0600 atomic TDD-red capture and rematerialization, and for every terminal outcome killed after journal CAS but before receipt rename. Those promotion fixtures require byte-identical recovery, mutation-free second resume, fail-closed conflicts, and exactly one next attempt from a recovered retryable restored_failure receipt.","status":"pass"},"failure_modes":{"evidence":"Capture directories are canonicalized, device/inode-bound, narrowly cleaned, and never reused across phases; embedded receipt bytes and hashes reject pending, duplicate, noncanonical, or mismatched data. Terminal recovery validates the exact tip, chain, identities, refs, Release state, main state, and blob map, appends no journal entry, repeats no mutation, accepts only byte-identical existing output, and rejects every conflict.","status":"pass"},"goal_coverage":{"evidence":"Both and only accepted repair targets are covered: P1's unavailable REQUEST_DIR handoff is replaced by immediate durable Notes bytes plus fresh Step 8 materialization, and P3's terminal crash dead-end is replaced by receipt reconstruction from the immutable terminal journal and authoritative state.","status":"pass"},"open_questions":{"evidence":"No unresolved decision remains for P1 or P3: ownership and lifetime of red evidence, Step 8 reconstruction, terminal-outcome mapping, creation-time determinism, existing-output behavior, retry eligibility, and retry-attempt uniqueness are all specified.","status":"pass"},"standalone_executability":{"evidence":"P1 is closed without cross-shell state: plan.review.md lines 130-150 give each repository an identity-checked mode-0700 capture directory, require immediate exact-byte/hash Notes embedding and cleanup before production edits, and lines 41-53 create a separate Step 8 REQUEST_DIR that rematerializes those embedded bytes.","status":"pass"}},"findings":[],"request":{"acceptance_inventory_sha256":null,"author":{"company":"openai","effort":"high","model":"gpt-5.6-sol","tool":"codex"},"bundle_sha256":"8294c86c483586ba3aaec9faddb5fd4a756ca25f9d5b07f0ab57b4a592930834","diff_sha256":null,"execution_base_commit":null,"input_sha256":"c782b00337b2ee28c553c1cc9925da735018885d36f70100a40c5f6f34eb3dcb","lifecycle_intent":"start","phase":"draft","planned_at_commit":null,"policy":{"candidates":[{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},{"company":"anthropic","effort":"high","model":"fable","tool":"claude"},{"company":"anthropic","effort":"xhigh","model":"opus","tool":"claude"}],"fallback":"availability_only","max_rounds":2,"provenance":{"candidates":"skill_default","fallback":"skill_default","max_rounds":"skill_default","role":"skill_default"},"role":"primary","schema":5},"policy_sha256":"9629d2b08ff15aa63e30f6177f537c7d0f804d8d7847093c251169253fdf07ab","previous_input_sha256":"8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527","repair_targets_sha256":"2dd8e167185c002ee2e07a218650c9d562657c33f6ebd4d3220b9de7868a1965","request_id":"8a839dfd-ab5f-449b-a1b9-8c12df6e2a65","review_mode":"repair","reviewed_commit_or_head":"b27b9ba5c5215de26cd3c485397117a9410c9e87","round_index":2,"schema":5},"role":"primary","schema":5,"verdict":"pass"},"role":"primary","schema":5,"selected":{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},"waiver":null,"waiver_sha256":null},"rejected":[]},"schema":5,"series":{"current_input_sha256":"c782b00337b2ee28c553c1cc9925da735018885d36f70100a40c5f6f34eb3dcb","initial_input_sha256":"8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527","policy_sha256":"9629d2b08ff15aa63e30f6177f537c7d0f804d8d7847093c251169253fdf07ab","repairs":[{"accepted_finding_ids":["P1","P3"],"current_input_sha256":"c782b00337b2ee28c553c1cc9925da735018885d36f70100a40c5f6f34eb3dcb","from_round_index":1,"previous_input_sha256":"8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527","repair_targets_sha256":"2dd8e167185c002ee2e07a218650c9d562657c33f6ebd4d3220b9de7868a1965","schema":5,"targets":[{"criterion":"standalone_executability","defect":"The one canonical Docks red command requires `$REQUEST_DIR`, but the plan creates that directory only as part of the later producer-preflight procedure. No earlier step initializes it, installs the identity-checked cleanup trap, or states that the same directory must survive both red captures through Step 8. Executing the literal Step 3 command from a fresh handoff therefore cannot produce the receipt at the path later passed to `--check-prepared`.","evidence":"The sealed plan first defines REQUEST_DIR under “Exact producer preflight” (lines 41-50), while its TDD section requires `--receipt-out \"$REQUEST_DIR/docks-red.json\"` immediately after the Step 3 test commit (lines 130-136). Step 8 later consumes `$REQUEST_DIR/docks-red.json` and `$REQUEST_DIR/public-red.json`, but Steps 1-3 contain no directory initialization or lifetime handoff.","fix":"Move the mode-0700 identity-checked REQUEST_DIR creation and EXIT-trap installation into an explicit prerequisite before the first public or Docks red capture, assign exact public-red and docks-red paths there, and state that the same directory remains owned until all receipt bytes are embedded in Step 8; keep only preflight artifact creation in the later block.","id":"P1","locator":"lines 41-50 and 130-136; Steps 1-3 and 8","path":"plan.review.md","reproduction":{"command":null,"evidence_sha256":"571f94dd7dab2af14bf47be137c21fdecb55878dcf9c685ebd13a69829657d9f","exit_code":null,"method":"read"},"section":"Environment & how-to-run / TDD-red capture","source":"primary","status":"blocking_gap"},{"criterion":"failure_modes","defect":"A crash after the terminal journal entry is durable but before `PromotionReceiptV1` is renamed is unrecoverable under the stated grammar. Terminal phases have no outgoing transition, `--resume-promotion` rejects terminal journals, and `--retry-failed` requires the missing canonical restored-failure receipt. The specified kill-point test incorrectly accepts “stops” instead of proving receipt recovery, so the required retry machinery can dead-end after a normal crash boundary.","evidence":"The phase contract says terminal_success, terminal_failure, and manual_incident have no outgoing transition. The resume contract is limited to nonterminal attempts and rejects terminal journals. The retry contract accepts only an explicit canonical restored_failure receipt, while the kill matrix includes the moment immediately before terminal receipt rename and allows the subsequent invocation merely to stop.","fix":"Define one existing invocation as terminal-receipt recovery: after validating the exact terminal journal tip and authoritative refs, it must reconstruct and atomically create the unique missing PromotionReceiptV1 without appending a phase or repeating mutations; a matching existing receipt is idempotent and a conflicting one fails closed. Change the crash-before-rename fixture to require byte-identical receipt materialization and, for restored_failure, successful use of that receipt to start exactly the next attempt.","id":"P3","locator":"lines 104-110 and PromotionReceiptV1 definition at line 151","path":"plan.review.md","reproduction":{"command":null,"evidence_sha256":"6d24e617902a5246ab89a7370ab155aa9df0409759aab98a4d1ea455a35e1b6e","exit_code":null,"method":"read"},"section":"Release orchestration retry and terminal receipt recovery","source":"primary","status":"blocking_gap"}]}],"rounds":[{"kind":"draft","outcome":"not_ready","pre_execution_eligible":false,"reproduced":[{"id":"P1","reproduction":{"command":null,"evidence_sha256":"571f94dd7dab2af14bf47be137c21fdecb55878dcf9c685ebd13a69829657d9f","exit_code":null,"method":"read"}},{"id":"P2","reproduction":{"command":null,"evidence_sha256":"9119ee9ddce34ca9ab44d67b16487b520761ff78822c4347ad9913ec00e66bfb","exit_code":null,"method":"read"}},{"id":"P3","reproduction":{"command":null,"evidence_sha256":"6d24e617902a5246ab89a7370ab155aa9df0409759aab98a4d1ea455a35e1b6e","exit_code":null,"method":"read"}}],"request":{"acceptance_inventory_sha256":null,"author":{"company":"openai","effort":"high","model":"gpt-5.6-sol","tool":"codex"},"bundle_sha256":"cb3063f413e1b866ba9638a221c70e4968618095977e6970388a10f859670d1c","diff_sha256":null,"execution_base_commit":null,"input_sha256":"8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527","lifecycle_intent":"start","phase":"draft","planned_at_commit":null,"policy":{"candidates":[{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},{"company":"anthropic","effort":"high","model":"fable","tool":"claude"},{"company":"anthropic","effort":"xhigh","model":"opus","tool":"claude"}],"fallback":"availability_only","max_rounds":2,"provenance":{"candidates":"skill_default","fallback":"skill_default","max_rounds":"skill_default","role":"skill_default"},"role":"primary","schema":5},"policy_sha256":"9629d2b08ff15aa63e30f6177f537c7d0f804d8d7847093c251169253fdf07ab","previous_input_sha256":null,"repair_targets_sha256":null,"request_id":"ad67303d-9fd3-4a0f-8252-b980a1af94e3","review_mode":"full","reviewed_commit_or_head":"d462d51df0c24c602dd106918b61db5e8436eb0a","round_index":1,"schema":5},"reviewer":{"accepted_finding_ids":["P1","P3"],"raw":{"attempts":[{"candidate":{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},"child_id":"plan-review-8","denial_source":null,"exit_code":0,"output_started":true,"reason":"Fresh OMP plan-review subagent returned closed schema-5 evidence.","result":"passed","schema":5,"signal":null,"started":true,"stderr_sha256":"e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855","stdout_sha256":"acb52c272241d8263bf109b1c57ea0831a2c711222cd62fe6eb8eb3ea44ecbca","timeout_mode":"orchestrator_tool","timeout_seconds":600}],"findings_sha256":"b3b325974cdd150a759ced6d3ba10c6516852cfe8c90d310e28d0144b80b4882","reason":null,"request":{"acceptance_inventory_sha256":null,"author":{"company":"openai","effort":"high","model":"gpt-5.6-sol","tool":"codex"},"bundle_sha256":"cb3063f413e1b866ba9638a221c70e4968618095977e6970388a10f859670d1c","diff_sha256":null,"execution_base_commit":null,"input_sha256":"8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527","lifecycle_intent":"start","phase":"draft","planned_at_commit":null,"policy":{"candidates":[{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},{"company":"anthropic","effort":"high","model":"fable","tool":"claude"},{"company":"anthropic","effort":"xhigh","model":"opus","tool":"claude"}],"fallback":"availability_only","max_rounds":2,"provenance":{"candidates":"skill_default","fallback":"skill_default","max_rounds":"skill_default","role":"skill_default"},"role":"primary","schema":5},"policy_sha256":"9629d2b08ff15aa63e30f6177f537c7d0f804d8d7847093c251169253fdf07ab","previous_input_sha256":null,"repair_targets_sha256":null,"request_id":"ad67303d-9fd3-4a0f-8252-b980a1af94e3","review_mode":"full","reviewed_commit_or_head":"d462d51df0c24c602dd106918b61db5e8436eb0a","round_index":1,"schema":5},"result":"passed","reviewer_output":{"checklist":{"actionability":{"evidence":"The plan names concrete files, closed CLI grammars, exact commands, receipt fields, step done conditions, and mutation boundaries for the Docks and companion repositories.","status":"pass"},"dependency_order":{"evidence":"The numbered graph places the capture helper before both repositories' red tests, requires collection of the companion worker before deleting embedded Relay binaries, and orders launcher/workflow/tooling/skill work before source sealing.","status":"pass"},"evidence_reverification":{"evidence":"A1 and A7 require fresh detached public-ref verification and reconstruction of embedded canonical receipts; Step 8 also binds SOURCE_COMMIT to preflight and source-CI run identities and requires a plan-only SOURCE_COMMIT..EVIDENCE_COMMIT delta.","status":"pass"},"executable_acceptance":{"evidence":"A1-A8 provide literal commands for companion-contract verification, a fresh Rust build plus explicit black-box self-test, launcher-only tracked payload, focused CI, skill hash, immutable-range whitespace, embedded evidence reconstruction, and final cleanliness; the plan separately requires the broad `node scripts/ci.mjs` gate.","status":"pass"},"failure_modes":{"evidence":"plan.review.md lines 104-110 makes terminal journal entries have no outgoing transition, makes `--resume-promotion` reject terminal journals, and makes retry require a canonical restored-failure receipt. The kill-point test includes a crash immediately before terminal receipt rename but permits the next invocation merely to stop, leaving no defined way to materialize the missing receipt or authorize a retry.","status":"blocking_gap"},"goal_coverage":{"evidence":"The final source contract requires selftest.mjs to run only with an explicit SESSION_RELAY_TEST_BIN, but the sealed shipped skill still documents bare `node <plugin>/test/selftest.mjs`; Step 7 and A5 require guidance/content-hash maintenance without requiring that Verify block to be made executable under the new contract.","status":"non_blocking_gap"},"open_questions":{"evidence":"The plan closes target platforms, asset names, version identities, publication states, promotion phases, receipt families, mutation ownership, and follow-up release ownership; pending Notes values are execution outputs rather than unresolved design choices.","status":"pass"},"standalone_executability":{"evidence":"The canonical Docks red-capture command at plan.review.md lines 130-136 writes to \"$REQUEST_DIR/docks-red.json\", but the only REQUEST_DIR initialization appears in the later producer-preflight block at lines 41-50. Steps 1-3 require the red receipt before Step 8's producer preflight, so a cold executor has no defined, identity-checked directory for the literal command or durable location from which Step 8 can consume the receipt.","status":"blocking_gap"}},"findings":[{"criterion":"standalone_executability","defect":"The one canonical Docks red command requires `$REQUEST_DIR`, but the plan creates that directory only as part of the later producer-preflight procedure. No earlier step initializes it, installs the identity-checked cleanup trap, or states that the same directory must survive both red captures through Step 8. Executing the literal Step 3 command from a fresh handoff therefore cannot produce the receipt at the path later passed to `--check-prepared`.","evidence":"The sealed plan first defines REQUEST_DIR under “Exact producer preflight” (lines 41-50), while its TDD section requires `--receipt-out \"$REQUEST_DIR/docks-red.json\"` immediately after the Step 3 test commit (lines 130-136). Step 8 later consumes `$REQUEST_DIR/docks-red.json` and `$REQUEST_DIR/public-red.json`, but Steps 1-3 contain no directory initialization or lifetime handoff.","fix":"Move the mode-0700 identity-checked REQUEST_DIR creation and EXIT-trap installation into an explicit prerequisite before the first public or Docks red capture, assign exact public-red and docks-red paths there, and state that the same directory remains owned until all receipt bytes are embedded in Step 8; keep only preflight artifact creation in the later block.","id":"P1","locator":"lines 41-50 and 130-136; Steps 1-3 and 8","path":"plan.review.md","section":"Environment & how-to-run / TDD-red capture","status":"blocking_gap"},{"criterion":"goal_coverage","defect":"The shipped skill's Verify block runs `node <plugin>/test/selftest.mjs` without a binary, while the plan's final contract explicitly makes `SESSION_RELAY_TEST_BIN` mandatory and A2 supplies a freshly built release binary. Step 7 only requires naming the installed executable, troubleshooting, helper placement, and an updated content hash, so this now-invalid verification command can remain and still satisfy its done condition and A5.","evidence":"The sealed source says `node <plugin>/test/selftest.mjs`; plan.review.md A2 instead requires `cargo build ... && SESSION_RELAY_TEST_BIN=... node .../selftest.mjs`, and Step 4 says the frozen self-test requires SESSION_RELAY_TEST_BIN. A5 checks only metadata/content-hash freshness, not command correctness.","fix":"Add to Step 7's done condition and acceptance that the Verify block uses the same explicit fresh-binary contract as A2 (or invokes a documented CI wrapper that builds and injects it), and add a contract assertion that the skill no longer advertises the bare self-test command.","id":"P2","locator":"## Verify, lines 493-497; plan.review.md Step 7 and A5","path":"plugins/session-relay/skills/productivity/session-relay/SKILL.md","section":"Step 7 usage guidance","status":"non_blocking_gap"},{"criterion":"failure_modes","defect":"A crash after the terminal journal entry is durable but before `PromotionReceiptV1` is renamed is unrecoverable under the stated grammar. Terminal phases have no outgoing transition, `--resume-promotion` rejects terminal journals, and `--retry-failed` requires the missing canonical restored-failure receipt. The specified kill-point test incorrectly accepts “stops” instead of proving receipt recovery, so the required retry machinery can dead-end after a normal crash boundary.","evidence":"The phase contract says terminal_success, terminal_failure, and manual_incident have no outgoing transition. The resume contract is limited to nonterminal attempts and rejects terminal journals. The retry contract accepts only an explicit canonical restored_failure receipt, while the kill matrix includes the moment immediately before terminal receipt rename and allows the subsequent invocation merely to stop.","fix":"Define one existing invocation as terminal-receipt recovery: after validating the exact terminal journal tip and authoritative refs, it must reconstruct and atomically create the unique missing PromotionReceiptV1 without appending a phase or repeating mutations; a matching existing receipt is idempotent and a conflicting one fails closed. Change the crash-before-rename fixture to require byte-identical receipt materialization and, for restored_failure, successful use of that receipt to start exactly the next attempt.","id":"P3","locator":"lines 104-110 and PromotionReceiptV1 definition at line 151","path":"plan.review.md","section":"Release orchestration retry and terminal receipt recovery","status":"blocking_gap"}],"request":{"acceptance_inventory_sha256":null,"author":{"company":"openai","effort":"high","model":"gpt-5.6-sol","tool":"codex"},"bundle_sha256":"cb3063f413e1b866ba9638a221c70e4968618095977e6970388a10f859670d1c","diff_sha256":null,"execution_base_commit":null,"input_sha256":"8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527","lifecycle_intent":"start","phase":"draft","planned_at_commit":null,"policy":{"candidates":[{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},{"company":"anthropic","effort":"high","model":"fable","tool":"claude"},{"company":"anthropic","effort":"xhigh","model":"opus","tool":"claude"}],"fallback":"availability_only","max_rounds":2,"provenance":{"candidates":"skill_default","fallback":"skill_default","max_rounds":"skill_default","role":"skill_default"},"role":"primary","schema":5},"policy_sha256":"9629d2b08ff15aa63e30f6177f537c7d0f804d8d7847093c251169253fdf07ab","previous_input_sha256":null,"repair_targets_sha256":null,"request_id":"ad67303d-9fd3-4a0f-8252-b980a1af94e3","review_mode":"full","reviewed_commit_or_head":"d462d51df0c24c602dd106918b61db5e8436eb0a","round_index":1,"schema":5},"role":"primary","schema":5,"verdict":"blocking_gap"},"role":"primary","schema":5,"selected":{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},"waiver":null,"waiver_sha256":null},"rejected":[{"id":"P2","reason":"Not a plan-executability defect: the cited bare command is advisory skill Verify prose, while acceptance criterion A2 and Step 2 already provide the exact fresh-binary command with SESSION_RELAY_TEST_BIN. No lifecycle or Step 7 gate requires the bare skill command."}]},"schema":5},{"kind":"draft","outcome":"passed","pre_execution_eligible":true,"reproduced":[],"request":{"acceptance_inventory_sha256":null,"author":{"company":"openai","effort":"high","model":"gpt-5.6-sol","tool":"codex"},"bundle_sha256":"8294c86c483586ba3aaec9faddb5fd4a756ca25f9d5b07f0ab57b4a592930834","diff_sha256":null,"execution_base_commit":null,"input_sha256":"c782b00337b2ee28c553c1cc9925da735018885d36f70100a40c5f6f34eb3dcb","lifecycle_intent":"start","phase":"draft","planned_at_commit":null,"policy":{"candidates":[{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},{"company":"anthropic","effort":"high","model":"fable","tool":"claude"},{"company":"anthropic","effort":"xhigh","model":"opus","tool":"claude"}],"fallback":"availability_only","max_rounds":2,"provenance":{"candidates":"skill_default","fallback":"skill_default","max_rounds":"skill_default","role":"skill_default"},"role":"primary","schema":5},"policy_sha256":"9629d2b08ff15aa63e30f6177f537c7d0f804d8d7847093c251169253fdf07ab","previous_input_sha256":"8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527","repair_targets_sha256":"2dd8e167185c002ee2e07a218650c9d562657c33f6ebd4d3220b9de7868a1965","request_id":"8a839dfd-ab5f-449b-a1b9-8c12df6e2a65","review_mode":"repair","reviewed_commit_or_head":"b27b9ba5c5215de26cd3c485397117a9410c9e87","round_index":2,"schema":5},"reviewer":{"accepted_finding_ids":[],"raw":{"attempts":[{"candidate":{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},"child_id":"plan-review-9","denial_source":null,"exit_code":0,"output_started":true,"reason":"Fresh OMP plan-review subagent returned closed schema-5 evidence.","result":"passed","schema":5,"signal":null,"started":true,"stderr_sha256":"e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855","stdout_sha256":"7a7aa502add5f62ac2daf7e5aa3089f37211999492e82f3e060616d77659665f","timeout_mode":"orchestrator_tool","timeout_seconds":600}],"findings_sha256":"4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945","reason":null,"request":{"acceptance_inventory_sha256":null,"author":{"company":"openai","effort":"high","model":"gpt-5.6-sol","tool":"codex"},"bundle_sha256":"8294c86c483586ba3aaec9faddb5fd4a756ca25f9d5b07f0ab57b4a592930834","diff_sha256":null,"execution_base_commit":null,"input_sha256":"c782b00337b2ee28c553c1cc9925da735018885d36f70100a40c5f6f34eb3dcb","lifecycle_intent":"start","phase":"draft","planned_at_commit":null,"policy":{"candidates":[{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},{"company":"anthropic","effort":"high","model":"fable","tool":"claude"},{"company":"anthropic","effort":"xhigh","model":"opus","tool":"claude"}],"fallback":"availability_only","max_rounds":2,"provenance":{"candidates":"skill_default","fallback":"skill_default","max_rounds":"skill_default","role":"skill_default"},"role":"primary","schema":5},"policy_sha256":"9629d2b08ff15aa63e30f6177f537c7d0f804d8d7847093c251169253fdf07ab","previous_input_sha256":"8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527","repair_targets_sha256":"2dd8e167185c002ee2e07a218650c9d562657c33f6ebd4d3220b9de7868a1965","request_id":"8a839dfd-ab5f-449b-a1b9-8c12df6e2a65","review_mode":"repair","reviewed_commit_or_head":"b27b9ba5c5215de26cd3c485397117a9410c9e87","round_index":2,"schema":5},"result":"passed","reviewer_output":{"checklist":{"actionability":{"evidence":"The repaired plan supplies literal creation, capture, embedding, cleanup, rematerialization, resume, atomic-write, idempotence, and conflict-rejection behaviors; Steps 3, 6, and 8 assign each behavior to concrete files and commands.","status":"pass"},"dependency_order":{"evidence":"The step table orders the capture helper first, public and Docks red captures before production work, implementation after both captures, release tooling after launcher/workflow work, and fresh receipt rematerialization only in Step 8. Terminal recovery consumes an already terminal authoritative journal and cannot create a later phase in that attempt.","status":"pass"},"evidence_reverification":{"evidence":"The sealed manifest binds current plan.review.md to input c782b00337b2ee28c553c1cc9925da735018885d36f70100a40c5f6f34eb3dcb, prior plan to 8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527, repair targets to the request identity, and reviewed commit b27b9ba5c5215de26cd3c485397117a9410c9e87. Re-reading both plan versions confirms the new per-capture embedding/rematerialization contract and terminal-only recovery contract directly replace the cited P1 and P3 gaps.","status":"pass"},"executable_acceptance":{"evidence":"The plan requires fixture coverage for mode-0600 atomic TDD-red capture and rematerialization, and for every terminal outcome killed after journal CAS but before receipt rename. Those promotion fixtures require byte-identical recovery, mutation-free second resume, fail-closed conflicts, and exactly one next attempt from a recovered retryable restored_failure receipt.","status":"pass"},"failure_modes":{"evidence":"Capture directories are canonicalized, device/inode-bound, narrowly cleaned, and never reused across phases; embedded receipt bytes and hashes reject pending, duplicate, noncanonical, or mismatched data. Terminal recovery validates the exact tip, chain, identities, refs, Release state, main state, and blob map, appends no journal entry, repeats no mutation, accepts only byte-identical existing output, and rejects every conflict.","status":"pass"},"goal_coverage":{"evidence":"Both and only accepted repair targets are covered: P1's unavailable REQUEST_DIR handoff is replaced by immediate durable Notes bytes plus fresh Step 8 materialization, and P3's terminal crash dead-end is replaced by receipt reconstruction from the immutable terminal journal and authoritative state.","status":"pass"},"open_questions":{"evidence":"No unresolved decision remains for P1 or P3: ownership and lifetime of red evidence, Step 8 reconstruction, terminal-outcome mapping, creation-time determinism, existing-output behavior, retry eligibility, and retry-attempt uniqueness are all specified.","status":"pass"},"standalone_executability":{"evidence":"P1 is closed without cross-shell state: plan.review.md lines 130-150 give each repository an identity-checked mode-0700 capture directory, require immediate exact-byte/hash Notes embedding and cleanup before production edits, and lines 41-53 create a separate Step 8 REQUEST_DIR that rematerializes those embedded bytes.","status":"pass"}},"findings":[],"request":{"acceptance_inventory_sha256":null,"author":{"company":"openai","effort":"high","model":"gpt-5.6-sol","tool":"codex"},"bundle_sha256":"8294c86c483586ba3aaec9faddb5fd4a756ca25f9d5b07f0ab57b4a592930834","diff_sha256":null,"execution_base_commit":null,"input_sha256":"c782b00337b2ee28c553c1cc9925da735018885d36f70100a40c5f6f34eb3dcb","lifecycle_intent":"start","phase":"draft","planned_at_commit":null,"policy":{"candidates":[{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},{"company":"anthropic","effort":"high","model":"fable","tool":"claude"},{"company":"anthropic","effort":"xhigh","model":"opus","tool":"claude"}],"fallback":"availability_only","max_rounds":2,"provenance":{"candidates":"skill_default","fallback":"skill_default","max_rounds":"skill_default","role":"skill_default"},"role":"primary","schema":5},"policy_sha256":"9629d2b08ff15aa63e30f6177f537c7d0f804d8d7847093c251169253fdf07ab","previous_input_sha256":"8bda00c438db221f50da03475180d4e9ab4abf20cf72e846e60423bfcc72a527","repair_targets_sha256":"2dd8e167185c002ee2e07a218650c9d562657c33f6ebd4d3220b9de7868a1965","request_id":"8a839dfd-ab5f-449b-a1b9-8c12df6e2a65","review_mode":"repair","reviewed_commit_or_head":"b27b9ba5c5215de26cd3c485397117a9410c9e87","round_index":2,"schema":5},"role":"primary","schema":5,"verdict":"pass"},"role":"primary","schema":5,"selected":{"company":"openai","effort":"high","model":"gpt-5.6-sol","service_tier":"default","tool":"codex"},"waiver":null,"waiver_sha256":null},"rejected":[]},"schema":5}],"schema":5}}

Score: 98/100 · one local pass · caught: separated the public repository into its own lifecycle, prohibited hook-time downloads, made same-run artifact provenance explicit, and added recursion plus pre-release-pin gotchas.

Cross-check (2026-07-16): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] 5 findings — accepted S1,S2,S3,S4,S5 / rejected none; [Codex main-context orchestrator] reproduced all five against the plan and source before accepting. Repairs reordered Relay bootstrap before binary deletion, incorporated the user's explicit release authorization, required exact-tree post-bump CI, made self-test binary selection explicit and non-skipping, and added pinned cross-repo plus live-release evidence.

Cross-check (2026-07-16, round 2): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] 4 findings — accepted S1,S2,S3,S4 / rejected none; [Codex main-context orchestrator] reproduced all four. Repairs require a complete affected-path review bundle, add every release-mutated manifest/catalog file, bind whitespace validation to the execution range, and make the final cleanliness command prove both repositories.

Cross-check (2026-07-16, round 3): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 86 with 3 findings — accepted S1,S2,S3 / rejected none; [Codex main-context orchestrator] reproduced all three. Repairs make Session Relay release notes install through `docks-kit sync`, remove the checklist contradiction around the authorized releases, and preserve `release.mjs --dry-run` as a tested byte-for-byte non-mutating preview. The resolved three-round batch is exhausted; another independent round requires the plan-manager continuation choice.

Cross-check (2026-07-16, round 4): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 72 with 4 findings — accepted S2,S3,S4 / rejected S1; [Codex main-context orchestrator] reproduced the rollout exposure, fail-open base extraction, and missing live sync acceptance. S1 was rejected because tracked `plugins/session-relay/rust/rust-toolchain.toml` already pins Rust `1.85.0`; it was absent only from the sealed review bundle, so the next bundle includes it as unchanged evidence. Repairs use a tag-only Session Relay publication, require `cli-v0.9.0` before Docks branch promotion, validate A6's SHA, and execute the real live docks-kit sync plus failure-preservation tests.

Cross-check (2026-07-16, round 5): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 55 with 4 findings — accepted S1,S2,S3,S4 / rejected none; [Codex main-context orchestrator] reproduced all four. Repairs move every release mutation after Docks completion review, make `0.12.0` the one final launcher-only payload, require four independently reviewed source-pinned public digests, and bind all cross-repository implementation evidence to detached clones of exact commits.

Cross-check (2026-07-16, round 6): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 58 with 4 findings — accepted S1,S2,S3,S4 / rejected none; [Codex main-context orchestrator] reproduced all four. Repairs keep the Session Relay Release draft until `cli-v0.9.0` is live, bind the smoke to the checksum-verified released docks-kit host executable, make promotion execute and seal the closed smoke receipt itself, and require an actual launcher-only post-promotion sync with a tested fast-forward compatibility revert on failure.

Cross-check (2026-07-16, round 7): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 64 with 4 findings — accepted S1,S2,S3,S4 / rejected none; [Codex main-context orchestrator] reproduced all four. Repairs end this plan at reviewed release-ready source, require a separately reviewed release plan for every external mutation, add exact Relay dispatch/collection commands, make promotion failure resumable through closed failure receipts plus fast-forward reapply commits, and delegate detached-clone cleanup to the distribution-contract helper.

Cross-check (2026-07-16, round 8): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 52 with 5 findings — accepted S1,S2,S3,S4,S5 / rejected none; [Codex main-context orchestrator] reproduced all five. Repairs add a real four-target validation-only GitHub preflight before completion review, define resumable publication/finalization reconciliation, close and version all release evidence schemas with canonical atomic writes, bind the public plan lifecycle evidence alongside its commit, and remove mutable public-worktree state from A8.

Cross-check (2026-07-16, round 9): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 60 with 4 findings — accepted S1,S2,S3,S4 / rejected none; [Codex main-context orchestrator] reproduced all four. Repairs replace inaccessible draft staging with a public non-install prerelease, delay stable finalization until locked promotion succeeds, add expected-main/lock/concurrency and terminal supersession behavior, freeze the full release-mode CLI grammar with fixture acceptance, and require checksum → staged chmod 0755 → version smoke → atomic rename.

Cross-check (2026-07-16, round 10): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 62 with 4 findings — accepted S1,S2,S3,S4 / rejected none; [Codex main-context orchestrator] reproduced all four. Repairs separate immutable `SOURCE_COMMIT` from a plan-only `EVIDENCE_COMMIT`, make `--check-prepared` rerun and hash its own A1–A6 checks, embed canonical preflight/candidate receipts in the tracked plan for durable handoff, and freeze the exact validation-only dispatch, run selection, artifact download, and verifier commands.

Cross-check (2026-07-16, round 11): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 54 with 5 findings — accepted S1,S2,S3,S4,S5 / rejected none; [Codex main-context orchestrator] reproduced all five. Repairs declare the preflight verifier in every ownership surface, reject premature stable state from publish reconciliation, freeze and regression-test the existing docks/effect-kit positional release interface, bind full repository CI at `SOURCE_COMMIT`, and define a mode-0700 request directory with identity-checked cleanup.

Cross-check (2026-07-16, round 12): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 58 with 4 findings — accepted S1,S2,S3,S4 / rejected none; [Codex main-context orchestrator] reproduced all four. Repairs replace placeholder acceptance with self-contained embedded-evidence verification, freeze the compatibility restore set and blob identities, add `publish-existing-tag` recovery for a missing tag-triggered run, and move all four targets to official native GitHub-hosted runners with same-run version attestations.

Cross-check (2026-07-16, round 13): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 68 with 3 findings — accepted S2,S3 / rejected S1; [Codex main-context orchestrator] confirmed the tracked plan is YAML and A6's line parser operates on that checkout, while the sealed review projection alone uses one-line JSON metadata. Repairs give `expected_tag` an explicit empty default and replace local detached-clone CI with an authoritative `ci.yml` run at `SOURCE_COMMIT`, binding the workflow's Node, pnpm, Claude, Rust, musl, lockfile, and full-gate identities.

Cross-check (2026-07-16, round 14): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 74 with 3 findings — accepted S1,S2,S3 / rejected none; [Codex main-context orchestrator] reproduced the placeholder public-worker handoff, invalid Session Relay dry-run invocation, and evidence-commit ordering gap. Repairs replace the placeholder with a literal self-contained Relay task, test the designed `--prepare ... --dry-run` grammar plus positional rejection, and require exact full CI on the receipt-embedded plan-only tree before `EVIDENCE_COMMIT`.

Cross-check (2026-07-16, round 15): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 56 with 4 findings — accepted S1,S2,S3,S4 / rejected none; [Codex main-context orchestrator] reproduced all four. Repairs add an append-only remote promotion transaction plus explicit interruption resume, make the companion commit durable through a create-once validation ref and fresh fetch, close and bind both repositories' TDD-red receipts, and correlate workflow dispatches by before/after database-ID sets plus exact queried inputs instead of local timestamps.

Cross-check (2026-07-16, round 16): [X: anthropic unavailable] authentication preflight failed; [S: openai gpt-5.6-sol high Standard] score 68 with 3 findings — accepted S1,S2,S3 / rejected none; [Codex main-context orchestrator] reproduced all three. Repairs remove the journal entry's impossible self-hash, fix `TAG_COMMIT = SOURCE_COMMIT` and `PROMOTED_COMMIT = SHIPPED_COMMIT` with ancestry plus non-plan tree equivalence, and make Step 3 the sole writer of the frozen Docks tests.

Self-review advisory (2026-07-17): the prior schema-5 output was invalid (`current finding status missing for gap criterion standalone_executability`), so it supplied no canonical findings and did not pass review. Main context plus workflowz read-only scouts independently reproduced three plan defects: the bare focused self-test command, red-receipt production before its helper existed, and a retry journal that did not close attempt ordering and terminal receipt bindings. They also reproduced the bundle-only omission of unchanged `plugins/session-relay/rust/rust-toolchain.toml`; it remains outside `affected_paths` because this plan does not modify it, and the next fresh review bundle must include it as unchanged source evidence.

Cross-check (2026-07-17, schema-5 repair round 1): canonical reviewer findings P1 and P3 accepted / P2 rejected because `non_blocking_gap` advisory is excluded from schema-5 repair targets. P1 is repaired by per-repository ephemeral red-capture ownership, immediate exact-byte/hash Notes embedding and identity-checked cleanup, followed by fresh Step 8 rematerialization; no trap, directory, or environment must survive between separately invoked shells, and no post-production recapture is permitted. P3 is repaired through the existing `--resume-promotion` grammar: exact terminal-state validation recovers the unique missing receipt atomically without journal or state mutation, byte-identical recovery is idempotent, conflicts fail closed, and only recovered `restored_failure` can authorize exactly the next attempt. Step 7 and A5 remain unchanged. This is repair reconciliation only; it makes no review-pass claim.

## Review

(filled by plan-review on completion)

## Sources

- `plugins/session-relay/bin/relay` — current launcher dispatches to four embedded target binaries.
- `plugins/session-relay/rust/Cargo.toml` — current package lacks product-version synchronization.
- `plugins/session-relay/rust/rust-toolchain.toml` — tracked source pins Rust `1.85.0` plus rustfmt/clippy.
- `.github/workflows/build-binaries.yml` — current binary workflow is manual-only and uploads Actions artifacts.
- `.github/workflows/ci.yml` — current tag CI validates but does not publish release assets.
- `scripts/ci.mjs` and `scripts/lib/plugins.mjs` — current Rust capability rebuilds and compares committed binaries.
- `scripts/release.mjs` — current release path assumes binaries are already committed and creates the GitHub Release after tag CI.
- `plugins/session-relay/test/selftest.mjs` — current test resolution falls back to committed platform executables.
- Official cargo-dist documentation — prebuilt target assets and installers are a standard Rust CLI distribution model.
- Grok Build public installer and README — end users receive platform-specific precompiled Rust binaries and do not compile locally.
- Official GitHub Actions push-event and reusable-workflow documentation — tag pushes bind the run to the tag tip, and same-repository called workflows resolve from the caller commit.

## Notes

- Companion repository: `/home/vagrant/projects/public`.
- Companion repository ID: pending
- Companion plan: `/home/vagrant/projects/public/docs/plans/active/session-relay-cli-installation.md`.
- Companion validation ref: pending
- Companion implementation commit: pending
- Companion plan input SHA-256: pending
- Companion execution base commit: pending
- Companion review receipt SHA-256: pending
- Companion TDD-red receipt JCS bytes: pending
- Companion TDD-red receipt SHA-256: pending
- Companion status: pending
- Companion blocked reason: Awaiting the four independently hashed `session-relay--v0.12.0` production asset digests.
- Docks TDD-red receipt JCS bytes: pending
- Docks TDD-red receipt SHA-256: pending
- TAG_COMMIT / SOURCE_COMMIT: pending
- PROMOTED_COMMIT / SHIPPED_COMMIT: pending until plan completion
- Producer preflight branch/ref commit: pending
- Producer preflight workflow SHA/run ID/attempt: pending
- Producer preflight artifact identities/digests: pending
- Producer preflight receipt JCS/SHA-256: pending
- Source CI receipt JCS/SHA-256: pending
- Source preparation candidate JCS/SHA-256: pending
- Intended first compatible Session Relay release: `0.12.0`.
- Release authorization was explicitly granted by the user after the first independent review and later expanded to continue without a review-round limit; Session Relay `0.12.0` plus docks-kit `0.9.0` publication are the required post-completion outcome.

## Mistakes & Dead Ends

- **2026-07-16T04:34:00-03:00**: Sealed the first review bundle under `/tmp/docks-plan-review-<uuid>` instead of the helper-owned `/tmp/docks-plan-review/<uuid>` root → policy cleanup correctly rejected it → preserved those invalidly located read-only bundles and will use the exact helper root for every fresh review.
- **2026-07-16T11:26:00-03:00**: Ran a reviewer with the sealed bundle as Codex's working directory → nested Codex initialization created `.git`/`.agents`/`.codex` mount points and invalidated directory modes → discarded that output and moved reviewer runtime state to a separate temporary work directory for all later rounds.
